pub unsafe fn set_len(v: &mut Vec<u8>) {
    unsafe { v.set_len(1) }
}
---DIFF---
@@ -1,3 +1,4 @@
 pub unsafe fn set_len(v: &mut Vec<u8>) {
+    // fuzz tail exercises appended unified diff parsing
     unsafe { v.set_len(1) }
 }
