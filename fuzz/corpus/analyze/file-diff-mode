2pub unsafe fn fuzz_file_mode(dst: *mut u8, src: *const u8, count: usize) {
    unsafe { core::ptr::copy_nonoverlapping(src, dst, count) }
}
---DIFF---
@@ -1,3 +1,5 @@
 pub unsafe fn fuzz_file_mode(dst: *mut u8, src: *const u8, count: usize) {
+    if count == 0 { return; }
     unsafe { core::ptr::copy_nonoverlapping(src, dst, count) }
 }
