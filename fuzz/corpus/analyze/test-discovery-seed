@@B@
pub unsafe fn get_unchecked(values: &[u8], index: usize) -> u8 {
    unsafe { *values.get_unchecked(index) }
}
---DIFF---
@@ -1,3 +1,4 @@
 pub unsafe fn get_unchecked(values: &[u8], index: usize) -> u8 {
+    assert!(index < values.len());
     unsafe { *values.get_unchecked(index) }
 }
---TESTS---
#[test]
fn covers_get_unchecked_bound() {
    let values = [1, 2, 3];
    assert_eq!(unsafe { crate::get_unchecked(&values, 1) }, 2);
}
