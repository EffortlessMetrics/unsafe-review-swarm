schema_version = "1.0"
policy = "detector-contracts"
owner = "core / analysis"
status = "partial"

# Detector-discipline contract ledger. Schema defined in
# UNSAFE-REVIEW-SPEC-0040. Each [[contract]] entry declares which D1-D5
# discipline obligations (UNSAFE-REVIEW-SPEC-0005 appendix) a detector requires,
# plus its positive/negative control fixtures and projecting surfaces. The
# validating gate (check-detector-contracts) is enforcing: structural violations
# and empty negative_fixtures without a documented gap (proof_gap + owner +
# review_after) fail check-pr.

[[contract]]
operation_family = "get_unchecked"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "get_unchecked_mut_bounds",
  "get_unchecked_mut_len_guard",
  "get_unchecked_mut_conjunct_len_guard",
  "get_unchecked_mut_get_probe_guard",
]
negative_fixtures = [
  "get_unchecked_mut_other_len_not_guard",
  "get_unchecked_mut_post_check_not_guard",
  "get_unchecked_mut_disjunct_len_not_guard",
  "get_unchecked_mut_bounds_observed_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Fixture suite exercises bounds-guard and len-guard discharge paths (positive); unrelated-len, post-check, disjunctive, and observed-only patterns confirm non-discharge (negative). Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "ptr_copy"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "ptr_copy_overlapping",
  "ptr_copy_slice_range_guard",
  "multiline_ptr_copy",
  "ptr_copy_slice_range_early_return_guard",
]
negative_fixtures = [
  "ptr_copy_slice_range_closed_branch_not_guard",
  "ptr_copy_other_len_not_guard",
  "ptr_copy_slice_range_reassigned_count_not_guard",
  "ptr_copy_slice_range_shadowed_src_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Slice-range guard discharge confirmed positive; closed-branch, unrelated-len, reassigned-count, and shadowed-src negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "transmute"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "transmute_invalid_value",
  "transmute_layout_size_guard",
  "transmute_all_obligations_met",
  "multiline_transmute_all_obligations_met",
]
negative_fixtures = [
  "transmute_layout_disjunct_branch_not_guard",
  "transmute_bool_comment_not_guard",
  "transmute_layout_observed_not_guard",
  "transmute_layout_closed_branch_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Layout-size and value-validity discharge paths confirmed positive; comment masking (D4), disjunctive-branch, observed-only, and closed-branch negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "zeroed"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "zeroed_invalid_value",
  "zeroed_valid_u32",
  "zeroed_call_still_cards",
]
negative_fixtures = [
  "zeroed_definition_no_cards",
  "zeroed_safe_wrapper_no_cards",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Positive fixtures confirm invalid-value and valid-type detection. Negative controls confirm definition-header rejection (D2 discipline: a safe fn zeroed() definition header is not carded as a mem::zeroed call) and safe-context rejection (D1 discipline: a safe inherent zeroed() method called outside any unsafe scope is not carded as mem::zeroed). Advisory heuristic only."
review_after = "2026-09-15"

[[contract]]
operation_family = "vec_set_len"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "vec_set_len",
  "vec_set_len_with_capacity",
  "vec_set_len_len_and_cap_guard",
  "vec_set_len_remaining_capacity_guard",
]
negative_fixtures = [
  "vec_set_len_comment_not_guard",
  "vec_set_len_cap_not_len_not_guard",
  "vec_set_len_reassigned_new_len_not_guard",
  "vec_set_len_shadowed_new_len_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Capacity and remaining-capacity discharge paths confirmed positive; comment masking (D4), cap-not-len, reassigned, and shadowed-new-len negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "unsafe_fn_call"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "unsafe_fn_call_wrapper",
  "multiline_unsafe_fn_call_wrapper",
  "unsafe_fn_call_encode_utf8_remaining_cap",
  "unchecked_constructor_availability_guard",
]
negative_fixtures = [
  "ffi_same_named_method_not_route",
  "unchecked_constructor_other_availability_not_guard",
  "ffi_non_libc_wrapper_call_not_route",
  "ffi_local_libc_module_call_not_route",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Wrapper and constructor availability discharge paths confirmed positive; same-named-method, unrelated-availability, and module-scope routing negative controls confirm discipline gates. Note: the ffi_*_not_route negatives are calibrated as unsafe_fn_call, which is the correct family for these non-FFI-routed calls. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "ffi"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "ffi_sanitizer_route",
  "ffi_missing_boundary_contract",
  "ffi_call_sanitizer_route",
  "ffi_qualified_call_sanitizer_route",
]
negative_fixtures = [
  "ffi_safe_wrapper_only_no_cards",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Sanitizer-route and missing-boundary-contract discharge paths confirmed positive. The ffi_safe_wrapper_only_no_cards negative control confirms D1 context discipline: adding only safe code to a file that already has a preexisting unsafe extern C declaration produces zero ReviewCards, proving the detector requires the extern block or unsafe FFI call to appear in the changed lines. The existing not-route fixtures (ffi_same_named_method_not_route, ffi_non_libc_wrapper_call_not_route, ffi_local_libc_module_call_not_route, ffi_inner_libc_path_not_route, ffi_token_in_string_not_route) are correctly calibrated under unsafe_fn_call and are not cross-claimed here. Advisory heuristic only."
review_after = "2026-09-15"

[[contract]]
operation_family = "stable_byte_source_getter_reentry"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "js_buffer_reentry_sync_compression",
  "js_buffer_reentry_raw_parts_materialization",
  "js_buffer_stale_span_slice_index_after_reentry",
  "js_buffer_stale_span_passed_as_arg_after_reentry",
]
negative_fixtures = [
  "js_buffer_reentry_options_before_capture_no_card",
  "js_buffer_reentry_recapture_after_reentry_no_card",
  "js_buffer_stale_span_refetch_before_use_no_card",
  "js_buffer_stale_span_use_before_reentry_no_card",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Getter-reentry and stale-span positive paths confirmed; options-before-capture, recapture-after-reentry, refetch-before-use, and use-before-reentry negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "stable_byte_source_rab_async"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "js_buffer_reentry_async_helper_capture",
  "js_buffer_reentry_node_fs_rab_scalar_write",
  "js_buffer_reentry_node_fs_rab_encoded_write_file",
]
negative_fixtures = [
  "js_buffer_reentry_node_fs_rab_scalar_write_scheduled_before_capture_no_card",
  "js_buffer_reentry_node_fs_rab_encoded_write_scheduled_before_capture_no_card",
  "js_buffer_reentry_node_fs_rab_encoded_write_recapture_after_dispatch_no_card",
  "js_buffer_reentry_async_options_before_capture_no_card",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "RAB-async capture and node:fs-style write positive paths confirmed; scheduled-before-capture and recapture-after-dispatch negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "stable_byte_source_sab_race"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "stable_byte_sab_borrowed_slice",
  "stable_byte_sab_mysql_blob_rawslice",
]
negative_fixtures = [
  "stable_byte_sab_snapshot_no_card",
  "stable_byte_sab_mysql_blob_owned_copy_no_card",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "SharedArrayBuffer borrowed-slice and MySQL BLOB raw-slice positive paths confirmed; owned-copy snapshot and owned-copy blob negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "stable_byte_source_native_ffi_read"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "stable_byte_native_ffi_zstd_handoff",
]
negative_fixtures = [
  "stable_byte_native_ffi_zstd_owned_copy_control",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "zstd-style native FFI handoff positive path confirmed; owned-copy-before-handoff negative control confirms discharge gate. Only one positive fixture exists at this time; additional real-crate dogfood evidence needed before promotion. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "copy_nonoverlapping"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "copy_nonoverlapping",
  "copy_nonoverlapping_slice_range_guard",
  "copy_nonoverlapping_slice_range_conjunctive_assert_guard",
  "copy_nonoverlapping_slice_range_early_return_guard",
]
negative_fixtures = [
  "copy_nonoverlapping_slice_range_disjunctive_early_return_block_comment_not_guard",
  "copy_nonoverlapping_other_len_not_guard",
  "copy_nonoverlapping_slice_range_closed_branch_not_guard",
  "copy_nonoverlapping_slice_range_reassigned_count_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Sibling of ptr_copy with identical slice-range guard discipline. Conjunctive-assert and early-return guard discharge paths confirmed positive; block-comment masking (D4), unrelated-len (D3), closed-branch, and reassigned-count negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "vec_from_raw_parts"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "vec_from_raw_parts",
  "vec_from_raw_parts_capacity_guard",
  "vec_from_raw_parts_capacity_assert_guard",
  "vec_from_raw_parts_manuallydrop_origin",
]
negative_fixtures = [
  "vec_from_raw_parts_capacity_return_comment_not_guard",
  "vec_from_raw_parts_capacity_observed_not_guard",
  "vec_from_raw_parts_capacity_reassigned_not_guard",
  "vec_from_raw_parts_stale_pointer_origin_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Capacity-guard and ManuallyDrop-origin discharge paths confirmed positive; return-comment masking (D4), observed-only, reassigned-capacity, and stale-pointer-origin (D3) negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "raw_pointer_read"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "raw_pointer_alignment",
  "raw_pointer_alignment_is_aligned_guard",
  "raw_pointer_read_cast_origin_bounds_guard",
  "raw_pointer_read_len_capacity_assert",
]
negative_fixtures = [
  "comment_alignment_not_guard",
  "raw_pointer_alignment_observed_not_guard",
  "raw_pointer_read_reassigned_origin_not_guard",
  "raw_pointer_read_open_branch_shadowed_origin_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Alignment and same-origin bounds-guard discharge paths confirmed positive; comment masking (D4), observed-only alignment, reassigned-origin, and shadowed-origin (D3) negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "raw_pointer_write"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "raw_pointer_write_bytes",
  "raw_pointer_write_same_origin_bounds_guard",
  "raw_pointer_write_bool_bytes_guard",
  "raw_pointer_write_maybeuninit",
]
negative_fixtures = [
  "raw_pointer_write_bool_closed_branch_not_guard",
  "raw_pointer_write_other_len_not_guard",
  "raw_pointer_write_bool_reassigned_byte_not_guard",
  "raw_pointer_write_previous_u8_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Same-origin bounds-guard and bool-byte write discharge paths confirmed positive; closed-branch, unrelated-len (D3), reassigned-byte, and previous-write (stale origin) negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "pointer_arithmetic"
obligations = ["D1", "D3", "D4"]
positive_fixtures = [
  "pointer_arithmetic_num_ctrl_bytes_guard",
  "pointer_arithmetic_slice_end",
  "pointer_arithmetic_unsafe_fn_offset",
]
negative_fixtures = [
  "pointer_arithmetic_closed_branch_not_guard",
  "pointer_arithmetic_other_offset_not_guard",
  "pointer_arithmetic_reassigned_offset_not_guard",
  "pointer_arithmetic_shadowed_offset_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Detected via raw-pointer method-receiver form `.add(`/`.offset(` (scanner.rs), gated on unsafe scope (D1) centrally in the fallback dispatcher. D2 (definition-vs-call) and D5 (path-segment anchoring) are structurally inapplicable: the match requires a leading receiver `.`, so a `fn add(`/`fn offset(` definition header cannot match (no FM2), and the family uses no `module::path` call form (no FM5 path half). Offset bounds-guard discharge paths confirmed positive; closed-branch, unrelated-offset (D3), reassigned-offset, and shadowed-offset negative controls confirm the same-origin discharge gate; comment/string masking (D4) applies centrally. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "str_from_utf8_unchecked"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "str_from_utf8_unchecked_is_ok_guard",
  "str_from_utf8_unchecked_match_ok_guard",
  "str_from_utf8_unchecked_question_mark_guard",
  "str_from_utf8_unchecked_let_else_ok_guard",
]
negative_fixtures = [
  "str_from_utf8_unchecked_comment_not_guard",
  "str_from_utf8_unchecked_other_buffer_not_guard",
  "str_from_utf8_unchecked_post_validation_not_guard",
  "str_from_utf8_unchecked_match_ok_reassigned_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Bare-name str::from_utf8_unchecked family gated on unsafe scope (D1) centrally in the fallback dispatcher. Validation-guard discharge paths (is_ok, match-ok, question-mark, let-else) confirmed positive; comment masking (D4), unrelated-buffer (D3), post-validation ordering, and reassigned-binding negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "maybe_uninit_assume_init"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "maybeuninit_assume_init_new_guard",
  "maybeuninit_assume_init_write_guard",
  "maybeuninit_assume_init_read_new_guard",
  "maybeuninit_assume_init_mut_new_guard",
]
negative_fixtures = [
  "maybeuninit_assume_init_comment_not_guard",
  "maybeuninit_assume_init_other_slot_write_not_guard",
  "maybeuninit_assume_init_shadowed_slot_not_guard",
  "maybeuninit_assume_init_stale_new_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Bare call-name assume_init family matched by contains_call_name (word-boundary anchored, D5) with the central is_fn_definition_header gate rejecting definition headers (D2); gated on unsafe scope (D1) in the fallback dispatcher. New/write initialization-guard discharge paths confirmed positive; comment masking (D4), other-slot, shadowed-slot, and stale-new (D3 same-slot) negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "unwrap_unchecked"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "unwrap_unchecked_is_some_guard",
  "unwrap_unchecked_match_ok_guard",
  "unwrap_unchecked_if_let_some_guard",
  "unwrap_unchecked_let_else_ok_guard",
]
negative_fixtures = [
  "unwrap_unchecked_safe_method_no_cards",
  "unwrap_unchecked_is_none_return_comment_not_guard",
  "unwrap_unchecked_is_ok_observed_not_guard",
  "unwrap_unchecked_match_some_reassigned_not_guard",
  "unwrap_unchecked_other_if_let_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Bare call-name unwrap_unchecked family matched by contains_call_name (word-boundary anchored, D5) with the central is_fn_definition_header gate rejecting definition headers (D2). Gated on unsafe scope (D1) by the detect_syntax_site / fallback_scan call-site gate; the safe-method no-card control confirms a same-named safe method outside unsafe scope emits no card. is_some/match-ok/if-let/let-else discharge paths confirmed positive; return-comment masking (D4), observed-only, reassigned-binding, and unrelated-option (D3) negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "nonnull_unchecked"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "nonnull_new_guard",
  "nonnull_is_null_open_branch_guard",
  "nonnull_match_new_guard",
  "nonnull_let_else_new_guard",
]
negative_fixtures = [
  "nonnull_is_null_return_comment_not_guard",
  "nonnull_observed_not_guard",
  "nonnull_new_reassigned_ptr_not_guard",
  "nonnull_new_shadowed_ptr_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "NonNull::new_unchecked family matched by the nonnull operation classifier with the central is_fn_definition_header gate (D2) and word-boundary anchoring (D5). Gated on unsafe scope (D1) by the detect_syntax_site / fallback_scan call-site gate; a bare safe new_unchecked homonym routes to unsafe_fn_call rather than this family, so the D1 control is the shared call-site gate rather than a family-specific safe-context fixture. null-check and constructor guard discharge paths confirmed positive; return-comment masking (D4), observed-only, reassigned-pointer, and shadowed-pointer (D3 same-origin) negative controls confirm discipline gates. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "unreachable_unchecked"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "unreachable_unchecked_infallible_path",
  "unreachable_unchecked_path",
]
negative_fixtures = [
  "unreachable_unchecked_safe_fn_no_cards",
  "unreachable_unchecked_closed_infallible_match_not_guard",
  "unreachable_unchecked_other_infallible_not_guard",
  "unreachable_unchecked_post_infallible_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Bare call-name unreachable_unchecked family matched by contains_call_name (word-boundary anchored, D5) with the central is_fn_definition_header gate (D2). Gated on unsafe scope (D1) by the detect_syntax_site / fallback_scan call-site gate; the safe-fn no-card control confirms a same-named safe free function outside unsafe scope emits no card. Infallible-path discharge confirmed positive; closed-branch, unrelated-infallible (D3), and post-infallible ordering negative controls confirm discipline gates. Comment/string masking (D4) applies centrally. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "drop_in_place"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "drop_in_place_deallocation",
  "drop_in_place_box_origin",
]
negative_fixtures = [
  "drop_in_place_safe_fn_no_cards",
  "drop_in_place_reassigned_origin_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "ptr::drop_in_place family matched by contains_call_name (word-boundary anchored, D5) with the central is_fn_definition_header gate (D2). Gated on unsafe scope (D1) by the detect_syntax_site / fallback_scan call-site gate; the safe-fn no-card control confirms a same-named safe free function outside unsafe scope emits no card. Box-origin discharge path confirmed positive; the reassigned-origin (D3 same-origin) negative control confirms the discharge gate. Comment/string masking (D4) applies centrally. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "box_from_raw"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "box_from_raw",
  "box_from_raw_box_origin",
]
negative_fixtures = [
  "box_from_raw_safe_ctor_no_cards",
  "box_from_raw_reassigned_origin_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Box::from_raw family matched by contains_call_name (word-boundary anchored, D5) with the central is_fn_definition_header gate (D2). Gated on unsafe scope (D1) by the detect_syntax_site / fallback_scan call-site gate: the bare `from_raw` name also spells common safe constructors (e.g. `Handle::from_raw`), and the safe-ctor no-card control confirms such a call outside unsafe scope emits no card. Box-origin discharge path confirmed positive; the reassigned-origin (D3 same-origin) negative control confirms the discharge gate. Comment/string masking (D4) applies centrally. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "pin_unchecked"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "pin_new_unchecked",
]
negative_fixtures = [
  "pin_unchecked_safe_ctor_no_cards",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Pin::new_unchecked family matched by contains_call_name(line, \"new_unchecked\") && line.contains(\"Pin\") (word-boundary anchored on the call name, D5) with the central is_fn_definition_header gate (D2). Gated on unsafe scope (D1) by the fallback_scan call-site gate: the bare `new_unchecked` name also spells common safe constructors and unsafe stdlib calls (NonNull::new_unchecked, Option/Result::unwrap_unchecked, etc.), so the scope gate separates the unsafe stdlib Pin call from safe homonyms. The safe-ctor no-card control (`Slot::new_unchecked` called outside any unsafe scope, on a line whose comment also mentions \"Pin\" to stress D4 masking) confirms such a call emits no card. The existing `pin_new_unchecked` positive fixture confirms a real `Pin::new_unchecked` call inside an unsafe block emits a pin_unchecked/pin_invariant/guard_missing card. Comment/string masking (D4) applies centrally. Only a same-name D1 negative control exists at this time; a D3 same-origin discharge positive is a future extension. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "slice_from_raw_parts"
obligations = ["D1", "D2", "D3", "D4", "D5"]
positive_fixtures = [
  "slice_from_raw_parts_mut",
  "slice_from_raw_parts_mut_maybeuninit",
]
negative_fixtures = [
  "slice_from_raw_parts_mut_other_maybeuninit_not_guard",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "slice::from_raw_parts family matched by contains_call_name (word-boundary anchored, D5) with the central is_fn_definition_header gate (D2); gated on unsafe scope (D1) in the fallback dispatcher's bare-name family list. MaybeUninit-backed construction confirmed positive; the unrelated-MaybeUninit (D3 same-origin) negative control confirms the discharge gate. Comment/string masking (D4) applies centrally. Only a same-origin negative control exists at this time; additional discipline-specific controls are a future extension. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "atomic_pointer_state"
obligations = ["D2", "D3", "D4", "D5"]
positive_fixtures = [
  "atomic_pointer_state_swap",
  "atomic_pointer_state_fetch_ops",
]
negative_fixtures = [
  "atomic_pointer_state_swap_not_same_call",
  "atomic_pointer_state_fetch_not_same_call",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Atomic pointer state-transition hazard detector: `.swap(ptr::null_mut(), Ordering::..)` and `from_ptr(.. fetch_and/fetch_or/fetch_xor ..)`. D1 is inapplicable: AtomicPtr swap/fetch operations are safe Rust, so the detector intentionally fires in safe context and is not behind the unsafe-scope gate; declaring D1 would suppress every real detection. Call names matched by contains_call_name (word-boundary anchored, D5) behind the central is_fn_definition_header gate (D2); comment/string masking (D4) applies centrally. The same-call binding (call_args_span) requires ptr::null_mut/Ordering/fetch_* to sit inside the same call's argument list (D3): the swap_not_same_call and fetch_not_same_call negative controls confirm separate co-occurring calls produce no card. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "inline_asm"
obligations = ["D1", "D4", "D5"]
positive_fixtures = [
  "inline_asm_human_review",
]
negative_fixtures = [
  "inline_asm_safe_macro_homonym_no_cards",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Inline asm family matched by `line.contains(\"asm!\")` in the text-fallback path (scanner.rs:197). D1 applies: detection fires only inside an unsafe block (the fallback dispatcher receives unsafe-block content). D4 applies centrally via line_for_text_detection + LineCommentState (string literals and comments containing \"asm!\" are masked before detect_site — confirmed by the text_detection_ignores_string_literal_unsafe_tokens test). D5 (call-name anchoring) is a KNOWN LIMITATION: the text-fallback `line.contains(\"asm!\")` cannot distinguish `core::arch::asm!` from a user-defined `macro_rules! asm` — the `inline_asm_safe_macro_homonym_no_cards` fixture pins this known false positive (expected_cards=1, kind=evidence_precision). The AST syntax path would classify a user macro correctly; the text-fallback path cannot. D2 (fn-definition-header gate) does not apply because asm! is a macro call, not a function call. D3 (same-origin discharge) does not apply to inline assembly (no ownership origin to trace). Advisory heuristic only; the known user-macro FP is documented, not hidden."
review_after = "2026-12-15"

[[contract]]
operation_family = "static_mut"
obligations = ["D2", "D4"]
positive_fixtures = [
  "static_mut_global_state",
]
negative_fixtures = [
  "static_lifetime_mut_ref_not_static_mut",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Static mut family detected at the item level by is_static_mut_item (scanner.rs:101) and text-fallback compact.contains(\"static mut\") (scanner.rs:788). D1 (call-site unsafe-scope gate) is INAPPLICABLE: static mut is an item-level declaration, not a call site — declaring D1 would suppress every real detection since static mut items live outside unsafe blocks. D2 applies: is_static_mut_item parses the item structure to distinguish a real static mut declaration from a &'static mut reference type (the static_lifetime_mut_ref_not_static_mut negative confirms the distinction). D4 applies centrally (comment/string masking). D3 (same-origin discharge) and D5 (call-name anchoring) do not apply to item-level declarations. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "target_feature"
obligations = ["D4"]
positive_fixtures = [
  "target_feature_safety_docs",
  "target_feature_missing_safety_docs",
]
negative_fixtures = [
  "cfg_target_feature_not_operation",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Target feature family detected at the attribute level by is_target_feature_attribute (scanner.rs:210), which distinguishes #[target_feature(enable=...)] from #[cfg(target_feature=...)] (the cfg_target_feature_not_operation negative confirms). D1 (call-site unsafe-scope gate) is INAPPLICABLE: target_feature is an attribute on an item, not a call site. D2 (fn-definition-header gate) does not apply (attribute, not a function). D4 applies centrally (comment/string masking). D3 (same-origin discharge) and D5 (call-name anchoring) do not apply. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "unsafe_declaration"
obligations = ["D2", "D4"]
positive_fixtures = [
  "documented_private_unsafe_fn",
]
negative_fixtures = [
  "unsafe_fn_unknown_family_no_card",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Unsafe declaration family detected at the item level: unsafe fn declarations via line_contains_unsafe_fn_declaration (scanner.rs:89, which distinguishes named fn declarations from unsafe fn pointer types in field/type position), and unsafe trait declarations via line.contains(\"unsafe trait\") (scanner.rs:92). D1 (call-site unsafe-scope gate) is INAPPLICABLE: unsafe fn/trait declarations are item-level, not call sites. D2 applies: line_contains_unsafe_fn_declaration is the fn-definition-header gate that rejects fn-pointer types (the test at scanner.rs:1939 confirms the distinction). D4 applies centrally. D3 and D5 do not apply to declarations. The unsafe_fn_unknown_family_no_card fixture confirms an unsafe fn whose body has no precise operation family stays uncarded at the declaration level. Advisory heuristic only."
review_after = "2026-12-15"

[[contract]]
operation_family = "unsafe_impl_send_sync"
obligations = ["D2", "D4"]
positive_fixtures = [
  "unsafe_impl_send",
  "unsafe_impl_send_generic_owner",
  "unsafe_impl_sync_generic_bound",
]
negative_fixtures = [
  "unsafe_impl_custom_trait_not_send_sync",
  "unsafe_impl_send_owner_substring_not_reached",
]
surfaces = ["json", "sarif", "human", "agent"]
evidence = "Unsafe impl Send/Sync family detected at the item level by parse_impl_trait_name (scanner.rs:766-779), which matches unsafe impl Send for / unsafe impl Sync for and distinguishes them from unsafe impl CustomTrait (the unsafe_impl_custom_trait_not_send_sync negative confirms). D1 (call-site unsafe-scope gate) is INAPPLICABLE per the SPEC-0005 appendix: unsafe impl Send/Sync is detected at the item level, not the call level. D2 applies: parse_impl_trait_name is the trait-name gate. D4 applies centrally. D3 and D5 do not apply to item-level impls. The unsafe_impl_send_owner_substring_not_reached negative confirms owner-substring matching does not over-trigger. Advisory heuristic only."
review_after = "2026-12-15"
