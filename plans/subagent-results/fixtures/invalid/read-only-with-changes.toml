# Invalid: read-only action with changes.
schema = "bounded-subagent-result-v1"
action = "investigate"
verdict = "clear"
summary = "Read-only claims changes."
changes = ["docs/README.md"]
proof = ["cargo test --locked -p xtask -- subagent_results"]

[work_item]
issue = "https://github.com/EffortlessMetrics/unsafe-review-swarm/issues/1924"
work_spec = "plans/work-specs/examples/UNSAFE-REVIEW-WORK-1924.toml"

[basis]
base_sha = "0f306ea0b6737c13df7abb97bdebb819eaeffdc7"

[[findings]]
severity = "P2"
claim = "Read-only with mutation."
evidence = ["xtask/src/subagent_results.rs:1"]

[uncertainty]
items = ["Should have empty changes."]

[recommended_next_action]
action = "Remove changes for read-only."
