# Support

This extension is advisory and read-only in v0.x. It does not execute witnesses,
does not edit source files, and does not enforce policy by default.

The saved-bundle runtime is covered by the extension host smoke lane. The
extension still does not provide a live LSP client; runtime behavior remains
read-only and is limited to the saved bundle.
