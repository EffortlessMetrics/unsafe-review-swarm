# Support

This extension is advisory and read-only in v0.x. It does not execute witnesses,
does not edit source files, and does not enforce policy by default.

The current scaffold is packaging-only. Runtime diagnostics, hovers, and
command-only actions remain blocked on the extension LSP-client lane.
