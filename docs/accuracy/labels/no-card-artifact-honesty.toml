schema_version = "0.1"
status = "fixture_pinned"
claim_id = "no-card-artifact-honesty"
operation_family = "none"
hazard = "none"
partition = "fixture"
source_kind = "fixture_golden"
trust_boundary = """
Static unsafe contract review only. These labels describe fixture-pinned
zero-card artifact behavior; they are not a proof of memory safety, not UB-free status, not a Miri result, not Miri-clean status, not site execution evidence, not calibrated precision or recall, not witness adequacy, and not policy readiness.
"""

[[samples]]
id = "safe-code-emits-no-cards"
fixture = "safe_code_no_cards"
kind = "negative"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A safe Rust-only change should not create advisory ReviewCards or imply an unsafe-review gap."

[[samples]]
id = "safe-reference-deref-emits-no-cards"
fixture = "safe_reference_deref_no_cards"
kind = "negative"
expected_cards = 0
label_source = "fixture_golden"
rationale = "Dereferencing a safe Rust reference is not an unsafe seam and should emit no ReviewCards."

[[samples]]
id = "unsafe-import-paths-emit-no-cards"
fixture = "imports_not_unsafe_operations"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "Importing or mentioning unsafe operation paths is not itself an executed unsafe operation."

[[samples]]
id = "cfg-target-feature-predicate-emits-no-cards"
fixture = "cfg_target_feature_not_operation"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A cfg target_feature predicate is configuration text, not a changed unsafe operation."

[[samples]]
id = "static-lifetime-mut-reference-emits-no-cards"
fixture = "static_lifetime_mut_ref_not_static_mut"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "`'static mut` reference type text is not a `static mut` global item and should emit no ReviewCards."

[[samples]]
id = "adjacent-unchanged-unsafe-declaration-emits-no-cards"
fixture = "adjacent_unchanged_unsafe_fn_no_card"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "An unchanged adjacent unsafe declaration should not become a ReviewCard for a neighboring safe edit."

[[samples]]
id = "unsafe-fn-pointer-type-in-field-emits-no-cards"
fixture = "unsafe_fn_pointer_field_no_cards"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A struct field whose type is an `unsafe fn(...)` fn-pointer is not an unsafe fn declaration and must not produce a ReviewCard."

[[samples]]
id = "safe-method-add-outside-unsafe-scope-emits-no-cards"
fixture = "pointer_arithmetic_safe_method_add_no_cards"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A safe inherent `.add()` method on a non-pointer newtype called outside any unsafe scope is not pointer arithmetic and must not produce a ReviewCard."

[[samples]]
id = "safe-set-len-method-outside-unsafe-scope-emits-no-cards"
fixture = "set_len_safe_method_no_cards"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A safe inherent `set_len()` method called outside any unsafe scope is not Vec::set_len and must not produce a ReviewCard."

[[samples]]
id = "safe-from-raw-parts-ctor-outside-unsafe-scope-emits-no-cards"
fixture = "from_raw_parts_safe_ctor_no_cards"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A safe inherent `from_raw_parts()` constructor called outside any unsafe scope is not slice::from_raw_parts and must not produce a ReviewCard."

[[samples]]
id = "safe-assume-init-method-outside-unsafe-scope-emits-no-cards"
fixture = "assume_init_safe_method_no_cards"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A safe inherent `assume_init()` method called outside any unsafe scope is not MaybeUninit::assume_init and must not produce a ReviewCard."

[[samples]]
id = "safe-from-utf8-unchecked-method-outside-unsafe-scope-emits-no-cards"
fixture = "from_utf8_unchecked_safe_wrapper_no_cards"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A safe inherent `from_utf8_unchecked()` method called outside any unsafe scope is not str::from_utf8_unchecked and must not produce a ReviewCard."

[[samples]]
id = "safe-zeroed-method-outside-unsafe-scope-emits-no-cards"
fixture = "zeroed_safe_wrapper_no_cards"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A safe inherent `zeroed()` constructor called outside any unsafe scope is not mem::zeroed and must not produce a ReviewCard."

[[samples]]
id = "path-alias-ending-in-ptr-not-raw-pointer-read"
fixture = "ptr_read_path_segment_not_raw"
kind = "false_positive_control"
expected_cards = 0
label_source = "fixture_golden"
rationale = "A call path alias whose name ends in `ptr` (e.g. registry_ptr::read_entry) is not a std::ptr module call and must not produce a raw-pointer-read ReviewCard."
