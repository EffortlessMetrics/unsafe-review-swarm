# 2026-06-14 - release 0.3.7 preparation

Scope: prepare the synchronized `0.3.7` adoption, telemetry, and real-code
low-noise improvements release from source `main` after the history-preserving
swarm import on branch `release/0.3.7-adoption-telemetry`.

This is release preparation only. It does not publish crates, add analyzer
behavior, execute witnesses, post comments, edit downstream source, or start
policy gating.

## What this release carries

Branch `release/0.3.7-adoption-telemetry` imports swarm main `e24c8ae2`
(history-preserved `--no-ff` merge), covering the post-0.3.6
adoption/telemetry/real-code low-noise lane:

**Adoption surface:**
- Composite GitHub Action (`unsafe-review-first-pr`) for two-line CI adoption;
  advisory, no blocking/comment-posting/inherited-fail (#1628, #1660–#1664).
- `unsafe-review pr` zero-config subcommand with repo/base autodetect (#1629).
- Adoption front-door `docs/START-HERE.md` and outward-facing docs (#1635,
  #1669).
- External-first peak-RSS posture ratified; scheduled full-corpus backstop +
  external RSS harness (SPEC-0039) (#1627, #1631).

**Telemetry and measurement:**
- Usefulness telemetry projected from `ReviewCard` (#1630, #1634, #1647).
  Telemetry is a usage/subset signal, not calibrated precision/recall.

**Framing and evidence discipline (stance/projection program #1705–#1718):**
- SARIF level and LSP severity derived from card class (#1706, #1713).
- `unsafe-review-gate.json` from repo mode (#1705).
- Receipt formal-tool provenance for `site_reached` (#1707).
- Outcome markdown unchanged-cards parity (#1708).
- Test reach requires call-shaped owner use; self-reach rejected (#1709).
- Owner cards grouped in human surfaces, kept in artifacts (#1710).
- `debug_assert` does not discharge runtime guard obligations (#1715).
- Per-card coverage-movement projection (#1716).
- `unsafe-review+` badge baseline-movement-aware (#1717).
- `summary.unsafe_sites` is the real pre-cap site count (#1718).

**Truthfulness and selection:**
- Agent/LSP readiness unified (#1633). Comment-plan ranking + bounded body
  (#1645, #1646). `pub(crate)`/`pub(super)` fix (#1666). Spread-aware
  selection (#1667). Multiple analysis false-positive fixes (scope, reach,
  alignment, null-check, pointer type/position, set_len, combined-diff).

**Documentation and corpus:**
- ADR-0009, PRINCIPLES.md, detector-discipline, FM taxonomy, agent-orchestration
  doctrine (#1714, #1719). Specs learnings (#1668). Dogfood validation (#1700,
  #1720).

## Import parentage

- previous source `main` parent: `b7250ff2` (0.3.6 publication receipt, source
  PR #535),
- swarm `main` parent imported: `e24c8ae2` (50 commits above source main,
  full post-0.3.6 adoption/telemetry/stance lane),
- merge model: `git switch -c release/0.3.7-adoption-telemetry public/main`
  then `git merge --no-ff origin/main`; conflicts in `CHANGELOG.md` (source
  placeholder vs swarm empty Unreleased) and `docs/handoffs/README.md` (source
  empty vs swarm new rows) resolved toward swarm reviewed state.

## Version bump

```text
unsafe-review-core  0.3.6 -> 0.3.7
unsafe-review-cli   0.3.6 -> 0.3.7
unsafe-review       0.3.6 -> 0.3.7
```

Cargo.lock: exactly three `unsafe-review*` version lines changed, no
transitive dependency movement.

## Validation (to run before publish)

- `cargo fmt --all --check`
- `cargo check --workspace --all-targets --locked`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked`
- `cargo run --locked -p xtask -- check-pr`
- `cargo package -p unsafe-review-core --list` / `-cli` / facade
- `cargo publish -p unsafe-review-core --dry-run` (must pass standalone)
- `cargo publish -p unsafe-review-cli --dry-run` (expected to fail pre-publish)
- `cargo publish -p unsafe-review --dry-run` (expected to fail pre-publish)
- Release-surface smoke: `first-pr` against `fixtures/raw_pointer_alignment`,
  `xtask check-first-pr-artifacts`, `unsafe-review support`.

## Publish order (manual, after authorization)

`unsafe-review-core` -> `unsafe-review-cli` -> `unsafe-review`, then tag
`v0.3.7`, GitHub Release ("v0.3.7 — adoption, telemetry, and real-code
low-noise improvements"), publication receipt, and mirror release metadata
back to `unsafe-review-swarm`.

## Trust boundary

`unsafe-review` 0.3.7 remains advisory static unsafe coverage evidence. No
proof, UB-free, Miri-clean, site-execution, or calibrated precision/recall
claims. Telemetry is a usage/subset signal. The GitHub Action is advisory with
no default blocking, comment-posting, or witness execution. No witnesses run,
no comments posted, no source edited, no blocking by default.
