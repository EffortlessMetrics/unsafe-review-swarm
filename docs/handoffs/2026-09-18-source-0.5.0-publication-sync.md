# Source 0.5.0 publication receipt sync

Source PR `EffortlessMetrics/unsafe-review#572` (`release/0.5.0-receipt`)
merged 2026-09-17 as merge commit `f1eda818`, carrying exactly one content
commit (`3ece0f4a`, `release: record 0.5.0 publication receipt`):
`docs/releases/0.5.0-publication-receipt.md` (59 lines). No version bumps,
lockfile, changelog, or code changes beyond the checkpoint already mirrored
by #2252.

## What was mirrored

- `docs/releases/0.5.0-publication-receipt.md` checked out verbatim from
  `source/main` at `f1eda818`. It records crates `unsafe-review-core`,
  `unsafe-review-cli`, and `unsafe-review` published at `0.5.0` (dry run
  `35175350041` green, publish run `35177910918` success), the 0.5.0 feature
  set, carried-over proof, public smoke (installed binary SHA-256
  `3a398710dd97fd941576968ee6ad38ae0c24dfc394843c634e51bab4bbff7b4c`),
  and the advisory trust boundary.

## Observed follow-through (not mirrored, already public)

- Tag `v0.5.0` exists on source and points at `f1eda818`.
- GitHub Release `v0.5.0` published `2026-09-17T05:57:40Z`.
- The receipt's "separate explicit go" for tag and Release has therefore
  happened since the receipt was written.

## Acknowledgment

`policy/source-sync.toml` now acknowledges source main `f1eda818` via this
document. This mirror moves release metadata and a bookkeeping checkpoint
only: no crates published, no behavior changed, no witnesses run, no
comments posted, no downstream source edited, no policy gating started. No
safety, UB-free, Miri-clean, site-execution, calibrated precision/recall,
or policy-readiness claim follows; the advisory trust boundary carries over
unchanged.
