# 0.5.0 publication mirror

Source PR `EffortlessMetrics/unsafe-review#570` merged 2026-09-17 as merge
commit `71cf254dc9561fb8790c753fe994049e72e77624` (two parents: source
`8f67aaa5` + swarm `f281130e`; history-preserving, no squash), carrying the
six 0.5.0 hill-climb slices, the cleanup/doc/test/UX round, and workbench
records. Release-prep #571 merged as `e718c063` (synchronized 0.5.0 bump,
dated changelog, version-stamped golden re-bless).

## Publication record

- Source main at publish: `e718c063`.
- Crates published in order via the source `crates-publish` workflow
  (dry run `35175350041` green, publish run `35177910918` success):
  `unsafe-review-core 0.5.0`, `unsafe-review-cli 0.5.0`,
  `unsafe-review 0.5.0` (all three resolve on the public index).
- Tag `v0.5.0` and the GitHub Release are not created; each requires a
  separate explicit go.
- Public smoke from crates.io into a clean prefix: `--version` reports
  `0.5.0`, doctor green, representative first-pr bundle written with
  `check-first-pr-artifacts` ok, unknown flag exits 2. Installed binary
  SHA-256 `3a398710dd97fd941576968ee6ad38ae0c24dfc394843c634e51bab4bbff7b4c`
  (release-build crate install).
- Release-readiness corpus: `dogfood-exec --strict` 15 ok / 0 failed.
- Source receipt: `docs/releases/0.5.0-publication-receipt.md`
  (source PR #572).

## Acknowledgment

`policy/source-sync.toml` now acknowledges source main `e718c063` via this
document. No safety, UB-free, Miri-clean, site-execution, or calibrated
accuracy claim follows from publication; the advisory trust boundary carries
over unchanged. Precision measurement (#2223) and guard-evidence separation
(#2226) remain open workbench levers.
