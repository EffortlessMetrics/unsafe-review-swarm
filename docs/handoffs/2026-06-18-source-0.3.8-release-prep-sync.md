# 2026-06-18 - source 0.3.8 release-prep sync

Scope: mirror the source 0.3.8 corpus control-plane release-preparation state
into the swarm workbench and move the acknowledged source-sync checkpoint.

This is a source-sync handoff. It does not publish crates, push a tag, cut a
GitHub Release, execute witnesses, post comments, edit downstream source, or
start policy gating.

## What source did

- Source PR #542 (`release/0.3.8-corpus-control-plane`) merged the
  history-preserving swarm import and release-preparation work, then squash
  merged to source `main` at `9751f956`.
- The source release branch imported the corpus control-plane,
  `unsafe_declaration`, explicit comment surfacing, runtime workspace-root, and
  surface-determinism swarm lane through #1753.
- Source crate metadata was prepared for `0.3.8`:
  `unsafe-review-core`, `unsafe-review-cli`, and `unsafe-review`.
- Source release notes, changelog, and preparation handoff were written for the
  0.3.8 corpus control-plane and declaration/surfacing correctness patch.
- Source PR #542 hosted checks passed: Rust result, policy contracts, Droid,
  CodeRabbit, and GitGuardian. Cursor Bugbot was neutral because usage limits
  prevented a run.

## What this mirror brings into swarm

- The synchronized 0.3.8 version bumps in all three `crates/*/Cargo.toml` and
  the matching `Cargo.lock` entries.
- `docs/releases/0.3.8-corpus-control-plane.md` (release notes).
- `docs/handoffs/2026-06-18-release-0.3.8-preparation.md` (source-side release
  preparation handoff).
- The dated `## 0.3.8 - 2026-06-18` `CHANGELOG.md` section, with a fresh empty
  `Unreleased` section above it.
- This source-sync handoff and its `docs/handoffs/README.md` index row.
- The moved `policy/source-sync.toml` checkpoint
  (`acknowledged_source_main = 9751f9567c21a64e830f2a64217fba04eb49b976`).

## Why swarm was behind source

Release preparation is source-side work by policy. Source gained the 0.3.8
release-preparation squash merge after the swarm product lane was promoted. This
sync records that source movement in the workbench and lets the source-sync
guard report no unacknowledged source commits before routine swarm development
resumes.

## Known follow-up

- Publication is still pending explicit owner authorization: crates.io publish,
  tag, GitHub Release, publication receipt, install smoke, and the later
  publication-sync checkpoint remain separate release actions.

## Trust boundary

`unsafe-review` 0.3.8 remains advisory static unsafe coverage evidence. This
handoff makes no proof, UB-free, Miri-clean, site-execution, calibrated
precision/recall, or policy-readiness claim. It mirrors release-preparation
metadata and a bookkeeping checkpoint only.
