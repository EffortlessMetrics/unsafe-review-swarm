# 2026-06-10 - source 0.3.5 publication sync

Scope: mirror the source 0.3.5 instrument-truthfulness publication into the
swarm workbench and move the acknowledged source-sync checkpoint.

This is a mirror handoff. It does not publish crates, change behavior, run
witnesses, post comments, edit downstream source, or start policy gating.

## What source did

- Source PR #533 (`release/0.3.5-instrument-truthfulness`) merged the
  history-preserving swarm import (`8b1f9692`, importing swarm main `13a33b31`)
  together with the 0.3.5 release prep: synchronized `0.3.4 -> 0.3.5` version
  bump, dated `0.3.5` CHANGELOG section, 0.3.5 release notes, and the release
  preparation handoff. Release merge commit `9b0b8ddd` (source `main`).
- Crates published in dependency order: `unsafe-review-core 0.3.5`,
  `unsafe-review-cli 0.3.5`, `unsafe-review 0.3.5` (crates.io sparse index
  confirms `0.3.5` for all three).
- Tag `v0.3.5` -> source `9b0b8ddd`; GitHub Release
  "v0.3.5 — instrument-truthfulness patch" (Latest, 2026-06-08).

## What this mirror brings into swarm

- the synchronized 0.3.5 version bumps and `Cargo.lock`,
- `docs/releases/0.3.5-instrument-truthfulness.md`,
- `docs/handoffs/2026-06-08-release-0.3.5-preparation.md`,
- the dated `## 0.3.5 - 2026-06-08` `CHANGELOG.md` section (swarm's prior
  `## Unreleased` entries converted to the dated section, with a fresh empty
  `## Unreleased` restored, and the two `Changed` entries missing from swarm's
  `Unreleased` — nested-checkout exclusion #1552 and malformed-diff rejection
  #1516 — added so the swarm `0.3.5` section reaches parity with source),
- the updated `docs/handoffs/README.md` index rows,
- the moved `policy/source-sync.toml` checkpoint
  (`acknowledged_source_main = 9b0b8ddde748128b324110ef47658a33ace3d75f`).

## Why swarm was briefly behind source

Release preparation and publication are source-side work by policy
(`docs/contributing/SWARM_TO_MAIN.md` routing). During publication, source
gained the catch-up import + release-prep merge (#533) that swarm did not have.
This mirror restores parity and the checkpoint records that the divergence is
acknowledged and absorbed.

## What shipped (the instrument-truthfulness batch)

unsafe-review 0.3.5 makes the instrument's core assertions exact for downstream
automation without adding analyzer breadth: nested git checkout / gitfile
worktree scan exclusion (#1552), unparseable `--diff` rejected as an explicit
input error (#1516), valid empty diff treated as a complete diff-scoped no-op
(#1558), documented exit-code contract 0/1/2 (#1518), `cards.json` /
`--format json` schema `0.1` -> `0.2` additive provenance block (#1517),
`repo --max-cards` partial status sidecar with unified `stop_reason` and
SPEC-0035 field reconciliation (#1545), and CLI guide scan-cost / large-repo
scoping documentation (#1546).

## Known source-side follow-ups (observed, not fixed here)

- No `docs/releases/0.3.5-publication-receipt.md` is committed in source yet,
  although crates, tag, and GitHub Release are live. The 0.3.3/0.3.4 pattern
  committed a publication receipt; the 0.3.5 receipt is still pending source-side.
- Source `CHANGELOG.md` carries a duplicated `## 0.3.4 - 2026-06-07` section.
  This duplication was **not** mirrored into swarm; swarm keeps a single, clean
  `0.3.4` section.

## Trust boundary

`unsafe-review` 0.3.5 remains advisory static unsafe coverage evidence. No
proof, UB-free, Miri-clean, site-execution, or calibrated precision/recall
claims. No witness execution, comments, source edits, or default blocking.
Manual candidates remain manual/advisory; confirmation cues are emitted, never
executed by default.
