# 2026-05-26 source 0.2.1 publication sync

Status: source-to-swarm release metadata sync

This handoff records the source-only `0.2.1` release-preparation and
publication work and mirrors the release metadata needed by the swarm
workbench. This is not a new analyzer lane.

Source PRs:

| Source PR | Source commit | Surface | Swarm status |
|---|---|---|---|
| `EffortlessMetrics/unsafe-review#497` | `1c9ec7d` | Prepared `0.2.1` version metadata and release-prep handoff | Mirrored by this sync as version metadata |
| `EffortlessMetrics/unsafe-review#498` | `27fa121` | Recorded `0.2.1` publication receipt | Recorded by this sync handoff and source-sync checkpoint |

Published release:

| Crate | Version | crates.io | docs.rs |
|---|---:|---|---|
| `unsafe-review-core` | `0.2.1` | <https://crates.io/crates/unsafe-review-core> | <https://docs.rs/unsafe-review-core/0.2.1/unsafe_review_core/> |
| `unsafe-review-cli` | `0.2.1` | <https://crates.io/crates/unsafe-review-cli> | <https://docs.rs/unsafe-review-cli/0.2.1/unsafe_review_cli/> |
| `unsafe-review` | `0.2.1` | <https://crates.io/crates/unsafe-review> | <https://docs.rs/unsafe-review/0.2.1/unsafe_review/> |

Tag and release:

- tag: `v0.2.1`
- tag target: `1c9ec7d81f65e1c87f414c3f70a0613471d8f498`
- GitHub Release: <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.2.1>
- latest release API returned `tag_name = v0.2.1`

Published smoke recorded in source:

- `cargo search unsafe-review --limit 10` reported all three crates at `0.2.1`.
- `cargo install unsafe-review --version 0.2.1 --locked --root target/install-published-0.2.1` installed `unsafe-review.exe`.
- installed `unsafe-review --version` reported `unsafe-review 0.2.1`.
- installed `unsafe-review doctor` passed first-run readiness checks with missing witness tools as informational.
- installed `unsafe-review first-pr` wrote the standard advisory bundle for `fixtures/raw_pointer_alignment`.
- `check-first-pr-artifacts` passed on the published first-pr bundle.
- installed `unsafe-review explain` and `unsafe-review support` preserved reviewer-first and advisory posture wording.
- installed `unsafe-review badges --out target/published-0.2.1-badges` emitted numeric `message` fields for both endpoint JSON files.
- docs.rs pages for all three `0.2.1` crates returned `200`.
- the crates.io README API for `unsafe-review 0.2.1` returned the facade CLI entrypoint text.

Swarm sync:

- `crates/unsafe-review-core/Cargo.toml` version is now `0.2.1`.
- `crates/unsafe-review-cli/Cargo.toml` version and local core dependency are now `0.2.1`.
- `crates/unsafe-review/Cargo.toml` version and local CLI/core dependencies are now `0.2.1`.
- `Cargo.lock` records the three workspace crates at `0.2.1`.
- `policy/source-sync.toml` acknowledges source main at
  `27fa121cdd6ec36a9788e54072bf4d6f01f2970a`.

Boundaries:

- no analyzer behavior change
- no source badge count import into swarm badge endpoints
- no coverage badge promotion to source
- no witness execution
- no automatic comments
- no source edits by `unsafe-review`
- no default blocking policy
- no safety, UB-free, Miri-clean, site-execution, precision, or recall claim

Validation:

- `cargo run --locked -p xtask -- check-docs`
- `cargo run --locked -p xtask -- check-pr`
- `cargo run --locked -p xtask -- source-divergence`
- `git diff --check`

Expected after merge:

- `cargo run --locked -p xtask -- source-divergence` reports
  `new_source_commits=0`.
