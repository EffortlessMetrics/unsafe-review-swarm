# 2026-05-31 source 0.3.0 publication sync

Status: source-to-swarm release metadata sync

This handoff records the source-only `0.3.0` advisory review cockpit
release-preparation and publication work and mirrors the release metadata needed
by the swarm workbench. This is not a new analyzer lane.

Source PRs and commits:

| Source PR / commit | Source commit | Surface | Swarm status |
|---|---|---|---|
| `fb82b859` | `fb82b859` | Promoted advisory review-kit artifacts: manifest, bounded GitHub summary, PR summary cockpit, and first-pr handoff | Acknowledged as part of this 0.3.0 release sync |
| `EffortlessMetrics/unsafe-review#500` | `97fb957` | Promoted comment-plan review-budget metadata | Acknowledged as part of this 0.3.0 release sync |
| `EffortlessMetrics/unsafe-review#501` | `c638a25` | Promoted repair queue artifact and agent-safe context boundaries | Acknowledged as part of this 0.3.0 release sync |
| `EffortlessMetrics/unsafe-review#502` | `7b10891` | Promoted receipt-audit first-pr artifact | Acknowledged as part of this 0.3.0 release sync |
| `EffortlessMetrics/unsafe-review#503` | `98585f3` | Prepared `0.3.0` version metadata and release-prep handoff | Mirrored by this sync as version metadata |
| `EffortlessMetrics/unsafe-review#504` | `241a5f5` | Recorded `0.3.0` publication receipt | Recorded by this sync handoff and source-sync checkpoint |

Published release:

| Crate | Version | crates.io | docs.rs |
|---|---:|---|---|
| `unsafe-review-core` | `0.3.0` | <https://crates.io/crates/unsafe-review-core/0.3.0> | <https://docs.rs/unsafe-review-core/0.3.0/unsafe_review_core/> |
| `unsafe-review-cli` | `0.3.0` | <https://crates.io/crates/unsafe-review-cli/0.3.0> | <https://docs.rs/unsafe-review-cli/0.3.0/unsafe_review_cli/> |
| `unsafe-review` | `0.3.0` | <https://crates.io/crates/unsafe-review/0.3.0> | <https://docs.rs/unsafe-review/0.3.0/unsafe_review/> |

Tag and release:

- tag: `v0.3.0`
- tag target: `98585f3d0a155031a078c112844df4f2f5d3439c`
- GitHub Release: <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.3.0>
- GitHub Release was non-draft and non-prerelease.

Published smoke recorded in source:

- `cargo search unsafe-review --limit 10` reported all three crates at `0.3.0`.
- `cargo info unsafe-review-core@0.3.0 --registry crates-io` reported
  `version: 0.3.0`.
- `cargo info unsafe-review-cli@0.3.0 --registry crates-io` reported
  `version: 0.3.0`.
- `cargo info unsafe-review@0.3.0 --registry crates-io` reported
  `version: 0.3.0`.
- `cargo install unsafe-review --version 0.3.0 --locked --root target/install-published-0.3.0`
  installed `unsafe-review.exe`.
- installed `unsafe-review --version` reported `unsafe-review 0.3.0`.
- installed `unsafe-review doctor` passed first-run readiness checks with
  missing witness tools as informational.
- installed `unsafe-review first-pr` wrote the standard advisory review kit for
  `fixtures/raw_pointer_alignment`.
- `check-first-pr-artifacts` passed on the published first-pr review kit.
- installed `unsafe-review explain`, `unsafe-review context --json`, and
  `unsafe-review support` preserved reviewer-first, bounded-agent, and advisory
  posture wording.
- installed `unsafe-review badges --out target/published-0.3.0-badges` emitted
  numeric `message` fields for both endpoint JSON files.
- docs.rs pages for all three `0.3.0` crates returned `200`.

Published first-pr review kit:

```text
review-kit.json
cards.json
pr-summary.md
github-summary.md
cards.sarif
comment-plan.json
witness-plan.md
receipt-audit.md
lsp.json
repair-queue.json
```

Swarm sync:

- `crates/unsafe-review-core/Cargo.toml` version is now `0.3.0`.
- `crates/unsafe-review-cli/Cargo.toml` version and local core dependency are
  now `0.3.0`.
- `crates/unsafe-review/Cargo.toml` version and local CLI/core dependencies are
  now `0.3.0`.
- `Cargo.lock` records the three workspace crates at `0.3.0`.
- `policy/source-sync.toml` acknowledges source main at
  `241a5f5f9f24cf32e602b48d85b70146fbc79780`.

Boundaries:

- no analyzer behavior change
- no source badge count import into swarm badge endpoints
- no Bun finding or vulnerability claim
- no witness execution
- no automatic comments
- no source edits by `unsafe-review`
- no default blocking policy
- no safety, UB-free, Miri-clean, site-execution, precision, recall, or
  policy-readiness claim

Validation:

- `cargo run --locked -p xtask -- check-docs`
- `cargo run --locked -p xtask -- check-pr`
- `cargo run --locked -p xtask -- source-divergence`
- `git diff --check`

Expected after merge:

- `cargo run --locked -p xtask -- source-divergence` reports
  `new_source_commits=0`.
