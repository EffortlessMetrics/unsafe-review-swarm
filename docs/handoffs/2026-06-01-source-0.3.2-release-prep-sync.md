# 2026-06-01 source 0.3.2 release-prep sync

Status: source-to-swarm release-preparation metadata sync

This handoff records the source-only `0.3.2` repo-dogfood release-preparation
work and mirrors the metadata needed by the swarm workbench. This is not a
publication receipt. No crate, tag, or GitHub Release is claimed here.

Source PRs and commits:

| Source PR / commit | Source commit | Surface | Swarm status |
|---|---|---|---|
| `EffortlessMetrics/unsafe-review#508` | `b399ece0` | Preserved the repo-dogfood swarm batch in source with merge-commit mode | Acknowledged by this sync as source history repair |
| `EffortlessMetrics/unsafe-review#509` | `6d660916` | Prepared `0.3.2` repo-dogfood usability patch metadata and release-prep handoff | Mirrored by this sync as version metadata |

Publication status:

- `0.3.2` is prepared in source but not published.
- No `v0.3.2` tag was created.
- No GitHub Release for `v0.3.2` was created.
- Crates.io still reports the public crates at `0.3.1`.
- An earlier local pass checked only `CARGO_REGISTRY_TOKEN` and home cargo
  credentials before stopping. That is not sufficient evidence of a
  repository-level publication blocker. Future publication work must follow
  `docs/releases/CRATES_IO_PATCH_RELEASE.md` and check the repo-specific
  credential/publish path before claiming a blocker.

Swarm sync:

- `crates/unsafe-review-core/Cargo.toml` version is now `0.3.2`.
- `crates/unsafe-review-cli/Cargo.toml` version and local core dependency are
  now `0.3.2`.
- `crates/unsafe-review/Cargo.toml` version and local CLI/core dependencies are
  now `0.3.2`.
- `Cargo.lock` records the three workspace crates at `0.3.2`.
- Source release-preparation metadata is mirrored:
  - `docs/releases/0.3.2-repo-dogfood-usability.md`
  - `docs/releases/CRATES_IO_PATCH_RELEASE.md`
  - `docs/handoffs/2026-05-31-release-0.3.2-preparation.md`
- `policy/source-sync.toml` acknowledges source main at
  `6d660916a02a47ea82c46c2dabf1fa44c2e44706`.

Boundaries:

- no publication claim
- no tag or GitHub Release claim
- no Bun vulnerability claim
- no witness execution
- no automatic comments
- no source edits by `unsafe-review`
- no default blocking policy
- no safety, UB-free, Miri-clean, site-execution, precision, recall, or
  policy-readiness claim

Validation:

- `cargo run --locked -p xtask -- check-docs`
- `cargo run --locked -p xtask -- check-pr`
- `cargo run --locked -p xtask -- source-divergence`
- `git diff --check`

Expected after merge:

- `cargo run --locked -p xtask -- source-divergence` reports
  `new_source_commits=0`.
