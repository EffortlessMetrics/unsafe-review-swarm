# 2026-06-07 - source 0.3.4 publication sync

Scope: mirror the source 0.3.4 publication into the swarm workbench and move
the acknowledged source-sync checkpoint.

This is a mirror handoff. It does not publish crates, change behavior, run
witnesses, post comments, edit downstream source, or start policy gating.

## What source did

- Source PR #529 merged the history-preserving swarm import (merge commit
  `8a1979f1`, importing swarm main `3805a0de`; tree byte-identical to validated
  swarm main, hash `09afcdc57beb8b029779cb1bf20616cdaefa02d5`) together with the
  0.3.4 release prep: synchronized 0.3.3 -> 0.3.4 version bump, 0.3.4 release
  notes, release-preparation handoff, CHANGELOG, and patch-release runbook
  update. Release merge commit `bb59c270`.
- Crates published in dependency order on 2026-06-07:
  `unsafe-review-core 0.3.4`, `unsafe-review-cli 0.3.4`, `unsafe-review 0.3.4`;
  docs.rs returned HTTP 302 (versioned-docs redirect, async build) for all three.
- Published install smoke passed: `--version` (`unsafe-review 0.3.4`), `doctor`
  (advisory posture + trust boundary printed), `repo --list-files`.
- Tag `v0.3.4` -> source `bb59c270`; GitHub Release
  <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.3.4>.
- Source PR #530 recorded the publication receipt
  (`docs/releases/0.3.4-publication-receipt.md`).

## What this mirror brings into swarm

- the synchronized 0.3.4 version bumps and `Cargo.lock`,
- `docs/releases/0.3.4-coverage-instrument-usability.md`,
- `docs/releases/0.3.4-publication-receipt.md`,
- `docs/handoffs/2026-06-07-release-0.3.4-preparation.md`,
- the updated `docs/releases/CRATES_IO_PATCH_RELEASE.md` current case and
  `docs/handoffs/README.md` index row,
- the moved `policy/source-sync.toml` checkpoint
  (`acknowledged_source_main = 2acc8cbbf990bbb0840fc5fe9d5e7bd045c41be7`).

## Why swarm was briefly behind source

Release preparation and publication receipts are source-side work by policy
(`docs/contributing/SWARM_TO_MAIN.md` routing). During publication, source
gained the catch-up merge and release-prep commit (#529) and the receipt commit
(#530) that swarm did not have. This mirror restores parity and the checkpoint
records that the divergence is acknowledged and absorbed.

## What shipped (the coverage-instrument usability batch)

unsafe-review 0.3.4 is the baseline-aware unsafe evidence coverage instrument:
coverage-slot model (SPEC-0029), baseline movement + diff-scoped no-new-debt
(SPEC-0030) with `baseline init`/`add` + live `worsened`, baseline-aware badges
(SPEC-0031), comment-plan coverage-gap hardening (SPEC-0032), file:range context
packet (SPEC-0033), `unsafe-review-gate.json` manifest (SPEC-0034), repo-scan
diagnosability (SPEC-0035), manual candidate `new`/`lint`/`import`, and a narrow
stable-byte coverage detector v1.

## Trust boundary

`unsafe-review` 0.3.4 remains advisory static unsafe coverage evidence. No proof,
UB-free, Miri-clean, site-execution, or calibrated precision/recall claims. No
witness execution, comments, source edits, or default blocking. Manual candidates
remain manual/advisory; confirmation cues are emitted, never executed.
