# 2026-06-05 - release 0.3.3 preparation

Scope: prepare the synchronized `0.3.3` Bun manual-candidate cockpit usability
patch from source `main` after the history-preserving swarm import in source
PR #526.

This is release preparation only. It does not publish crates, run Bun, add
analyzer behavior, execute witnesses, post comments, edit downstream source, or
start policy gating.

## What this release carries

Source `main` merge commit `345c5b1a` imports swarm main `4b444594`
(history-preserved, tree byte-identical to validated swarm main), covering the
post-0.3.2 usability lane recorded under `CHANGELOG.md` `0.3.3`:

- per-card confirmation cues framing findings as hypotheses pending external
  confirmation (`hypothesis_to_confirm`, `build_this_first`, `minimal_repro`,
  `confirmation_step`),
- stable-byte manual-candidate metadata and JS/TypeScript oracle maps with
  node-parity coverage,
- ReviewCard proof-path projection across output surfaces,
- `tokmd-packets.json` manual packet inputs with `tokmd_run = false`,
- manual repair handoff: sidecar buckets, mix summary, repair-queue cockpit
  panel, bucket reasons,
- diff-scope counts and `repo --list-files` JSON/Markdown formats,
- the Bun manual-candidate smoke-report drift guard,
- unified ReviewCard trust boundary,
- roadmap record of stale-span-after-reentry detection (swarm #1393) and
  optional confirmation-cue execution (swarm #1394) as next analyzer work,
- clippy 1.95 lint-debt clearance.

## Version bump

Synchronized public crates:

```text
unsafe-review-core 0.3.2 -> 0.3.3
unsafe-review-cli  0.3.2 -> 0.3.3
unsafe-review      0.3.2 -> 0.3.3
```

`Cargo.lock` updated for the three workspace members only.

## Validation

From this branch:

- `cargo fmt --check`
- `cargo check --workspace --all-targets --locked`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked`
- `cargo run --locked -p xtask -- check-pr`
- `cargo package -p unsafe-review-core --list`
- `cargo package -p unsafe-review-cli --list`
- `cargo package -p unsafe-review --list`
- `cargo publish -p unsafe-review-core --dry-run`
- `cargo publish -p unsafe-review-cli --dry-run` (expected to fail registry
  resolution until `unsafe-review-core 0.3.3` is published; known publish-order
  condition, not a product failure)
- `cargo publish -p unsafe-review --dry-run` (same publish-order condition)

## Manual publish order

```text
cargo publish -p unsafe-review-core
cargo publish -p unsafe-review-cli
cargo publish -p unsafe-review
```

Then install-smoke the published facade per
`docs/releases/CRATES_IO_PATCH_RELEASE.md`, tag `v0.3.3` from source `main`,
create the GitHub Release, record the publication receipt in source, and mirror
the receipt to `unsafe-review-swarm`.

## What not to do next

- Do not publish before the release-prep PR merges and dry-run/package
  inspection passes from source `main`.
- Do not rewrite the `0.3.2` tag or any earlier release tag.
- Do not squash or rebase the history catch-up merge.
- Do not claim proof, UB-free status, Miri-clean status, site execution,
  calibrated precision/recall, policy readiness, witness execution, automatic
  comments, source edits, or default blocking.

## Trust boundary

`unsafe-review` remains advisory static review evidence with manually imported
advisory candidates. Confirmation cues are emitted, never executed.
