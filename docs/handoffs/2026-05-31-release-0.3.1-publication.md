# 2026-05-31 - release 0.3.1 publication receipt

Scope: record manual publication and post-publish verification for
`unsafe-review` `0.3.1`.

## Source

- Source repository: <https://github.com/EffortlessMetrics/unsafe-review>
- Source commit: `82378e3b745f4c842ec5e7e18b88e95fe8ad03bc`
- Source history catch-up PR:
  <https://github.com/EffortlessMetrics/unsafe-review/pull/505>
- Preparation PR: <https://github.com/EffortlessMetrics/unsafe-review/pull/506>
- Tag: `v0.3.1`
- GitHub Release:
  <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.3.1>

## Published crates

| Crate | Version | crates.io | docs.rs |
|---|---|---|---|
| `unsafe-review-core` | `0.3.1` | <https://crates.io/crates/unsafe-review-core/0.3.1> | <https://docs.rs/unsafe-review-core/0.3.1/unsafe_review_core/> |
| `unsafe-review-cli` | `0.3.1` | <https://crates.io/crates/unsafe-review-cli/0.3.1> | <https://docs.rs/unsafe-review-cli/0.3.1/unsafe_review_cli/> |
| `unsafe-review` | `0.3.1` | <https://crates.io/crates/unsafe-review/0.3.1> | <https://docs.rs/unsafe-review/0.3.1/unsafe_review/> |

## Release purpose

`0.3.1` is a crates.io package-surface hotfix after source history repair. It
publishes from source history that contains the reviewed swarm commit graph,
including `3a71a65c packaging: restore stable crates.io logo URL (#637)`.

The facade package README uses:

```text
https://raw.githubusercontent.com/EffortlessMetrics/unsafe-review/main/unsafe-review-logo.svg
```

This release is not a new analyzer release, Bun lane, calibration release,
policy-readiness release, witness execution release, automatic comment release,
or source-editing release.

## Publish order

Published manually in dependency order:

```bash
rtk cargo publish -p unsafe-review-core
rtk cargo search unsafe-review-core --limit 10

rtk cargo publish -p unsafe-review-cli --dry-run
rtk cargo publish -p unsafe-review-cli
rtk cargo search unsafe-review-cli --limit 10

rtk cargo publish -p unsafe-review --dry-run
rtk cargo publish -p unsafe-review
rtk cargo search unsafe-review --limit 10
```

Observed registry state:

- `cargo search unsafe-review --limit 10` reported all three crates at
  `0.3.1`.
- `cargo info unsafe-review-core@0.3.1 --registry crates-io` reported
  `version: 0.3.1`.
- `cargo info unsafe-review-cli@0.3.1 --registry crates-io` reported
  `version: 0.3.1`.
- `cargo info unsafe-review@0.3.1 --registry crates-io` reported
  `version: 0.3.1`.

## Package surface

Before publishing the facade crate:

- `cargo publish -p unsafe-review --dry-run` passed,
- `target/package/unsafe-review-0.3.1/README.md` contained the stable raw
  GitHub logo URL,
- the facade package included `README.md` and `unsafe-review-logo.svg`.

The crates.io HTML/API endpoint returned `403` for the local `curl` client due
to crates.io data-access policy, so package page rendering was verified through
`cargo info`, `cargo search`, docs.rs, and local packaged README inspection.

## Docs and release

- `curl -I https://docs.rs/unsafe-review-core/0.3.1/unsafe_review_core/`
  returned `HTTP/1.1 200`.
- `curl -I https://docs.rs/unsafe-review-cli/0.3.1/unsafe_review_cli/`
  returned `HTTP/1.1 200`.
- `curl -I https://docs.rs/unsafe-review/0.3.1/unsafe_review/` returned
  `HTTP/1.1 200`.
- `gh release view v0.3.1 --repo EffortlessMetrics/unsafe-review` reported a
  non-draft, non-prerelease GitHub Release published at
  `2026-05-31T11:51:02Z`.
- The latest release API reported `tag_name = v0.3.1`.

## Published install smoke

Installed from crates.io into an isolated root:

```bash
rtk cargo install unsafe-review --version 0.3.1 --locked --root target/install-published-0.3.1
```

Observed:

```text
target/install-published-0.3.1/bin/unsafe-review.exe v0.3.1
```

Version smoke:

```bash
rtk target/install-published-0.3.1/bin/unsafe-review.exe --version
```

Observed:

```text
unsafe-review 0.3.1
```

Doctor smoke:

```bash
rtk target/install-published-0.3.1/bin/unsafe-review.exe doctor
```

Observed: git, repo, `origin/main`, cargo metadata, artifact directory, witness
tool signals, advisory policy, and trust-boundary output were present. Missing
witness tools were informational.

## First-pr artifact smoke

Command:

```bash
rtk target/install-published-0.3.1/bin/unsafe-review.exe first-pr --root fixtures/raw_pointer_alignment --diff fixtures/raw_pointer_alignment/change.diff --out-dir target/unsafe-review-published-0.3.1-smoke
rtk cargo run --locked -p xtask -- check-first-pr-artifacts target/unsafe-review-published-0.3.1-smoke
```

Observed:

- `first-pr` wrote one advisory PR bundle with one ReviewCard.
- `check-first-pr-artifacts` returned ok for
  `target/unsafe-review-published-0.3.1-smoke`.
- The published bundle included:

```text
review-kit.json
cards.json
pr-summary.md
github-summary.md
cards.sarif
comment-plan.json
witness-plan.md
receipt-audit.md
lsp.json
repair-queue.json
```

Top smoke card:

```text
UR-raw-pointer-alignment-fixture-src-lib-rs-read-header-operation-raw_pointer_read-cast-header-8a1362456e39-pointer_validity-c1
```

## Explain, context, and support smoke

Commands:

```bash
rtk target/install-published-0.3.1/bin/unsafe-review.exe explain --root fixtures/raw_pointer_alignment UR-raw-pointer-alignment-fixture-src-lib-rs-read-header-operation-raw_pointer_read-cast-header-8a1362456e39-pointer_validity-c1
rtk target/install-published-0.3.1/bin/unsafe-review.exe context --root fixtures/raw_pointer_alignment UR-raw-pointer-alignment-fixture-src-lib-rs-read-header-operation-raw_pointer_read-cast-header-8a1362456e39-pointer_validity-c1 --json
rtk target/install-published-0.3.1/bin/unsafe-review.exe support
```

Observed:

- `explain` rendered the top card with required safety conditions, evidence
  found, evidence missing, witness routes, non-resolution examples, and trust
  boundary.
- `context --json` rendered a bounded card packet with repair queue buckets,
  allowed repairs, do-not-do rules, stop conditions, and trust boundary.
- `support` reported the advisory/deferred posture.

## Trust boundary

`unsafe-review` `0.3.1` reports static unsafe-review evidence. It does not
prove memory safety, UB-free status, Miri-clean status, site execution, witness
success, calibrated precision/recall, or policy readiness.

It does not execute witnesses, post comments, edit source, or enforce blocking
policy by default.

## Known limits

- All current public surfaces remain experimental advisory surfaces.
- The release is not a calibrated analyzer or policy gate.
- Bun investigation is a separate consumer lane; any Bun candidate still
  requires manual proof of a safe API path, local prevention, and a focused
  regression or witness appropriate to that finding.

## Next sync

Mirror this publication receipt back to `unsafe-review-swarm` before routine
swarm development resumes.
