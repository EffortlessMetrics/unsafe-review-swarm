# 2026-05-21 - release 0.2.0 publication receipt

Date: 2026-05-21

Scope: record the manual `0.2.0` crates.io publication for
`unsafe-review-core`, `unsafe-review-cli`, and `unsafe-review`.

Release PR: `#488`

Publication receipt PR: `#489`

Source commit: `5e6535b79e121c14472c2285e646c9db8484f181`

Tag:

```text
v0.2.0 -> 5e6535b79e121c14472c2285e646c9db8484f181
annotated tag object: 6f27d82c0f7a35a7248c4427e54fa22ec5473a8a
```

## Published crates

Published manually in dependency order:

| Crate | Version | Registry URL | docs.rs |
|---|---:|---|---|
| `unsafe-review-core` | `0.2.0` | <https://crates.io/crates/unsafe-review-core/0.2.0> | <https://docs.rs/unsafe-review-core/0.2.0/unsafe_review_core/> |
| `unsafe-review-cli` | `0.2.0` | <https://crates.io/crates/unsafe-review-cli/0.2.0> | <https://docs.rs/unsafe-review-cli/0.2.0/unsafe_review_cli/> |
| `unsafe-review` | `0.2.0` | <https://crates.io/crates/unsafe-review/0.2.0> | <https://docs.rs/unsafe-review/0.2.0/unsafe_review/> |

## Pre-publish verification

Run from clean source main commit `5e6535b79e121c14472c2285e646c9db8484f181`:

```bash
rtk cargo fmt --check
rtk cargo check --workspace --all-targets --locked
rtk cargo clippy --workspace --all-targets --locked -- -D warnings
rtk cargo test --workspace --locked
rtk cargo run --locked -p xtask -- check-pr
rtk cargo run --locked -p xtask -- check-calibration
rtk cargo run --locked -p xtask -- check-dogfood
rtk cargo package -p unsafe-review-core --list
rtk cargo package -p unsafe-review-cli --list
rtk cargo package -p unsafe-review --list
rtk cargo publish -p unsafe-review-core --dry-run
rtk git diff --check
```

Observed result:

- formatting, workspace check, clippy, workspace tests, `check-pr`,
  `check-calibration`, `check-dogfood`, package lists, core publish dry-run,
  and diff whitespace checks passed
- workspace tests: `456 passed`
- `check-pr`: ok with `217 fixtures`, `215 calibration cases`,
  `30 dogfood targets`, and `7 repositories`
- `unsafe-review-core` dry-run packaged `45 files`, `753.7KiB` unpacked and
  `115.2KiB` compressed, verified, and reached the dry-run upload boundary
- before publication, `rtk cargo search unsafe-review --limit 10` still
  reported the public crates at `0.1.0`

## Publish commands

The crates were published manually in dependency order:

```bash
rtk cargo publish -p unsafe-review-core
rtk cargo search unsafe-review-core --limit 10
rtk cargo publish -p unsafe-review-cli --dry-run
rtk cargo publish -p unsafe-review-cli
rtk cargo search unsafe-review-cli --limit 10
rtk cargo publish -p unsafe-review --dry-run
rtk cargo publish -p unsafe-review
rtk cargo search unsafe-review --limit 10
```

Observed result:

- `unsafe-review-core 0.2.0` uploaded and Cargo waited for registry
  availability; registry search then reported `unsafe-review-core = "0.2.0"`
- `unsafe-review-cli 0.2.0` dry-run downloaded
  `unsafe-review-core 0.2.0`, then the crate uploaded and Cargo waited for
  registry availability; registry search then reported
  `unsafe-review-cli = "0.2.0"`
- `unsafe-review 0.2.0` dry-run downloaded `unsafe-review-cli 0.2.0`, then
  the facade crate uploaded and Cargo waited for registry availability
- final registry search reported:

```text
unsafe-review = "0.2.0"
unsafe-review-cli = "0.2.0"
unsafe-review-core = "0.2.0"
```

## Post-publish smoke

The published facade was installed from crates.io into an isolated install root:

```bash
rtk cargo install unsafe-review --locked --root target/install-published-0.2.0
```

Observed result:

```text
installed unsafe-review.exe v0.2.0
```

Product smoke commands:

```bash
rtk powershell -NoProfile -Command "& 'target\install-published-0.2.0\bin\unsafe-review.exe' --version"
rtk powershell -NoProfile -Command "& 'target\install-published-0.2.0\bin\unsafe-review.exe' doctor"
rtk powershell -NoProfile -Command "& 'target\install-published-0.2.0\bin\unsafe-review.exe' first-pr --root fixtures/raw_pointer_alignment --diff fixtures/raw_pointer_alignment/change.diff --out-dir target/unsafe-review-published-0.2.0-smoke"
rtk cargo run --locked -p xtask -- check-first-pr-artifacts target/unsafe-review-published-0.2.0-smoke
rtk powershell -NoProfile -Command "& 'target\install-published-0.2.0\bin\unsafe-review.exe' explain --root fixtures/raw_pointer_alignment UR-raw-pointer-alignment-fixture-src-lib-rs-read-header-operation-raw_pointer_read-cast-header-8a1362456e39-pointer_validity-c1"
rtk powershell -NoProfile -Command "& 'target\install-published-0.2.0\bin\unsafe-review.exe' support"
```

Observed result:

- `--version` printed `unsafe-review 0.2.0`
- `doctor` reported git, repo, `origin/main`, cargo metadata, artifact
  directory readiness, advisory policy mode, and no witness execution by default
- `support` reported ReviewCards, first-pr bundles, receipts, outcome
  comparison, policy reports, comment posting, source edits, witness execution,
  blocking policy, and live LSP as advisory/deferred as appropriate
- `first-pr` wrote the standard advisory bundle to
  `target/unsafe-review-published-0.2.0-smoke`
- `check-first-pr-artifacts` passed for that bundle
- `explain` rendered the top raw-pointer-alignment ReviewCard with required
  safety conditions, found/missing evidence, resolution guidance, witness
  routes, and trust-boundary wording

## docs.rs checks

Checked the exact version URLs:

```bash
rtk curl -I https://docs.rs/unsafe-review-core/0.2.0/unsafe_review_core/
rtk curl -I https://docs.rs/unsafe-review-cli/0.2.0/unsafe_review_cli/
rtk curl -I https://docs.rs/unsafe-review/0.2.0/unsafe_review/
rtk curl -I -L https://docs.rs/unsafe-review-cli/0.2.0/unsafe_review_cli/
rtk curl -I -L https://docs.rs/unsafe-review/0.2.0/unsafe_review/
```

Observed result:

- `unsafe-review-core` returned HTTP `200`
- `unsafe-review-cli` and `unsafe-review` exact module URLs returned HTTP `302`
  to the corresponding crate pages, and `-L` returned HTTP `200`

## Trust boundary

`0.2.0` is an experimental advisory static unsafe-review evidence release.

It is not:

```text
memory-safety proof
UB-free claim
Miri-clean claim
site-execution proof
target-feature availability proof
default policy gate
automatic PR comment publisher
automatic unsafe-code repair tool
```

## Known limits

- Support tiers remain experimental/advisory unless the detailed support ledger
  says otherwise.
- Real-crate dogfood is useful but not calibrated precision/recall.
- No witness tools are executed by default.
- No default no-new-debt or blocking CI policy is enabled.
- Saved `lsp.json` is the `0.2.0` editor-adjacent surface; live LSP remains
  deferred.
- Agent packets are copy-only and do not execute repairs.

## Next lane

After this publication receipt merges:

```text
1. Run source/swarm divergence from swarm.
2. Mirror release-only metadata back to swarm if source is ahead.
3. Continue routine development in swarm, not source.
4. Start the quiet PR review loop: comment-plan ranking reasons, verifier
   coverage, and examples without automatic posting.
```

Do not start default blocking policy, automatic comments, witness execution,
source edits, VS Code publication, or live LSP source promotion from this
publication alone.
