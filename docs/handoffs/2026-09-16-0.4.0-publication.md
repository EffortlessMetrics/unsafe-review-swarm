# 0.4.0 publication mirror — issue #1879

Source PR `EffortlessMetrics/unsafe-review#568` merged 2026-09-16 as merge
commit `8f67aaa5114ccad7b10c568353d95ae0c46f750e` (two parents:
`c25d6527` source base + `a2b9cc48` candidate; history-preserving, no
squash). This record mirrors the publication receipt back to swarm and
acknowledges the new source main for `source-divergence`.

## Publication record

- Source merge: `8f67aaa5` (merge commit, two parents).
- Tag: `v0.4.0` at the merge commit.
- GitHub Release: `unsafe-review 0.4.0`
  (`https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.4.0`).
- Crates published in order via the source `crates-publish` workflow
  (dry run `35141573886` green, publish run `35141824830` success):
  `unsafe-review-core 0.4.0`, `unsafe-review-cli 0.4.0`,
  `unsafe-review 0.4.0` (all three resolve on the public index).
- Public smoke from crates.io into a clean prefix: `--version` reports
  `0.4.0`, doctor green, representative first-pr bundle written with
  `check-first-pr-artifacts` ok, unknown flag exits 2. Installed binary
  SHA-256 `74da0ea82fb8644ee131fe470251ce248f1075d1e1800bc925ce14fead0d7ed7`
  (release-build crate install; differs from the candidate-tree install by
  build path, as expected).

## Qualification linkage

Candidate `a2b9cc48` was qualified under swarm #1925
(`qualified_for_owner_decision` at `9b532bbe`): ISSUE-2100 deferred (#2213),
installed receipt rebound with byte-identical binary (#2214), Windows 17/17
green on hosted run `35132286041` (#2216/#2217/#2218), source exact-head gate
enforced with merge-only (ruleset `16511440`).

## Acknowledgment

`policy/source-sync.toml` now acknowledges source main `8f67aaa5` via this
document. No safety, UB-free, Miri-clean, site-execution, or calibrated
accuracy claim follows from publication; the advisory trust boundary from the
closeout carries over unchanged.
