# 2026-06-01 source 0.3.2 preparation sync

Status: source-to-swarm release-preparation metadata sync

This handoff records source PR #509 and advances the swarm source-sync
checkpoint after source prepared the synchronized `0.3.2` repo dogfood
usability patch. This is not a crates.io publication receipt.

Source PR and commit:

| Source PR / commit | Source commit | Surface | Swarm status |
|---|---|---|---|
| `EffortlessMetrics/unsafe-review#509` | `6d66091` | Prepared the synchronized `0.3.2` repo dogfood usability patch after source PR #508 imported the reviewed swarm batch | Mirrored by this sync as release-preparation metadata |

Mirrored source preparation state:

- `unsafe-review-core`, `unsafe-review-cli`, and `unsafe-review` are prepared at
  `0.3.2`.
- Workspace dependency pins and `Cargo.lock` are prepared for `0.3.2`.
- `docs/releases/0.3.2-repo-dogfood-usability.md` is present.
- `docs/handoffs/2026-05-31-release-0.3.2-preparation.md` is present.
- `docs/releases/CRATES_IO_PATCH_RELEASE.md` names the current 0.3.2 patch flow.

Swarm sync:

- `unsafe-review-swarm` merges source main `6d660916a02a47ea82c46c2dabf1fa44c2e44706`.
- `policy/source-sync.toml` acknowledges source main at
  `6d660916a02a47ea82c46c2dabf1fa44c2e44706`.
- Existing swarm-only work remains workbench state until deliberately promoted.

Boundaries:

- no crates.io publication
- no GitHub Release or tag creation
- no Bun finding or vulnerability claim
- no witness execution
- no automatic comments
- no source edits by `unsafe-review`
- no default blocking policy
- no safety, UB-free, Miri-clean, site-execution, precision, recall, or
  policy-readiness claim

Validation:

- `cargo fmt --check`
- `cargo run --locked -p xtask -- check-calibration`
- `cargo run --locked -p xtask -- check-dogfood`
- `cargo run --locked -p xtask -- check-doc-artifacts`
- `cargo run --locked -p xtask -- check-goals`
- `cargo run --locked -p xtask -- check-pr`
- `git diff --check`

Expected after merge:

- `cargo run --locked -p xtask -- source-divergence` reports
  `new_source_commits=0`.
