# 2026-06-18 - source 0.3.8 publication sync

Scope: mirror the verified source 0.3.8 publication receipt into the swarm
workbench and move the acknowledged source-sync checkpoint.

This is a source-sync handoff. It does not publish crates, push a tag, cut a
GitHub Release, execute witnesses, post comments, edit downstream source, or
start policy gating.

## What source did

- Source PR #542 prepared and promoted the 0.3.8 corpus control-plane and
  declaration/surfacing correctness release to source `main` at `9751f956`.
- The `crates-publish` workflow dry run succeeded on source `main` at
  `9751f956` (run id 27795826268).
- The `crates-publish` workflow real publish succeeded on source `main` at
  `9751f956` (run id 27795856638), publishing:
  - `unsafe-review-core 0.3.8`
  - `unsafe-review-cli 0.3.8`
  - `unsafe-review 0.3.8`
- Tag `v0.3.8` points at `9751f956`; the GitHub Release is
  <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.3.8>.
- Source PR #543 recorded the 0.3.8 publication receipt and advanced source
  `main` to `209c76fe`.

## What this mirror brings into swarm

- `docs/releases/0.3.8-publication-receipt.md`.
- `docs/handoffs/README.md` index rows for this source publication sync and the
  0.3.8 publication receipt.
- The moved `policy/source-sync.toml` checkpoint
  (`acknowledged_source_main = 209c76fef1da653172a21c3348d6e3a3fb1eedbd`).

## Evidence mirrored

- crates.io reports `unsafe-review`, `unsafe-review-cli`, and
  `unsafe-review-core` at `0.3.8`.
- docs.rs crate pages for all three 0.3.8 crates returned HTTP 200.
- Installed `unsafe-review 0.3.8` from crates.io and verified `--version`,
  `doctor`, expected `first-pr --out` exit 2, `first-pr --out-dir` exit 0 plus
  `check-first-pr-artifacts`, `pr --help`, and `support`.

## Trust boundary

`unsafe-review` 0.3.8 remains advisory static unsafe coverage evidence. This
handoff makes no proof, UB-free, Miri-clean, site-execution, calibrated
precision/recall, or policy-readiness claim. It mirrors release metadata and a
bookkeeping checkpoint only.
