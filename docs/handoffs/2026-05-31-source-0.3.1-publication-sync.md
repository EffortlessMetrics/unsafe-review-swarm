# 2026-05-31 source 0.3.1 publication sync

Status: source-to-swarm release metadata sync

This handoff records the source-only `0.3.1` crates.io README/image hotfix,
source history repair, and publication receipt work and mirrors the release
metadata needed by the swarm workbench. This is not a new analyzer lane.

Source PRs and commits:

| Source PR / commit | Source commit | Surface | Swarm status |
|---|---|---|---|
| `EffortlessMetrics/unsafe-review#505` | `32aa4cb` | Preserved unsafe-review-swarm history in source with merge-commit mode and carried process docs | Acknowledged by this sync as source history repair |
| `EffortlessMetrics/unsafe-review#506` | `82378e3` | Prepared `0.3.1` crates.io README/image hotfix version metadata and release-prep handoff | Mirrored by this sync as version metadata |
| `EffortlessMetrics/unsafe-review#507` | `9c9db8b` | Recorded `0.3.1` publication receipt | Recorded by this sync handoff and source-sync checkpoint |

Published release:

| Crate | Version | crates.io | docs.rs |
|---|---:|---|---|
| `unsafe-review-core` | `0.3.1` | <https://crates.io/crates/unsafe-review-core/0.3.1> | <https://docs.rs/unsafe-review-core/0.3.1/unsafe_review_core/> |
| `unsafe-review-cli` | `0.3.1` | <https://crates.io/crates/unsafe-review-cli/0.3.1> | <https://docs.rs/unsafe-review-cli/0.3.1/unsafe_review_cli/> |
| `unsafe-review` | `0.3.1` | <https://crates.io/crates/unsafe-review/0.3.1> | <https://docs.rs/unsafe-review/0.3.1/unsafe_review/> |

Tag and release:

- tag: `v0.3.1`
- tag target: `82378e3b745f4c842ec5e7e18b88e95fe8ad03bc`
- GitHub Release: <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.3.1>
- GitHub Release was non-draft and non-prerelease.

Published smoke recorded in source:

- `cargo search unsafe-review --limit 10` reported all three crates at `0.3.1`.
- `cargo info unsafe-review-core@0.3.1 --registry crates-io` reported
  `version: 0.3.1`.
- `cargo info unsafe-review-cli@0.3.1 --registry crates-io` reported
  `version: 0.3.1`.
- `cargo info unsafe-review@0.3.1 --registry crates-io` reported
  `version: 0.3.1`.
- docs.rs pages for all three `0.3.1` crates returned `200`.
- `cargo install unsafe-review --version 0.3.1 --locked --root target/install-published-0.3.1`
  installed `unsafe-review.exe`.
- installed `unsafe-review --version` reported `unsafe-review 0.3.1`.
- installed `unsafe-review doctor` passed first-run readiness checks with
  missing witness tools as informational.
- installed `unsafe-review first-pr` wrote the standard advisory review kit for
  `fixtures/raw_pointer_alignment`.
- `check-first-pr-artifacts` passed on the published first-pr review kit.
- installed `unsafe-review explain`, `unsafe-review context --json`, and
  `unsafe-review support` preserved reviewer-first, bounded-agent, and advisory
  posture wording.

Published first-pr review kit:

```text
review-kit.json
cards.json
pr-summary.md
github-summary.md
cards.sarif
comment-plan.json
witness-plan.md
receipt-audit.md
lsp.json
repair-queue.json
```

Package-surface fix:

- `crates/unsafe-review/README.md` uses
  `https://raw.githubusercontent.com/EffortlessMetrics/unsafe-review/main/unsafe-review-logo.svg`.
- The packaged facade README was inspected from
  `target/package/unsafe-review-0.3.1/README.md` before publication.

Swarm sync:

- `crates/unsafe-review-core/Cargo.toml` version is now `0.3.1`.
- `crates/unsafe-review-cli/Cargo.toml` version and local core dependency are
  now `0.3.1`.
- `crates/unsafe-review/Cargo.toml` version and local CLI/core dependencies are
  now `0.3.1`.
- `Cargo.lock` records the three workspace crates at `0.3.1`.
- Source history/process docs from source are mirrored:
  - `docs/contributing/SOURCE_HISTORY_CATCHUP.md`
  - `docs/contributing/SWARM_TO_MAIN.md`
  - `docs/releases/CRATES_IO_PATCH_RELEASE.md`
  - `docs/releases/0.3.1-crates-io-readme-hotfix.md`
  - `docs/handoffs/2026-05-31-source-history-catchup-process.md`
  - `docs/handoffs/2026-05-31-swarm-history-catchup.md`
  - `docs/handoffs/2026-05-31-release-0.3.1-preparation.md`
  - `docs/handoffs/2026-05-31-release-0.3.1-publication.md`
- `policy/source-sync.toml` acknowledges source main at
  `9c9db8be7cfc0fbd7a75198c8febb0cfcaddcacf`.

Boundaries:

- no analyzer behavior change
- no source badge count import into swarm badge endpoints
- no Bun finding or vulnerability claim
- no witness execution
- no automatic comments
- no source edits by `unsafe-review`
- no default blocking policy
- no safety, UB-free, Miri-clean, site-execution, precision, recall, or
  policy-readiness claim

Validation:

- `cargo run --locked -p xtask -- check-docs`
- `cargo run --locked -p xtask -- check-pr`
- `cargo run --locked -p xtask -- source-divergence`
- `git diff --check`

Expected after merge:

- `cargo run --locked -p xtask -- source-divergence` reports
  `new_source_commits=0`.
