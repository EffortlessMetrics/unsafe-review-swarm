# 0.5.0 publication receipt

Source `main` `e718c063` (promotion #570 merged as `71cf254d`, release-prep
#571 merged as `e718c063`). Crates `unsafe-review-core`, `unsafe-review-cli`,
and `unsafe-review` published at `0.5.0` via the source `crates-publish`
workflow (dry run `35175350041` green, publish run `35177910918` success; all
three resolve at `max_version 0.5.0` on the public index).

Tag `v0.5.0` and the GitHub Release are not created by this receipt; each
requires a separate explicit go.

## What 0.5.0 carries

- `--short` risk-ranked one-line-per-card human output (`check`, `repo`,
  `pilot`); rejected for the policy report (swarm #2245, #2249).
- `SAFETY:` in doc comments recognized as contract evidence (#2235).
- Next-action routes inner unsafe-fn sites to caller-contract review (#2237).
- Deref-of-`get_unchecked` folded into the single `get_unchecked` card
  (#2239).
- Unreached witness commands gated behind a test-first cue (#2241).
- Slot-level missing counts alongside class counters in headers (#2243).
- 0/1/2 exit-code contract in review-command helps; expanded facade rustdoc
  (#2250).
- `sha2` `0.10.9` → `0.11.0` (#2207).

## Proof carried over

- Swarm head proof pre-promotion: fmt, clippy `-D warnings`, doc clean;
  2109 tests passed, 0 failed; `check-pr` ok.
- Promotion merge tree: `check-pr` ok; six conflicts resolved explicitly;
  LSP golden re-blessed with the merged analyzer.
- Release-prep: fmt/check/clippy/doc clean; `cargo package --list` for all
  three crates; core dry-run upload ok. The first hosted gate run failed only
  on version-stamped surface goldens (`tool_version 0.4.0` vs `0.5.0`);
  re-blessed (version-stamp-only diff) and green on re-run.
- Release-readiness corpus: `dogfood-exec --strict` 15 ok / 0 failed.
- `repo_sigterm_keeps_completed_file_partial_report` flakes intermittently
  under parallel load (6/6 pass in isolation; pre-existing timing class,
  unrelated to this release).

## Public smoke (crates.io install into a clean prefix)

- `--version` reports `0.5.0`; `doctor` green.
- Representative `first-pr` bundle written;
  `check-first-pr-artifacts` ok.
- Unknown flag exits 2; `repo --list-files` works; review-command helps
  print the exit-code footer.
- Installed binary SHA-256
  `3a398710dd97fd941576968ee6ad38ae0c24dfc394843c634e51bab4bbff7b4c`
  (release-build crate install).
- docs.rs 0.5.0 pages resolving at publish time; build status queued.

## Known limits and trust boundary

A crates.io release does not prove memory safety, UB-free status, Miri-clean
status, site execution, witness success, calibrated precision/recall, or
policy readiness. It does not execute witnesses, post comments, edit source,
or enforce blocking policy by default. Precision measurement (#2223) and
guard-evidence separation (#2226) remain open workbench levers.
