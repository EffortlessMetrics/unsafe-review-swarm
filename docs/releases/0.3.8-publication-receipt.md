# 0.3.8 publication receipt

Records the verified publication of `unsafe-review` 0.3.8 (corpus control
plane and declaration/surfacing correctness). This is a publication receipt. It
makes no memory-safety proof, UB-free, Miri-clean, site-execution, calibrated
precision/recall, or policy-readiness claim.

## Source

- Source PR #542 (`release/0.3.8-corpus-control-plane`) merged to source
  `main` as `9751f9567c21a64e830f2a64217fba04eb49b976`.
- Tag: `v0.3.8` -> `9751f9567c21a64e830f2a64217fba04eb49b976`.
- GitHub Release:
  <https://github.com/EffortlessMetrics/unsafe-review/releases/tag/v0.3.8>.

## crates.io

Published on 2026-06-18 via the `crates-publish` GitHub Actions workflow
(`workflow_dispatch` on source `main`, `dry_run=false`, run id 27795856638);
the workflow published in dependency order and reported all three crates as
published:

- `unsafe-review-core 0.3.8`
- `unsafe-review-cli 0.3.8`
- `unsafe-review 0.3.8`

The preceding dry run (`dry_run=true`, run id 27795826268) succeeded on source
`main` at the same commit. `cargo search unsafe-review --limit 5` reported
`0.3.8` for the facade, CLI, and core crates after publication.

## docs.rs

The 0.3.8 docs.rs crate pages returned HTTP 200 after publication:

- <https://docs.rs/crate/unsafe-review/0.3.8>
- <https://docs.rs/crate/unsafe-review-cli/0.3.8>
- <https://docs.rs/crate/unsafe-review-core/0.3.8>

## Install smoke (from crates.io)

`cargo install unsafe-review --version 0.3.8 --locked` installed
`target/install-published-0.3.8/bin/unsafe-review.exe`.

- `--version` -> `unsafe-review 0.3.8`
- `doctor` -> advisory trust boundary printed; witness tools were not run
- `first-pr --out <file>` -> exited 2 with the expected `--out-dir`
  suggestion and no bundle claim
- `first-pr --out-dir target/unsafe-review-published-0.3.8-smoke` -> exited 0
  and wrote a complete advisory bundle
- `xtask check-first-pr-artifacts target/unsafe-review-published-0.3.8-smoke`
  -> passed
- `pr --help` -> routes to `first-pr`
- `support` -> lists current support posture and support docs

## What shipped

- Additive `unsafe_declaration` operation family for unsafe fn and unsafe trait
  declarations; unsafe impls and genuine unknown unsafe blocks remain out of
  scope.
- Explicit surfacing disposition for comment eligibility, so family label
  changes alone cannot make cards eligible for inline comments.
- Detector-contract, stance-decision, spec-coverage, stance-coverage,
  fixture-surface-parity, real-PR movement, surface-determinism, and dogfood
  corpus rails.
- Runtime workspace-root resolution in `xtask`, removing the shared-target
  stale-checkout hazard.
- Residual unknown report and release notes that keep the next classifier family
  evidence-driven instead of count-driven.

## Pre-publish validation

The source release PR #542 passed hosted Rust result, policy contracts, Droid,
CodeRabbit, and GitGuardian checks on `9751f956`. Local source validation before
publication included:

- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked`
- `cargo run --locked -p xtask -- check-pr`
- `cargo package -p unsafe-review-core --list`
- `cargo package -p unsafe-review-cli --list`
- `cargo package -p unsafe-review --list`
- `cargo publish -p unsafe-review-core --dry-run`
- prepublish `first-pr --out-dir` smoke plus
  `xtask check-first-pr-artifacts`
- `unsafe-review support`

## Known limits

- Publication does not imply runtime witness execution, Miri success, UB-free
  status, site-execution proof, calibrated precision/recall, policy readiness,
  automatic comment posting, source edits, or default blocking.
- The residual unknown report intentionally does not select a new classifier
  family merely to reduce the unknown count; unsafe impl classification remains
  a separate stance/corpus lane.

## Trust boundary

`unsafe-review` 0.3.8 remains advisory static unsafe coverage evidence. It does
not prove memory safety, UB-free status, Miri-clean status, site execution,
calibrated precision/recall, or policy readiness. The corpus and real-PR checks
are diagnostics and release-readiness controls, not accuracy measurements or
merge verdicts. unsafe-review does not run witnesses, post comments, edit
source, or block by default.
