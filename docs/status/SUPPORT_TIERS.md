# Support tiers

All tiers describe static review evidence. None means memory-safety proof.

For a concise front panel, see [`SUPPORT_SUMMARY.md`](SUPPORT_SUMMARY.md). This
file remains the detailed claim-to-proof ledger.

Recent core smoke proof additions include fixture-backed `get_unchecked_mut`
bounds controls for same-receiver `get(index)` probes, early-return probes,
if-let, let-else, and match Some-arm forms, plus stale or shadowed index,
receiver, and receiver-path targets recorded in
`docs/accuracy/labels/get-unchecked-mut-bounds.toml`. These pin that positive
bounds branches must still dominate `get_unchecked`, bare predicate
observations and comment-only early-return text are not guards, get-probe
evidence must target the same slice, and reassigned, compound-reassigned, or
shadowed checked indexes, receivers, or receiver paths do not discharge bounds
evidence.
Recent `NonNull::new_unchecked` nullability additions include fixture-backed
same-pointer `NonNull::new` guards, if-let, let-else, match Some-arm guards,
non-null open branches, null early-return guards, cast-target uncertainty, and
stale or shadowed pointer/receiver controls recorded in
`docs/accuracy/labels/nonnull-new-unchecked-nullability.toml`. These pin the
top-level conjunctive/disjunctive boolean shapes that dominate the operation,
while rejecting comment-only return text, inverse evidence, bare observations,
post-constructor checks, wrong pointers, cast-target/provenance uncertainty,
and stale or shadowed checked-pointer or method-receiver evidence.
Recent guard-evidence additions include
`raw_pointer_alignment_is_aligned_guard`,
`raw_pointer_alignment_observed_not_guard`,
`raw_pointer_alignment_closed_branch_not_guard`, and
`raw_pointer_alignment_reassigned_pointer_not_guard`, which pin same-pointer
`is_aligned` guard evidence without treating observations, closed branches, or
stale checked pointers as alignment discharge.
The same stale-evidence controls are also pinned for same-pointer modulo
alignment checks through `raw_pointer_alignment_modulo_guard`,
`raw_pointer_alignment_modulo_observed_not_guard`,
`raw_pointer_alignment_modulo_closed_branch_not_guard`, and
`raw_pointer_alignment_modulo_reassigned_pointer_not_guard`.
`Vec::from_raw_parts` capacity evidence is pinned for same-call len/cap
assertions and invalid-path early returns, and rejects bare relation
observations, closed positive branches, comment-only early-return text, and
reassigned checked cap arguments.
`Vec::set_len` capacity evidence rejects unrelated local arguments merely named
`cap` unless a const-capacity context is visible, and it rejects capacity checks
made stale by reassigning the checked vector before `set_len`, including
capacity bindings measured before the reassignment.
`Vec::set_len` initialization evidence now rejects unrelated initialization
statements unless the evidence is tied to the receiver being lengthened.
`Vec::set_len` initialized-range proof also includes slice-binding loop
evidence through `vec_set_len_slice_binding_initialized_loop` and
`vec_set_len_remaining_capacity_guard`, while
`vec_set_len_other_slice_binding_not_guard`,
`vec_set_len_partial_slice_binding_not_guard`,
`vec_set_len_stale_slice_binding_not_guard`,
`vec_set_len_single_index_init_not_guard`, and
`vec_set_len_other_remaining_capacity_not_guard` keep wrong-target, partial,
stale, single-index, and other-receiver initialization from discharging the
extended-range obligation.
`Vec::set_len` capacity evidence also recognizes narrow same-vector
`reserve(additional)` and `try_reserve(additional)?` patterns for
`len + additional`, and rejects those calls when `additional` is reassigned
after computing the new length.
`Vec::set_len` shrink evidence rejects start/end bounds tied to a stale
receiver length binding.
`Vec::set_len` last-index shrink evidence rejects stale receivers after the
last index is measured.
`Box::from_raw` and `ptr::drop_in_place` reject `Box::into_raw` origin evidence
when the raw pointer is reassigned before use.
Unchecked-constructor availability evidence is pinned for same-receiver
assertions, enclosing positive branches, and unavailable-path early returns, and
rejects other receivers, bare observations, and closed positive branches.
`get_unchecked_mut` get-probe evidence rejects stale same-slice targets when an
early-return guard is followed by reassignment of the checked receiver before
the unsafe access.
It also rejects early-return guard evidence when the checked receiver is
shadowed before the unsafe access.
It also rejects stale same-slice targets when an `if let Some(_)` guard is
followed by receiver reassignment before `get_unchecked_mut`.
The same stale-receiver control is pinned for `let Some(_) = get(index) else`
early-return guards.
Match `Some(_)` get-probe arms also reject receiver reassignment before the
unsafe access.
`MaybeUninit::assume_init` initialized-memory evidence is pinned for
same-slot open-branch `write` and `MaybeUninit::new` evidence through
`maybeuninit_assume_init_open_branch_write_guard` and
`maybeuninit_assume_init_open_branch_new_guard`, while
`maybeuninit_assume_init_mut_new_guard` pins a mutable same-slot
`MaybeUninit::new` binding before `assume_init` and
`maybeuninit_assume_init_mutslot_new_not_guard` keeps a prefixed `mutslot`
binding from matching `let mut slot` evidence.
`maybeuninit_assume_init_closed_branch_write_not_guard`,
`maybeuninit_assume_init_closed_branch_new_not_guard`,
`maybeuninit_assume_init_partial_field_not_guard`, and
`maybeuninit_assume_init_partial_array_not_guard` keep closed-branch,
partial-field, and partial-array initialization from counting as full
initialized-memory evidence.
`MaybeUninit::assume_init_read` pins same-slot `write` evidence through
`maybeuninit_assume_init_read_write_guard`, pins same-slot `MaybeUninit::new`
evidence through `maybeuninit_assume_init_read_new_guard`, pins open-branch
`write` evidence through
`maybeuninit_assume_init_read_open_branch_write_guard`, pins open-branch
`MaybeUninit::new` evidence through
`maybeuninit_assume_init_read_open_branch_new_guard`, rejects closed conditional write
evidence through `maybeuninit_assume_init_read_closed_branch_write_not_guard`,
rejects closed conditional `MaybeUninit::new` evidence through
`maybeuninit_assume_init_read_closed_branch_new_not_guard`,
keeps `MaybeUninit::new` evidence made stale by reassignment from discharging
initialized-memory evidence through
`maybeuninit_assume_init_read_stale_new_not_guard`,
keeps prefixed-slot `MaybeUninit::new` evidence from discharging
initialized-memory evidence through
`maybeuninit_assume_init_read_mutslot_new_not_guard`,
keeps writes made stale by reassignment from discharging initialized-memory
evidence through `maybeuninit_assume_init_read_stale_write_not_guard`, and
keeps writes made stale by shadowing from discharging initialized-memory
evidence through `maybeuninit_assume_init_read_shadowed_slot_not_guard`, and
`maybeuninit_assume_init_read_other_slot_write_not_guard` keeps a write to a
different slot from discharging `assume_init_read` initialized-memory evidence.
`MaybeUninit::assume_init_ref` pins same-slot `write` evidence through
`maybeuninit_assume_init_ref_write_guard`, pins same-slot `MaybeUninit::new`
evidence through `maybeuninit_assume_init_ref_new_guard`, pins open-branch
`write` evidence through
`maybeuninit_assume_init_ref_open_branch_write_guard`, pins open-branch
`MaybeUninit::new` evidence through
`maybeuninit_assume_init_ref_open_branch_new_guard`, rejects closed conditional write
evidence through `maybeuninit_assume_init_ref_closed_branch_write_not_guard`,
rejects closed conditional `MaybeUninit::new` evidence through
`maybeuninit_assume_init_ref_closed_branch_new_not_guard`,
keeps `MaybeUninit::new` evidence made stale by reassignment from discharging
initialized-memory evidence through
`maybeuninit_assume_init_ref_stale_new_not_guard`,
keeps prefixed-slot `MaybeUninit::new` evidence from discharging
initialized-memory evidence through
`maybeuninit_assume_init_ref_mutslot_new_not_guard`,
and `maybeuninit_assume_init_ref_stale_write_not_guard` keeps a write made
stale by reassignment from discharging initialized-memory evidence and
`maybeuninit_assume_init_ref_shadowed_slot_not_guard` keeps a write made stale
by shadowing from discharging initialized-memory evidence and
`maybeuninit_assume_init_ref_other_slot_write_not_guard` keeps a write to a
different slot from discharging `assume_init_ref` initialized-memory evidence.
`MaybeUninit::assume_init_mut` pins same-slot `write` evidence through
`maybeuninit_assume_init_mut_write_guard`, pins same-slot `MaybeUninit::new`
evidence through `maybeuninit_assume_init_mut_method_new_guard`, pins
open-branch `write` evidence through
`maybeuninit_assume_init_mut_open_branch_write_guard`, pins
open-branch `MaybeUninit::new` evidence through
`maybeuninit_assume_init_mut_open_branch_new_guard`, rejects closed conditional write
evidence through `maybeuninit_assume_init_mut_closed_branch_write_not_guard`,
rejects closed conditional `MaybeUninit::new` evidence through
`maybeuninit_assume_init_mut_closed_branch_new_not_guard`,
keeps `MaybeUninit::new` evidence made stale by reassignment from discharging
initialized-memory evidence through
`maybeuninit_assume_init_mut_stale_new_not_guard`,
keeps prefixed-slot `MaybeUninit::new` evidence from discharging
initialized-memory evidence through
`maybeuninit_assume_init_mut_mutslot_new_not_guard`,
and `maybeuninit_assume_init_mut_stale_write_not_guard` keeps a write made
stale by reassignment from discharging initialized-memory evidence and
`maybeuninit_assume_init_mut_shadowed_slot_not_guard` keeps a write made stale
by shadowing from discharging initialized-memory evidence and
`maybeuninit_assume_init_mut_other_slot_write_not_guard` keeps a write to a
different slot from discharging `assume_init_mut` initialized-memory evidence.
`MaybeUninit::assume_init_drop` pins same-slot `write` evidence through
`maybeuninit_assume_init_drop_write_guard`, pins same-slot `MaybeUninit::new`
evidence through `maybeuninit_assume_init_drop_new_guard`, pins open-branch
`write` evidence through
`maybeuninit_assume_init_drop_open_branch_write_guard`, pins open-branch
`MaybeUninit::new` evidence through
`maybeuninit_assume_init_drop_open_branch_new_guard`, rejects closed conditional write
evidence through `maybeuninit_assume_init_drop_closed_branch_write_not_guard`,
rejects closed conditional `MaybeUninit::new` evidence through
`maybeuninit_assume_init_drop_closed_branch_new_not_guard`,
keeps `MaybeUninit::new` evidence made stale by reassignment from discharging
initialized-memory evidence through
`maybeuninit_assume_init_drop_stale_new_not_guard`,
keeps prefixed-slot `MaybeUninit::new` evidence from discharging
initialized-memory evidence through
`maybeuninit_assume_init_drop_mutslot_new_not_guard`,
and `maybeuninit_assume_init_drop_stale_write_not_guard` keeps a write made
stale by reassignment from discharging initialized-memory evidence and
`maybeuninit_assume_init_drop_shadowed_slot_not_guard` keeps a write made stale
by shadowing from discharging initialized-memory evidence and
`maybeuninit_assume_init_drop_other_slot_write_not_guard` keeps a write to a
different slot from discharging `assume_init_drop` initialized-memory evidence.

Copy operation range evidence is intentionally conservative:
`copy_nonoverlapping_slice_range_guard`,
`copy_nonoverlapping_slice_range_conjunctive_assert_guard`,
`copy_nonoverlapping_slice_range_early_return_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_guard`,
`copy_nonoverlapping_slice_range_open_branch_guard`,
`copy_nonoverlapping_slice_range_conjunctive_open_branch_guard`,
`ptr_copy_slice_range_guard`,
`ptr_copy_slice_range_conjunctive_assert_guard`,
`ptr_copy_slice_range_early_return_guard`,
`ptr_copy_slice_range_disjunctive_early_return_guard`,
`ptr_copy_slice_range_open_branch_guard`, and
`ptr_copy_slice_range_conjunctive_open_branch_guard` pin same-call source and
destination slice length assertions, conjunctive assertions, early returns, or
disjunctive invalid-range early returns, or open branches as valid-range
evidence, while
`copy_nonoverlapping_slice_range_src_only_not_guard`,
`copy_nonoverlapping_slice_range_dst_only_not_guard`,
`ptr_copy_slice_range_src_only_not_guard`,
`ptr_copy_slice_range_dst_only_not_guard`,
`copy_nonoverlapping_slice_range_closed_branch_not_guard`,
`ptr_copy_slice_range_closed_branch_not_guard`,
`copy_nonoverlapping_slice_range_or_branch_not_guard`,
`ptr_copy_slice_range_or_branch_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_block_comment_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_block_comment_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_reassigned_count_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_compound_reassigned_count_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_shadowed_count_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_reassigned_src_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_reassigned_src_path_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_shadowed_src_path_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_shadowed_src_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_reassigned_dst_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_reassigned_dst_path_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_shadowed_dst_path_not_guard`,
`copy_nonoverlapping_slice_range_disjunctive_early_return_shadowed_dst_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_reassigned_count_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_compound_reassigned_count_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_shadowed_count_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_reassigned_src_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_reassigned_src_path_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_shadowed_src_path_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_shadowed_src_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_reassigned_dst_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_reassigned_dst_path_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_shadowed_dst_path_not_guard`,
`ptr_copy_slice_range_disjunctive_early_return_shadowed_dst_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_reassigned_count_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_compound_reassigned_count_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_shadowed_count_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_reassigned_src_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_reassigned_src_path_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_shadowed_src_path_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_shadowed_src_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_reassigned_dst_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_reassigned_dst_path_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_shadowed_dst_path_not_guard`,
`copy_nonoverlapping_slice_range_open_branch_shadowed_dst_not_guard`,
`ptr_copy_slice_range_open_branch_reassigned_count_not_guard`,
`ptr_copy_slice_range_open_branch_compound_reassigned_count_not_guard`,
`ptr_copy_slice_range_open_branch_shadowed_count_not_guard`,
`ptr_copy_slice_range_open_branch_shadowed_src_not_guard`,
`ptr_copy_slice_range_open_branch_shadowed_dst_not_guard`,
`ptr_copy_slice_range_open_branch_reassigned_src_not_guard`,
`ptr_copy_slice_range_open_branch_reassigned_src_path_not_guard`,
`ptr_copy_slice_range_open_branch_reassigned_dst_path_not_guard`,
`ptr_copy_slice_range_open_branch_shadowed_src_path_not_guard`,
`ptr_copy_slice_range_open_branch_shadowed_dst_path_not_guard`,
`ptr_copy_slice_range_open_branch_reassigned_dst_not_guard`,
`copy_nonoverlapping_slice_range_reassigned_count_not_guard`,
`copy_nonoverlapping_slice_range_shadowed_count_not_guard`,
`copy_nonoverlapping_slice_range_reassigned_src_not_guard`,
`copy_nonoverlapping_slice_range_reassigned_src_path_not_guard`,
`copy_nonoverlapping_slice_range_shadowed_src_path_not_guard`,
`copy_nonoverlapping_slice_range_shadowed_src_not_guard`,
`copy_nonoverlapping_slice_range_reassigned_dst_not_guard`,
`copy_nonoverlapping_slice_range_reassigned_dst_path_not_guard`,
`copy_nonoverlapping_slice_range_shadowed_dst_path_not_guard`,
`copy_nonoverlapping_slice_range_shadowed_dst_not_guard`,
`ptr_copy_slice_range_reassigned_count_not_guard`,
`ptr_copy_slice_range_shadowed_count_not_guard`,
`ptr_copy_slice_range_reassigned_src_not_guard`,
`ptr_copy_slice_range_reassigned_src_path_not_guard`,
`ptr_copy_slice_range_shadowed_src_path_not_guard`,
`ptr_copy_slice_range_shadowed_src_not_guard`,
`ptr_copy_slice_range_reassigned_dst_not_guard`,
`ptr_copy_slice_range_reassigned_dst_path_not_guard`,
`ptr_copy_slice_range_shadowed_dst_path_not_guard`,
`ptr_copy_slice_range_shadowed_dst_not_guard`,
`copy_nonoverlapping_other_len_not_guard`, and `ptr_copy_other_len_not_guard`
pin that one-sided, closed-branch, disjunctive positive-branch, comment-only
early-return text, stale count, shadowed count, stale receiver or receiver
path, shadowed receiver or receiver path, stale open-branch, or unrelated slice
length assertions do not discharge the source/destination range obligation.

| Capability | Tier | Surface | Proof | Known limits |
|---|---|---|---|---|
| Diff unsafe site inventory | experimental | CLI JSON/human | syntax-backed fixture goldens for unsafe blocks, split unsafe blocks (`split_unsafe_block`), inline unsafe operation dedupe (`inline_unsafe_raw_pointer_deref_no_duplicate`), attributed unsafe functions (`attributed_unsafe_fn_no_duplicate`), raw pointer operations, import/declaration, adjacent unchanged unsafe declaration, multi-line `impl Trait` owner inference (`impl_trait_bound_owner_inference`), long unsafe-function owner inference (`long_unsafe_fn_owner_inference`), macro owner inference (`macro_rules_owner_inference`), and `cfg(target_feature)` false-positive controls, negative safe-code cases, and human output operation-expression plus operation-family context | source-based, not MIR |
| No-card artifact honesty | experimental | CLI JSON/Markdown/artifacts | fixture goldens `safe_code_no_cards`, `safe_reference_deref_no_cards`, `imports_not_unsafe_operations`, `cfg_target_feature_not_operation`, `static_lifetime_mut_ref_not_static_mut`, and `adjacent_unchanged_unsafe_fn_no_card` pin safe Rust, safe reference derefs, import-only text, cfg predicates, `'static mut` reference type text, and unchanged adjacent unsafe declarations as zero-ReviewCard outputs | fixture-pinned zero-card behavior only; not an all-clear, not proof of no unsafe-adjacent risk, not UB-free status, not Miri-clean status, not site execution evidence, not calibrated precision/recall, and not policy-ready |
| Review-card JSON schema | experimental | CLI JSON | serde-backed DTOs, `schema_version`, top-level trust boundary, mixed-diff summary file counts, site visibility/public API surface fields, concrete operation expression, structured witness routes, next-action summary, and `fixture_card_goldens_match_rendered_json` | fixture corpus is still small; diff-scope counters are telemetry only and do not create non-Rust cards; no schema compatibility promise yet |
| Review-card identity | experimental | card `id` | `card_identity` tests cover exact package/file/owner/site-kind/operation-family/operation-path/snippet-hash/hazard/count components, line drift, and duplicate counted identities | exact identity is consumed by baselines, suppressions, and receipts, but broader drift behavior still needs dogfood |
| Advisory baseline/suppression matching | experimental | policy ledgers / card class | xtask ledger tests plus analyzer tests for exact `baseline_known` and `suppressed` card identity matches | exact identity only; no broad suppressions and no blocking policy |
| Explicit no-new-debt mode | experimental | `--policy no-new-debt` | CLI parser tests and e2e cover nonzero exit for unbaselined actionable gaps and success for exact baseline matches | opt-in only; not default, not calibrated blocking, and no broad suppression patterns |
| Advisory no-new-debt policy report | experimental | `policy report` | core report tests, CLI parser tests, and CLI e2e cover JSON/Markdown reporting of new gaps, baseline-known cards, resolved/unmatched baseline entries, expired suppressions, ledger owner/reason/evidence/review metadata, operation expression, operation family, policy reasons, classification explanations, limitations, next action, and trust-boundary wording | advisory report only; does not block, execute witnesses, broaden suppressions, or change card classification |
| Raw pointer card slice | experimental | cards | `raw_pointer_alignment`, `align_of_only_not_guard`, `alignment_other_pointer_not_guard`, `comment_alignment_not_guard`, `raw_pointer_alignment_post_check_not_guard`, `raw_pointer_deref`, `raw_pointer_read_unaligned`, `raw_pointer_read_volatile`, `raw_pointer_read_len_capacity_assert`, `raw_pointer_read_assert_shadowed_origin_not_guard`, `raw_pointer_read_len_capacity_assert_shadowed_origin_not_guard`, `raw_pointer_read_bounds_observed_not_guard`, `raw_pointer_read_len_capacity_observed_not_guard`, `raw_pointer_read_as_cast_origin_bounds_guard`, `raw_pointer_read_cast_origin_bounds_guard`, `raw_pointer_read_open_branch_bounds_guard`, `raw_pointer_read_open_branch_shadowed_origin_not_guard`, `raw_pointer_read_typed_shadowed_origin_not_guard`, `raw_pointer_read_other_len_not_guard`, `raw_pointer_read_reassigned_origin_not_guard`, `raw_pointer_write_assignment`, `raw_pointer_write_unaligned`, `raw_pointer_write_bytes` including narrow `*mut u8` alignment/value evidence, `raw_pointer_write_bool_bytes_guard`, `raw_pointer_write_bool_conjunct_branch_guard`, and `raw_pointer_write_bool_disjunct_return_guard` for guarded `*mut bool` byte-domain evidence, `raw_pointer_write_bool_reassigned_byte_not_guard` for stale byte-domain guard rejection, `raw_pointer_write_bool_closed_branch_not_guard`, `raw_pointer_write_bool_disjunct_branch_not_guard`, and `raw_pointer_write_bool_conjunct_return_not_guard` for closed-branch or wrong-shape branch/return rejection, `raw_pointer_write_previous_slice_not_guard` for operation-specific write-bytes bounds evidence, `raw_pointer_write_previous_u8_not_guard`, `raw_pointer_write_previous_bool_not_guard`, and `raw_pointer_write_previous_maybeuninit_not_guard` for operation-specific write-bytes target evidence, `raw_pointer_write_other_u8_not_guard`, `raw_pointer_write_maybeuninit`, `raw_pointer_write_other_maybeuninit_not_guard`, `raw_pointer_write_volatile`, `split_raw_pointer_read_call`, `split_unsafe_block`, and safe-reference negative fixtures | source-level review evidence only; guarded bool byte evidence does not prove pointer liveness, bounds, or allocation; stale byte-domain guards and closed positive branches do not discharge value evidence; raw read bounds and len/capacity evidence must belong to the current pointer origin, including same-origin cast or `as *const` pointer bindings, and reject closed observation branches, typed/shadowed assertion or open-branch origins, and stale reassigned origins; write-bytes bounds and target evidence must belong to the current operation; unaligned read/write fixtures prove the card omits alignment evidence, not broader pointer validity; volatile memory/device semantics beyond raw-read/write obligations are not modeled |
| Core operation smoke slice | experimental | cards | `maybeuninit_assume_init`, `maybeuninit_assume_init_comment_not_guard`, `maybeuninit_assume_init_write_guard`, `maybeuninit_assume_init_new_guard`, `maybeuninit_assume_init_other_slot_write_not_guard`, `maybeuninit_assume_init_stale_write_not_guard`, `maybeuninit_assume_init_stale_field_write_not_guard`, `maybeuninit_assume_init_stale_new_not_guard`, `maybeuninit_assume_init_shadowed_slot_not_guard`, `maybeuninit_assume_init_read`, `maybeuninit_assume_init_ref`, `maybeuninit_assume_init_mut`, `maybeuninit_assume_init_drop`, `vec_set_len`, `vec_set_len_comment_not_guard`, `vec_set_len_initialized_loop`, `vec_set_len_capacity_observed_not_guard`, `vec_set_len_unrelated_capacity_comparison_not_guard`, `vec_set_len_capacity_binding`, `vec_set_len_stale_capacity_binding_not_guard`, `vec_set_len_cap_argument_not_guard`, `vec_set_len_reassigned_receiver_not_guard`, `vec_set_len_reassigned_new_len_not_guard`, `vec_set_len_shadowed_new_len_not_guard`, `vec_set_len_with_capacity`, `vec_set_len_reserve_capacity`, `vec_set_len_reserve_reassigned_additional_not_guard`, `vec_set_len_call_result_init`, `vec_set_len_shrink`, `vec_set_len_last_index_shrink`, `vec_set_len_stale_last_index_shrink_not_guard`, `vec_set_len_other_last_index_shrink_not_guard`, `vec_set_len_start_bound_shrink`, `vec_set_len_stale_start_bound_shrink_not_guard`, `vec_set_len_zero_clear`, `vec_set_len_post_init_not_guard`, `vec_from_raw_parts`, `vec_from_raw_parts_capacity_guard`, `vec_from_raw_parts_capacity_observed_not_guard`, `vec_from_raw_parts_capacity_return_comment_not_guard`, `box_from_raw`, `box_from_raw_box_origin`, `box_from_raw_reassigned_origin_not_guard`, `copy_nonoverlapping`, `ptr_copy_overlapping`, `ptr_replace_value`, `str_from_utf8_unchecked`, `str_from_utf8_unchecked_comment_not_guard`, `str_from_utf8_unchecked_is_ok_guard`, `str_from_utf8_unchecked_is_err_return_guard`, `str_from_utf8_unchecked_question_mark_guard`, `str_from_utf8_unchecked_match_return_guard`, `str_from_utf8_unchecked_post_validation_not_guard`, `str_from_utf8_unchecked_other_buffer_not_guard`, `str_from_utf8_unchecked_is_ok_observed_not_guard`, `str_from_utf8_unchecked_guard_then_reassigned_not_guard`, `str_from_utf8_unchecked_guard_then_mutated_not_guard`, `zeroed_invalid_value`, `zeroed_valid_u32`, `static_mut_global_state`, `inline_asm_human_review`, `transmute_invalid_value`, `transmute_layout_size_guard`, `transmute_layout_mismatch_return_comment_not_guard`, `transmute_bool_comment_not_guard`, `transmute_bool_valid_value_guard`, `transmute_bool_invalid_return_guard`, `transmute_bool_invalid_return_comment_not_guard`, `transmute_bool_other_value_not_guard`, `transmute_bool_value_observed_not_guard`, `transmute_bool_closed_if_observed_not_guard`, `transmute_bool_guard_then_reassigned_not_guard`, `transmute_bool_guard_then_shadowed_not_guard`, `transmute_copy_invalid_value`, `transmute_copy_layout_size_guard`, `transmute_copy_layout_mismatch_return_comment_not_guard`, `transmute_copy_bool_valid_value_guard`, `transmute_copy_bool_invalid_return_guard`, `transmute_copy_bool_invalid_return_comment_not_guard`, `transmute_copy_bool_value_observed_not_guard`, `transmute_copy_bool_closed_if_observed_not_guard`, `transmute_copy_bool_guard_then_reassigned_not_guard`, `multiline_transmute_copy_invalid_value`, `unwrap_unchecked_result`, `unwrap_unchecked_infallible_result`, `unwrap_unchecked_other_infallible_not_guard`, `unwrap_unchecked_is_some_guard`, `unwrap_unchecked_is_some_reassigned_not_guard`, `unwrap_unchecked_is_ok_guard`, `unwrap_unchecked_is_ok_reassigned_not_guard`, `unwrap_unchecked_if_let_some_guard`, `unwrap_unchecked_if_let_ok_guard`, `unwrap_unchecked_is_some_observed_not_guard`, `unwrap_unchecked_is_ok_observed_not_guard`, `unwrap_unchecked_guard_then_reassigned_not_guard`, `unwrap_unchecked_other_if_let_not_guard`, `unwrap_unchecked_other_if_let_ok_not_guard`, `unwrap_unchecked_post_check_not_guard`, `unwrap_unchecked_is_none_return_guard`, `unwrap_unchecked_is_none_return_comment_not_guard`, `unwrap_unchecked_is_err_return_guard`, `unreachable_unchecked_path`, `unreachable_unchecked_infallible_path`, `unreachable_unchecked_other_infallible_not_guard`, `unreachable_unchecked_post_infallible_not_guard`, `unreachable_unchecked_closed_infallible_match_not_guard`, `get_unchecked_mut_bounds`, `get_unchecked_mut_len_guard`, `get_unchecked_mut_conjunct_len_guard`, `get_unchecked_mut_get_probe_guard`, `get_unchecked_mut_get_probe_early_return_guard`, `get_unchecked_mut_if_let_get_guard`, `get_unchecked_mut_let_else_get_guard`, `get_unchecked_mut_match_get_guard`, `get_unchecked_mut_other_len_not_guard`, `get_unchecked_mut_disjunct_len_not_guard`, `get_unchecked_mut_post_check_not_guard`, `get_unchecked_mut_return_comment_not_guard`, `get_unchecked_mut_reassigned_index_not_guard`, `get_unchecked_mut_reassigned_receiver_not_guard`, `get_unchecked_mut_get_probe_reassigned_index_not_guard`, `get_unchecked_mut_get_probe_reassigned_receiver_not_guard`, `get_unchecked_mut_get_probe_early_return_reassigned_index_not_guard`, `get_unchecked_mut_if_let_get_reassigned_index_not_guard`, `get_unchecked_mut_let_else_get_reassigned_index_not_guard`, `get_unchecked_mut_match_get_reassigned_index_not_guard`, `pin_new_unchecked`, `drop_in_place_deallocation`, `drop_in_place_box_origin`, `drop_in_place_reassigned_origin_not_guard`, `atomic_pointer_state_swap`, `atomic_pointer_state_fetch_ops`, `slice_from_raw_parts_mut`, `slice_from_raw_parts_mut_maybeuninit`, `slice_from_raw_parts_mut_other_maybeuninit_not_guard`, `pointer_arithmetic_num_ctrl_bytes_guard`, `pointer_arithmetic_slice_end`, `target_feature_safety_docs`, `target_feature_missing_safety_docs`, `unsafe_fn_call_wrapper`, `multiline_unsafe_fn_call_wrapper`, `unsafe_fn_call_encode_utf8_remaining_cap`, `ffi_non_libc_wrapper_call_not_route`, `ffi_local_libc_module_call_not_route`, `unchecked_constructor_availability_guard`, `unchecked_constructor_availability_assert_guard`, `unchecked_constructor_unavailable_return_guard`, `unchecked_constructor_unavailable_return_comment_not_guard`, `unchecked_constructor_other_availability_not_guard`, `unchecked_constructor_availability_observed_not_guard`, `unchecked_constructor_availability_closed_branch_not_guard`, `nonnull_new_guard`, `nonnull_if_let_new_guard`, `nonnull_let_else_new_guard`, `nonnull_match_new_guard`, `nonnull_other_guard_not_evidence`, `nonnull_is_null_nonreturning_not_guard`, `nonnull_observed_not_guard`, `nonnull_post_check_not_guard`, `nonnull_if_let_new_reassigned_ptr_not_guard`, `nonnull_let_else_new_reassigned_ptr_not_guard`, `nonnull_match_new_reassigned_ptr_not_guard`, and `nested_unsafe_operation_call_dedupe` fixture goldens | curated fixtures, not broad semantic proof; broader `Vec::set_len`, initialization beyond pre-call local evidence for the extended range, `MaybeUninit` proof beyond same-slot `write`, same-slot `MaybeUninit::new`, comment-only rejection, stale-write rejection, stale-field-write rejection, stale-new rejection, shadowed-slot rejection, and the `assume_init`, `assume_init_read`, `assume_init_ref`, `assume_init_mut`, and `assume_init_drop` smoke fixtures, `Vec::from_raw_parts` allocator/layout evidence and capacity evidence beyond same-call `len`/`cap` bound guards, `Box::from_raw` allocator and unique-ownership evidence beyond same-pointer `Box::into_raw` origin evidence and reassignment rejection, copy range evidence beyond `copy_nonoverlapping` and overlapping `ptr::copy` smoke fixtures, `ptr::replace` drop/ownership evidence beyond operation classification, UTF-8 validation evidence beyond comment-only rejection and same-buffer `from_utf8` `is_ok` enclosing branches, `is_err` early-return, question-mark propagation, match-return evidence, and stale-buffer rejection after reassignment or direct buffer mutation, `get_unchecked` bounds evidence beyond same-receiver len guards, top-level conjunctive len guards, and `get(index)` probes, including if-let, let-else, and match Some-arm forms, valid-zero evidence beyond known primitive scalar target types and the `u32` fixture, static mutable state synchronization evidence beyond route selection, inline-asm register/memory/target evidence, transmute layout evidence beyond explicit matching `size_of` equality checks, transmute comment-only rejection, transmute valid-value evidence beyond narrow executable `u8` to `bool` byte-domain guards, open positive branches containing the call, invalid-byte early returns, and stale-guard rejection after reassignment or shadowing, `transmute_copy` ownership/drop-specific evidence beyond referenced-byte value guards, open positive branches containing the call, invalid-byte early returns, and stale-guard rejection after reassignment or shadowing, drop/deallocation beyond same-pointer `Box::into_raw` origin evidence and reassignment rejection, atomic pointer state transitions beyond narrow null swaps and pointer-returning fetch-and/or/xor operations, mutable slice range proof, pointer-arithmetic guard naming beyond narrow `num_ctrl_bytes` and same-slice end-pointer evidence, target-feature availability proof beyond documented and missing-contract declaration cards, option/result state proof beyond local infallible-result, same-receiver enclosing `is_some`/`is_ok` branches, early-return `is_none`/`is_err` guards, narrow `if let ... as_ref()` branches, and stale-receiver rejection, control-flow proof beyond open local infallible-path evidence, `NonNull` evidence beyond narrow same-argument `NonNull::new?`, same-pointer if-let, let-else, and match Some-arm `NonNull::new` guards, and same-pointer pre-constructor returning `is_null` guard evidence, `Vec::set_len` comment-only rejection and capacity evidence beyond same-vector shrink, call-result, const-capacity, same-vector capacity bindings, same-vector `Vec::with_capacity(new_len)`, narrow same-vector `reserve(additional)` for `len + additional`, argument-specific capacity bound guard patterns, unrelated-comparison rejection, local cap-name rejection, stale-additional rejection, stale-last-index shrink rejection, stale-start-bound shrink rejection, and stale-receiver rejection, nested operation attribution, and unsafe-call contract modeling beyond narrow `encode_utf8`, same-receiver unchecked-constructor availability evidence, and comment-only unavailable-return rejection remain limited |
| UTF-8 validation applicability slice | experimental | cards | fixture goldens `str_from_utf8_unchecked_if_let_err_return_comment_not_guard`, `str_from_utf8_unchecked_if_let_err_return_string_not_guard`, `str_from_utf8_unchecked_if_let_err_shadowed_not_guard`, `str_from_utf8_unchecked_if_let_ok_comment_not_guard`, `str_from_utf8_unchecked_if_let_ok_reassigned_not_guard`, `str_from_utf8_unchecked_if_let_ok_string_not_guard`, `str_from_utf8_unchecked_is_err_return_reassigned_not_guard`, `str_from_utf8_unchecked_is_err_return_shadowed_not_guard`, `str_from_utf8_unchecked_is_err_return_string_not_guard`, `str_from_utf8_unchecked_is_ok_comment_not_guard`, `str_from_utf8_unchecked_is_ok_string_not_guard`, `str_from_utf8_unchecked_let_else_ok_comment_not_guard`, `str_from_utf8_unchecked_let_else_ok_shadowed_not_guard`, `str_from_utf8_unchecked_let_else_ok_string_not_guard`, `str_from_utf8_unchecked_match_err_reassigned_not_guard`, `str_from_utf8_unchecked_match_err_shadowed_not_guard`, `str_from_utf8_unchecked_match_ok_comment_not_guard`, `str_from_utf8_unchecked_match_ok_shadowed_not_guard`, `str_from_utf8_unchecked_match_ok_string_not_guard`, `str_from_utf8_unchecked_match_return_comment_not_guard`, `str_from_utf8_unchecked_match_return_string_not_guard`, `str_from_utf8_unchecked_question_mark_comment_not_guard`, `str_from_utf8_unchecked_question_mark_string_not_guard`, and `str_from_utf8_unchecked_suffix_validation_not_guard` pin same-buffer UTF-8 validation applicability across if-let, let-else, match, question-mark, and early-return shapes, while keeping comment-only/string-only returns, suffix-only validation, stale reassignment, and shadowing from discharging the unchecked conversion | fixture-pinned source pattern only; does not prove arbitrary UTF-8 validity, indirect wrapper validation, caller behavior, site execution, Miri-clean status, UB-free status, or memory safety |
| Transmute layout/value applicability slice | experimental | cards | fixture goldens `transmute_bool_conjunct_branch_guard`, `transmute_bool_conjunct_return_not_guard`, `transmute_bool_disjunct_branch_not_guard`, `transmute_bool_disjunct_return_guard`, `transmute_bool_guard_then_compound_reassigned_not_guard`, `transmute_bool_prior_guarded_call_not_guard`, `transmute_copy_bool_comment_not_guard`, `transmute_copy_bool_conjunct_branch_guard`, `transmute_copy_bool_conjunct_return_not_guard`, `transmute_copy_bool_disjunct_branch_not_guard`, `transmute_copy_bool_disjunct_return_guard`, `transmute_copy_bool_guard_then_compound_reassigned_not_guard`, `transmute_copy_bool_guard_then_shadowed_not_guard`, `transmute_copy_bool_other_value_not_guard`, `transmute_copy_bool_prior_guarded_call_not_guard`, `transmute_copy_layout_conjunct_branch_guard`, `transmute_copy_layout_conjunct_return_not_guard`, `transmute_copy_layout_disjunct_branch_not_guard`, `transmute_copy_layout_mismatch_return_guard`, `transmute_layout_closed_branch_not_guard`, `transmute_layout_conjunct_branch_guard`, `transmute_layout_conjunct_return_not_guard`, `transmute_layout_disjunct_branch_not_guard`, `transmute_layout_mismatch_return_guard`, and `transmute_layout_observed_not_guard` pin layout and bool valid-value evidence applicability for open branches, mismatch early returns, conjunctive/disjunctive branch and return shapes, comment-only controls, prior guarded calls, other-value controls, stale compound reassignment, shadowing, closed branches, and bare observations | fixture-pinned source pattern only; does not prove arbitrary type validity, layout compatibility, provenance, aliasing, drop semantics, site execution, Miri-clean status, UB-free status, or memory safety |
| `get_unchecked_mut` stale/wrong-target applicability slice | experimental | cards | fixture goldens `get_unchecked_mut_get_probe_other_slice_not_guard`, `get_unchecked_mut_bounds_observed_not_guard`, `get_unchecked_mut_closed_bounds_not_guard`, `get_unchecked_mut_compound_reassigned_index_not_guard`, `get_unchecked_mut_shadowed_index_not_guard`, `get_unchecked_mut_reassigned_receiver_path_not_guard`, `get_unchecked_mut_shadowed_receiver_not_guard`, `get_unchecked_mut_get_probe_shadowed_index_not_guard`, `get_unchecked_mut_get_probe_reassigned_receiver_path_not_guard`, `get_unchecked_mut_get_probe_shadowed_receiver_path_not_guard`, `get_unchecked_mut_get_probe_shadowed_receiver_not_guard`, `get_unchecked_mut_get_probe_early_return_shadowed_index_not_guard`, `get_unchecked_mut_get_probe_early_return_reassigned_receiver_not_guard`, `get_unchecked_mut_get_probe_early_return_shadowed_receiver_not_guard`, `get_unchecked_mut_if_let_get_shadowed_index_not_guard`, `get_unchecked_mut_if_let_get_reassigned_receiver_not_guard`, `get_unchecked_mut_if_let_get_reassigned_receiver_path_not_guard`, `get_unchecked_mut_if_let_get_shadowed_receiver_path_not_guard`, `get_unchecked_mut_if_let_get_shadowed_receiver_not_guard`, `get_unchecked_mut_let_else_get_shadowed_index_not_guard`, `get_unchecked_mut_let_else_get_reassigned_receiver_not_guard`, `get_unchecked_mut_let_else_get_reassigned_receiver_path_not_guard`, `get_unchecked_mut_let_else_get_shadowed_receiver_path_not_guard`, `get_unchecked_mut_let_else_get_shadowed_receiver_not_guard`, `get_unchecked_mut_match_get_shadowed_index_not_guard`, `get_unchecked_mut_match_get_reassigned_receiver_not_guard`, `get_unchecked_mut_match_get_reassigned_receiver_path_not_guard`, `get_unchecked_mut_match_get_shadowed_receiver_path_not_guard`, and `get_unchecked_mut_match_get_shadowed_receiver_not_guard` pin that bounds evidence must target the same slice, receiver path, and index, and must still dominate the unsafe access after get-probe, early-return, if-let, let-else, and match forms | fixture-pinned source pattern only; does not prove arbitrary iterator bounds, aliasing, caller behavior, site execution, Miri-clean status, UB-free status, or memory safety |
| `NonNull::new_unchecked` nullability applicability slice | experimental | cards | fixture goldens `nonnull_new_guard`, `nonnull_new_reassigned_ptr_not_guard`, `nonnull_new_shadowed_ptr_not_guard`, `nonnull_cast_checked_pointer_not_guard`, `nonnull_method_receiver_reassigned_not_guard`, `nonnull_method_receiver_shadowed_not_guard`, `nonnull_if_let_new_guard`, `nonnull_let_else_new_guard`, `nonnull_match_new_guard`, `nonnull_other_guard_not_evidence`, `nonnull_is_null_nonreturning_not_guard`, `nonnull_is_null_return_comment_not_guard`, `nonnull_is_null_disjunct_return_guard`, `nonnull_is_null_conjunct_return_not_guard`, `nonnull_is_null_reassigned_ptr_not_guard`, `nonnull_is_null_shadowed_ptr_not_guard`, `nonnull_is_null_open_branch_guard`, `nonnull_is_null_conjunct_open_branch_guard`, `nonnull_is_null_disjunct_open_branch_not_guard`, `nonnull_is_null_open_branch_reassigned_ptr_not_guard`, `nonnull_is_null_open_branch_shadowed_ptr_not_guard`, `nonnull_observed_not_guard`, `nonnull_post_check_not_guard`, `nonnull_if_let_new_reassigned_ptr_not_guard`, `nonnull_if_let_new_shadowed_ptr_not_guard`, `nonnull_let_else_new_reassigned_ptr_not_guard`, `nonnull_let_else_new_shadowed_ptr_not_guard`, `nonnull_match_new_reassigned_ptr_not_guard`, and `nonnull_match_new_shadowed_ptr_not_guard` pin same-pointer nullability evidence across `NonNull::new`, if-let, let-else, match, executable null early returns, executable non-null open branches, and conjunctive/disjunctive branch shapes, while keeping cast-target uncertainty, wrong pointers, bare observations, post-checks, non-returning null checks, comment-only returns, stale reassignment, shadowing, and stale method receivers from discharging the non-null obligation | fixture-pinned source pattern only; does not prove arbitrary pointer validity, provenance, aliasing, caller behavior, site execution, Miri-clean status, UB-free status, or memory safety |
| `Vec::from_raw_parts` capacity/ownership applicability slice | experimental | cards | fixture goldens `vec_from_raw_parts`, `vec_from_raw_parts_capacity_guard`, `vec_from_raw_parts_capacity_assert_guard`, `vec_from_raw_parts_capacity_observed_not_guard`, `vec_from_raw_parts_capacity_value_observed_not_guard`, `vec_from_raw_parts_capacity_closed_branch_not_guard`, `vec_from_raw_parts_capacity_return_comment_not_guard`, `vec_from_raw_parts_capacity_reassigned_not_guard`, `vec_from_raw_parts_manuallydrop_origin`, and `vec_from_raw_parts_stale_pointer_origin_not_guard` pin capacity and same-origin raw-parts evidence, while keeping bare observations, closed branches, comment-only early returns, stale cap arguments, and stale pointer origins from discharging the matching obligation | fixture-pinned source pattern only; does not prove allocator compatibility, ownership transfer, provenance, arbitrary caller behavior, site execution, Miri-clean status, UB-free status, or memory safety |
| `Vec::set_len` capacity/initialization applicability slice | experimental | cards | fixture goldens `vec_set_len_compound_reassigned_new_len_not_guard`, `vec_set_len_self_new_const_cap_not_guard`, `vec_set_len_stale_with_capacity_len_not_guard`, `vec_set_len_stale_with_capacity_not_guard`, `vec_set_len_try_reserve_capacity`, `vec_set_len_try_reserve_reassigned_additional_not_guard`, `vec_set_len_unrelated_const_cap_not_guard`, and `vec_set_len_unrelated_initialization_not_guard` pin same-vector capacity and initialization evidence applicability, including `try_reserve(additional)?`, opaque self-capacity constructors, unrelated const-capacity controls, compound-reassigned new lengths, stale `with_capacity` receiver/new-length evidence, stale `try_reserve` additional evidence, and unrelated initialization rejection | fixture-pinned source pattern only; does not prove arbitrary vector initialization, allocation behavior, caller behavior, site execution, Miri-clean status, UB-free status, or memory safety |
| Pointer arithmetic bounds applicability slice | experimental | cards | fixture goldens `pointer_arithmetic_num_ctrl_bytes_guard`, `pointer_arithmetic_other_offset_not_guard`, `pointer_arithmetic_reassigned_offset_not_guard`, `pointer_arithmetic_shadowed_offset_not_guard`, `pointer_arithmetic_compound_offset_not_guard`, `pointer_arithmetic_stale_bound_not_guard`, `pointer_arithmetic_disjunct_bounds_not_guard`, `pointer_arithmetic_closed_branch_not_guard`, and `pointer_arithmetic_slice_end` prove narrow same-offset bounds evidence and same-slice end-pointer evidence, while different offsets, reassigned/shadowed/compound-mutated offsets, stale local bounds, disjunctive positive branches, and closed positive branches stay guard-missing | fixture-pinned source pattern only; does not prove arbitrary pointer provenance, allocation, aliasing, caller behavior, site execution, Miri-clean status, UB-free status, or memory safety |
| JS-backed buffer reentry heuristic | experimental | cards | focused analyzer tests and fixture goldens `js_buffer_reentry_sync_compression`, `js_buffer_reentry_raw_parts_materialization`, `js_buffer_reentry_options_before_capture_no_card`, `js_buffer_reentry_recapture_after_reentry_no_card`, `js_buffer_reentry_async_options_before_capture_no_card`, and `js_buffer_reentry_async_recapture_after_reentry_no_card` cover same-owner captures of `StringOrBuffer::from_js`, possible options getter or callback reentry, one-hop helper byte materialization through `byte_slice`, direct `core::slice::from_raw_parts` materialization while retaining the underlying raw-parts operation card, manual-review routing, negative controls where options/callback reentry occurs before descriptor capture, and negative controls where a descriptor is re-fetched and materialized after reentry | advisory Bun/JSC-oriented heuristic only; uses existing ReviewCard projection and stable-byte getter-reentry family wording; limited to same-owner source order plus one-hop helper or direct raw-parts materialization tied to the captured descriptor binding when parseable, not a call graph, not proof of stale backing storage, not RAB/SAB/GSAB proof, not site execution, not witness execution, not calibrated, and not policy gating |
| Stable-byte RAB async heuristic | experimental | cards | focused analyzer tests and fixture goldens `js_buffer_reentry_async_helper_capture`, `js_buffer_reentry_node_fs_rab_scalar_write`, and `js_buffer_reentry_node_fs_rab_scalar_write_scheduled_before_capture_no_card` cover same-owner non-encoded `from_js_maybe_async_into` capture before callback reentry or async worker dispatch, later helper byte materialization through `byte_slice`, node:fs-style scalar write worker materialization, stable-byte RAB async operation-family projection, observable-red-green proof-path wording, manual-review routing, and no-card controls where callback reentry occurs before capture, the descriptor is re-fetched after reentry, or async scheduling occurs before scalar write input capture | advisory Bun/JSC-oriented heuristic only; uses existing ReviewCard projection and stable-byte RAB async family wording; limited to same-owner source order plus one-hop helper or worker materialization tied to the captured descriptor binding when parseable, not a call graph, not proof of stale backing storage, not node:fs proof, not RAB/SAB/GSAB proof, not site execution, not witness execution, not calibrated, and not policy gating |
| Stable-byte SAB race heuristic | experimental | cards | focused analyzer tests and fixture goldens `stable_byte_sab_borrowed_slice`, `stable_byte_sab_mysql_blob_rawslice`, `stable_byte_sab_snapshot_no_card`, and `stable_byte_sab_mysql_blob_owned_copy_no_card` cover same-owner shared backing signals before borrowed raw-parts or temporary raw-slice materialization, retention of the underlying raw-parts operation card, stable-byte SAB race operation-family projection, mutation-plus-Miri proof-path wording, manual-review routing, and no-card controls where shared bytes are copied into owned storage before decode or MySQL packet data storage | advisory Bun/JSC-oriented heuristic only; uses existing ReviewCard projection and stable-byte SAB race family wording; limited to same-owner source order plus direct raw-parts materialization before an owned snapshot, not a call graph, not proof of stale backing storage, not SAB/GSAB proof, not MySQL site execution, not witness execution, not calibrated, and not policy gating |
| Stable-byte native FFI heuristic | experimental | cards | focused analyzer tests and fixture goldens `stable_byte_native_ffi_zstd_handoff` and `stable_byte_native_ffi_zstd_owned_copy_control` cover same-owner JS-backed byte capture before zstd-style native FFI pointer/length handoff, retention of the underlying generic FFI card, stable-byte native FFI operation-family projection, observable-red-green proof-path wording, manual-review routing, and a control where bytes are copied into owned storage before the native handoff | advisory Bun/JSC-oriented heuristic only; uses existing ReviewCard projection and stable-byte native FFI family wording; limited to same-owner source order plus zstd-style native pointer/length handoff tied to the captured descriptor binding when parseable, not a generic FFI detector, not overlap detection, not proof of stale backing storage, not zstd site execution, not witness execution, not calibrated, and not policy gating |
| Panic-from-safe-JS heuristic | experimental | cards | focused analyzer tests and fixture goldens `panic_from_safe_js_direct_try_from_expect`, `panic_from_safe_js_bound_try_from_unwrap`, `panic_from_safe_js_observed_only_not_guard`, `panic_from_safe_js_inline_max_no_card`, `panic_from_safe_js_return_guard_no_card`, and `panic_from_safe_js_non_js_signed_no_card` cover same-owner JS-derived signed `to_int32` inputs converted through panicking unsigned `try_from` `expect`/`unwrap` forms, local `panic-guard` obligation projection, human-review routing, direct and bound source shapes, no-card controls for inline nonnegative clamp and explicit negative-value error return, an observed-only branch control that still emits, and a non-JS signed local no-card control | advisory Bun/JSC-oriented heuristic only; limited to same-owner source order and narrow JS-derived signed-source shapes, not a call graph, not proof of a Bun crash, not source execution, not witness execution, not calibrated, not policy gating, and not a substitute for focused runtime receipts showing safe JS throws/returns rather than aborting |
| `MaybeUninit::assume_init` branch-scope slice | experimental | cards | fixture goldens `maybeuninit_assume_init_open_branch_write_guard` and `maybeuninit_assume_init_closed_branch_write_not_guard` prove same-slot writes count only when they dominate the `assume_init` operation, including a write in the same open branch and rejecting a closed conditional write | fixture-pinned source pattern only; does not prove arbitrary field initialization, type validity, caller behavior, Miri-clean status, or memory safety |
| `unwrap_unchecked` let-else guard slice | experimental | cards | fixture goldens `unwrap_unchecked_let_else_some_guard` and `unwrap_unchecked_let_else_some_reassigned_not_guard` prove same-receiver `let Some(_) = option.as_ref() else { return ... }` evidence can discharge the valid-value obligation, while reassignment after that guard keeps the card guard-missing | fixture-pinned source pattern only; does not prove arbitrary Option/Result validity, caller behavior, Miri-clean status, or memory safety |
| `unwrap_unchecked` Result let-else guard slice | experimental | cards | fixture goldens `unwrap_unchecked_let_else_ok_guard` and `unwrap_unchecked_let_else_ok_reassigned_not_guard` prove same-receiver `let Ok(_) = result.as_ref() else { return ... }` evidence can discharge the valid-value obligation, while reassignment after that guard keeps the card guard-missing | fixture-pinned source pattern only; does not prove arbitrary Option/Result validity, caller behavior, Miri-clean status, or memory safety |
| `unwrap_unchecked` match guard slice | experimental | cards | fixture goldens `unwrap_unchecked_match_some_guard`, `unwrap_unchecked_match_some_reassigned_not_guard`, `unwrap_unchecked_match_ok_guard`, and `unwrap_unchecked_match_ok_reassigned_not_guard` prove same-receiver match Some/Ok arms can discharge the valid-value obligation, while receiver reassignment inside the arm keeps the card guard-missing | fixture-pinned source pattern only; does not prove arbitrary Option/Result validity, caller behavior, Miri-clean status, or memory safety |
| `unwrap_unchecked` open-branch stale guard slice | experimental | cards | fixture goldens `unwrap_unchecked_is_some_reassigned_not_guard` and `unwrap_unchecked_is_ok_reassigned_not_guard` prove same-receiver `is_some` / `is_ok` branches stop counting after receiver reassignment before `unwrap_unchecked()` | fixture-pinned source pattern only; does not prove arbitrary Option/Result validity, caller behavior, Miri-clean status, or memory safety |
| Fixture calibration manifest | experimental | `fixtures/calibration.toml` / `cargo xtask check-calibration` | manifest covers positive, negative, and false-positive-control core fixture claims, including import/declaration and `cfg(target_feature)` false-positive controls, and validates expected card counts, classes, operation families, and hazards against goldens | proof index only; not real-world calibration or support-tier promotion evidence by itself |
| Real-crate dogfood measurement | experimental | dogfood handoff | `2026-05-18-real-crate-dogfood-v0.6.md` records top-50 capped `rust-smallvec`, `arrayvec`, `memchr`, `hashbrown`, `bytes`, `crossbeam`, and `mio` repo runs plus `memchr#215`, `rust-smallvec#407`, `rust-smallvec#277`, `rust-smallvec#64`, `rust-smallvec#254`, `arrayvec#308`, `arrayvec#137`, `arrayvec#138`, `arrayvec#187`, `arrayvec#174`, `arrayvec#288`, `hashbrown#469`, `hashbrown#501`, `hashbrown#556`, `hashbrown#657`, `hashbrown#667`, `hashbrown#692`, `hashbrown#681`, `hashbrown#693`, `bytes#826`, `crossbeam#1226`, `crossbeam#1187`, and `mio#1388` PR-diff runs; it also records capped `crossbeam` concurrency-heavy Send/Sync, atomic-ordering, raw pointer, ownership-transfer, and transmute_copy card measurement plus capped and PR-diff `mio` unsafe function call, `Vec::set_len`, zeroed-value, pointer-operation, socket-address layout, and unsafe Send/Sync route measurement alongside the import/declaration, adjacent unchanged unsafe declaration, `cfg(target_feature)`, capped-scan, syntax-scan performance hardening for large changed files, owner-contract inheritance, comment-aware owner inference, multi-line `impl Trait` owner-inference hardening, generic unsafe impl owner inference and Sync classification, `Safety:` doc contract evidence, attributed unsafe-fn dedupe, inline unsafe operation dedupe, `drop_in_place` operation modeling, documented public unsafe API declaration handling, documented private unsafe declaration handling, unsafe-call wrapper labeling including multi-line wrappers, narrow remaining-capacity argument evidence, and unchecked-constructor availability evidence, parent-call dedupe for nested unsafe operations, `slice::from_raw_parts_mut` operation modeling and `MaybeUninit` slice evidence, `write_bytes` raw pointer write modeling and `MaybeUninit` raw-write destination evidence, `num_ctrl_bytes` and same-slice end-pointer arithmetic bounds evidence, target-feature declaration contract evidence, len/capacity equality raw-read bounds evidence, `unwrap_unchecked` invalid-value operation modeling and local infallible-result evidence, `unreachable_unchecked` unreachable-path operation modeling and local infallible-path evidence, `Vec::from_raw_parts` operation modeling, `&'static mut` false-positive control, arrayvec#137 raw pointer accessor soundness-fix measurement, hashbrown#681 unsafe-call contract and raw-pointer deref measurement, crossbeam#1226 strict-provenance Miri cfg atomic pointer contract measurement, crossbeam#1187 atomic pointer state transition measurement, and the fixture-backed `Vec::set_len` evidence improvements including call-result initialization evidence | seven capped snapshots plus capped follow-up reruns and twenty-three PR diffs across seven crates only; no calibrated rates, no full audit, no uncapped performance claim, narrow `Vec::set_len`, `Vec::from_raw_parts`, contract evidence, owner inference including generic unsafe impl headers, Send/Sync trait classification with generic bounds, drop/deallocation, atomic pointer state transitions, mutable slice, non-`u8` raw pointer write byte-pattern validity, pointer arithmetic, target-feature declaration contracts, raw pointer read, raw pointer accessor soundness-fix measurement, `unwrap_unchecked`, `unreachable_unchecked`, unsafe-call, nested operation attribution, unsafe declaration dogfood improvements, crossbeam Send/Sync route measurement, crossbeam#1226 strict-provenance contract measurement, crossbeam#1187 atomic pointer state measurement, mio unsafe-call, zeroed-value, and socket-address layout measurement, transmute_copy classification and multi-line snippet coverage, hashbrown#681 unsafe-call contract measurement, and syntax-scan performance hardening only, and no support-tier promotion |
| Contract evidence mining | experimental | cards | public unsafe API fixtures `public_unsafe_fn_missing_safety`, `public_unsafe_trait_missing_safety`, `public_unsafe_fn_safety_comment_not_docs`, `split_public_unsafe_fn_missing_safety`, `public_unsafe_fn_with_safety_docs`, and `public_unsafe_fn_safety_colon_docs`; private/local fixtures `documented_private_unsafe_fn`, `private_unsafe_helper_safety_comment`, `local_safety_colon_comment`, and `unsafe_impl_custom_trait_not_send_sync` | comment quality is heuristic; unsafe declarations with `# Safety` or doc-comment `Safety:` docs are treated as contract-only sites, while local `SAFETY:` / `Safety:` comments remain contract evidence and do not by themselves erase guard prompts; custom unsafe trait impls that are not Send/Sync stay manual-review contract evidence, not Send/Sync concurrency routing |
| Guard evidence mining | experimental | cards | raw-pointer alignment, bare-`align_of`, other-pointer alignment, and comment-not-guard fixtures prove bounds evidence, standalone alignment constants, mismatched pointer checks, and prose do not discharge alignment; `nested_unsafe_operation_call_dedupe` proves the `NonNull::new_unchecked` operation name does not itself discharge non-null evidence | obligation-specific patterns are still sparse |
| Witness routing | experimental | cards | route-table tests plus raw pointer, atomic pointer state swap/fetch operations, FFI declaration and call fixtures `ffi_sanitizer_route`, `ffi_missing_boundary_contract`, `ffi_call_sanitizer_route`, `ffi_qualified_call_sanitizer_route`, and `ffi_libc_call_sanitizer_route`, non-`libc::` same-named local-wrapper control, unsafe impl Send through `unsafe_impl_send`, generic unsafe impl Send owner inference through `unsafe_impl_send_generic_owner`, generic unsafe impl Sync classification through `unsafe_impl_sync_generic_bound`, custom unsafe impl Send/Sync control, Pin / `pin_unchecked`, inline asm, target-feature declarations, unsafe-fn-call callee-contract wrappers including multiline wrappers, invalid-value, and drop/deallocation fixture routes; next-action tests pin manual-review wording for inline asm, `pin_unchecked`, and unsafe-fn-call callee-contract review | route recommendation only; manual deep-review routes are advisory human review work items, not witness execution, invariant proof, source edits, automatic comments, or blocking policy; receipt import is a separate surface and does not execute witnesses |
| Witness plan output | experimental | `--format witness-plan` | renderer tests and CLI e2e cover card-sourced route groups, concrete operation expression, next actions, verify commands, route limits, receipt import hints, and trust-boundary wording | route artifact only; does not execute witnesses or prove witness success |
| Witness receipt template | experimental | `receipt template` | CLI e2e covers valid JSON rendering from explicit user metadata, including `external-integration-test` / `site_reached` reach receipt metadata | authoring aid only; does not run witnesses or external integration tests, validate command output, prove witness success, independently prove site reach, or prove safety |
| Miri saved-output receipt adapter | experimental | `receipt import-miri` | core parser tests and CLI e2e cover converting a saved success log into a `miri`/`ran` receipt and rejecting failure-looking output | reads saved output only; does not run Miri, infer site reach, parse native UB diagnostics into cards, or prove safety |
| cargo-careful saved-output receipt adapter | experimental | `receipt import-careful` | core parser tests and CLI e2e cover converting a saved success log into a `cargo-careful`/`ran` receipt and rejecting failure-looking output | reads saved output only; does not run `cargo-careful`, infer site reach, parse diagnostics into cards, or prove safety |
| Sanitizer saved-output receipt adapter | experimental | `receipt import-sanitizer --tool asan|msan|tsan|lsan` | core parser tests and CLI e2e cover converting a saved success log into a sanitizer/`ran` receipt, rejecting unsupported sanitizer tools, and rejecting failure-looking output | reads saved output only; does not run sanitizers, infer site reach, parse diagnostics into cards, or prove safety |
| Loom/Shuttle saved-output receipt adapter | experimental | `receipt import-concurrency --tool loom|shuttle` | core parser tests and CLI e2e cover converting a saved success log into a concurrency/`ran` receipt, rejecting unsupported concurrency tools, and rejecting failure-looking output | reads saved output only; does not run Loom/Shuttle, infer site reach, parse interleaving diagnostics into cards, or prove scheduler coverage |
| Kani/Crux saved-output receipt adapter | experimental | `receipt import-proof --tool kani|crux` | core parser tests and CLI e2e cover converting a saved verification-success log into a proof/`ran` receipt, rejecting unsupported proof tools, and rejecting failure-looking output | reads saved output only; does not run Kani/Crux, infer site reach, parse proof diagnostics into cards, or prove beyond the recorded harness/output |
| Witness receipt validation | experimental | `receipt validate` | CLI e2e covers counting importable receipt files through the importer validation path; core receipt tests cover exact counted ReviewCard IDs, path-safe manual candidate IDs, human-review `reviewed` receipts, and external integration `site_reached` receipts with required command metadata | validation only; does not analyze cards, execute witnesses or external integration tests, independently prove site reach, or prove witness success |
| Witness receipt audit | experimental | `receipt audit` | core receipt-audit tests cover matched, importable-as-witness ReviewCard receipts, importable-as-reach external integration receipts, matched manual candidate receipts with `source = manual`, `manual_candidate = true`, and `analyzer_discovered = false`, unmatched, stale, expired, wrong-identity, wrong-tool, weaker-than-required, command-hash-mismatch, duplicate by imported evidence kind, invalid metadata, saved summary/author/recorded/command-hash/limitation fields, matched-card operation expression, operation family, and next-action context; CLI e2e covers JSON and Markdown audit metadata projection plus manual candidate receipt audit | advisory metadata audit only; external integration receipts can improve exact-card reach evidence but do not become witness evidence; manual candidate receipts attach external evidence to the manual candidate and do not become imported ReviewCard witness evidence; does not execute witnesses or external integration tests, independently prove site reach, make policy decisions, or prove witness success |
| Repo inventory, posture Markdown, status sidecars, partial output promotion, file-list artifacts, and badge JSON | experimental | `repo --format json`, `repo --format markdown`, `repo --out <file> --progress`, `repo --list-files --format json|markdown`, `badges` | core Markdown and serde-backed badge renderer tests plus CLI e2e cover repo-scope open-gap counts, posture sections, per-card source location, operation expression, operation family, related sink clusters grouped from existing ReviewCards by file and inferred owner/helper, next actions, trust boundary, `<out>.status.json` completion status with scan scope and remaining-file metadata, progress heartbeat, successful `<out>.partial` promotion, incomplete status on normal output errors, Unix SIGTERM/SIGINT terminated status markers, completed-file partial report snapshots on interruption, retained partial reports, JSON/Markdown repo file-list dry-run artifacts with replayable scan scope and no-analysis boundary, and badge messages for fixture card and zero-gap cases | static open-gap counts, report-only related-sink triage, operational scan status, and selected-file dry-run artifacts only; not calibrated, not a call graph, not proof of a shared root cause, not a safety badge, not policy gating, not portable signal handling, and not periodic partial-card flushing |
| Manual candidate import and first projections | experimental | `candidate import`, `candidate list`, `explain`, `context`, `candidate witness-plan`, `receipt validate`, `receipt audit`, `outcome`, `first-pr` manual candidate index, manual repair queue sidecar, and tokmd packet input sidecar | core parser/renderer tests and CLI e2e cover `manual-candidate/v1` canonicalization, `source = manual`, `manual_candidate = true`, `analyzer_discovered = false` in the first-pr `manual-candidates.json` index, `manual-repair-queue.json`, `tokmd-packets.json`, and `candidate list` ledger, sorted imported candidate reporting, explain fallback, context JSON fallback, witness-plan rendering, derived implementer handoff with file:line, safe caller route, invariant, optional oracle map, proof mode, fix boundary, PR aperture, external evidence commands and limitations, candidate-specific fix options, test targets, do-not-touch non-goals, and stop condition, external evidence references, manual candidate receipt matching, outcome comparison for saved single-candidate JSON artifacts and aggregate `manual-candidates/v1` indexes, source-aware manual/analyzer identity separation, first-pr verifier cross-checks between `review-kit.json` manual handoff, `manual-candidates.json`, `manual-repair-queue.json`, and `tokmd-packets.json` count/first ID/projection/oracle-map/root-local stable-byte seed-row data, bounded candidate queue, implementer handoff target/route/evidence/guidance/oracle-map/proof-mode projection, compact `pr-summary.md`, `github-summary.md`, bundled `witness-plan.md` proof-mode/fix-boundary/PR-aperture/stable-byte-seed owner and next-PR front-door cues, and ReviewCard-only `policy-report.json` / `policy-report.md` manual-candidate applicability cues with bounded queue previews and optional guidance, committed example smoke via `cargo run --locked -p xtask -- check-manual-candidate-examples`, and manual trust-boundary wording | manual/advisory candidates only; oracle maps, proof mode, fix boundary, PR aperture, and stable-byte seed rows are copy-only handoff fields, not analyzer-discovered findings, not witness execution, not repair execution, not rendered tokmd output, not proof of UB, not site execution, not repository safety, not policy gating; cards JSON, SARIF, saved LSP, ReviewCard repair queue, policy-report JSON/Markdown, and comment-plan remain ReviewCard-only unless explicitly promoted later |
| Outcome comparison | experimental | `outcome --before before.json --after after.json` | core outcome renderer tests and CLI e2e cover comparing saved JSON snapshots, new-card counts, reviewer-delta movement reasons, top remaining gap card/class/priority/missing-count/family/next-action projection, operation expression, operation family, next action, witness receipt strength movement, Markdown output, and trust-boundary wording; repo-policy handoff records capped `memchr` saved-snapshot dogfood with 10 improved cards and 40 unchanged cards | compares existing snapshots only; limited dogfood on one capped repo snapshot pair; does not rerun analysis, execute witnesses, or make policy decisions |
| PR Markdown summary | experimental | PR artifact Markdown | `pr_summary` renderer tests, CLI `--format pr-summary`, CLI e2e, first-pr e2e, and advisory workflow upload cover card counts, mixed-diff scope header bullets, reviewer cockpit front panel, top card, missing/weak evidence, next reviewer action, witness route, explain/context commands, repair-queue agent-readiness cue, concrete operation expression, operation family, card table, witness plan, and trust-boundary wording | advisory artifact only; diff-scope counts are telemetry and do not create non-Rust cards; no comments, source edits, witness execution, agent execution, proof, or blocking policy |
| SARIF projection | experimental | PR artifact SARIF | `sarif` renderer tests, CLI `--format sarif`, CLI e2e, advisory artifact verifier, and advisory workflow upload cover card IDs, concrete operation expression, structured route details, verify commands, and trust boundary | advisory static review evidence; no default blocking |
| Advisory PR workflow | experimental | GitHub Actions artifacts | workflow renders cards JSON, PR summary, GitHub summary, SARIF, comment plan, witness plan, receipt audit, manual candidate index, manual repair queue sidecar, tokmd packet input sidecar, LSP JSON, and repair queue; runs `cargo xtask check-advisory-artifacts target/unsafe-review` before upload; downloaded and first-pr artifacts are verified for trust boundary, schema versions, ReviewCard identity, review-kit and repair-queue summary count alignment with cards JSON, review-kit bounded ReviewCard queue projection from cards JSON and repair-queue buckets/readiness, bounded GitHub summary, receipt-audit metadata-only boundary, manual candidate advisory/manual markers, manual repair queue alignment with manual-candidates.json, tokmd packet alignment with manual-candidates.json, comment-plan review budget, repair-queue bucket/readiness/do-not-do contract, and no `WorkspaceEdit` | no comments, witnesses, agents, source edits, rendered tokmd output, or blocking policy |
| Inline comment plan | experimental | PR artifact JSON | `comment_plan` renderer tests, CLI `--format comment-plan`, CLI e2e, and advisory artifact verifier cover plan-only mode, projected card IDs, selected and not-selected review-budget summary, closed reason/relevance/actionability vocabularies, changed-line-scoped selected comments, duplicate card/anchor rejection, concrete operation expression, structured next actions, route details, verify commands, artifact-only/no-posting comment-body wording, zero-gap `no_changed_gaps` wording, and advisory workflow upload | artifact-only; no posting by default; selection is reviewer-noise budgeting, not card truth or policy gating |
| Saved LSP JSON projection | experimental | `--format lsp` JSON | `lsp_projection` renderer tests and CLI e2e cover read-only status data, diagnostics with concrete operation expression, next action, witness route details, verify commands, hovers with concrete operation expression and missing-evidence context, reach-limitation wording, copy-command action data, and related-test open-command data projected from ReviewCards | no LSP server, no editor integration, no source edits, and no claim that static related-test mentions executed the unsafe site |
| LSP server/editor integration | planned | editor | saved-card fixtures | read-only first |
| Explain and agent packets | experimental | `explain <card-id>` / `context <card-id> --json` | `card_detail` and `agent_packet` renderer tests plus CLI e2e cover card-scoped explanation, concrete operation expression, operation family, routes, verify commands, first-confirmation cue, minimal repro cue, bounded read-only packets, allowed repairs that do not offer suppression as repair, repair-queue buckets, do-not-do rules including no current-card suppression as repair, stop conditions, and trust-boundary wording projected from ReviewCards | copy-only; no agent execution, source edits, comments, witness execution, `build_this_first` or minimal repro execution, or repair success claim |
| Witness receipt import | experimental | `.unsafe-review/receipts/*.json` / `WitnessReceipt` SDK DTO | receipt parser tests cover exact counted ReviewCard identity, path-safe manual candidate identity, tool, strength, author, timestamp, and expiry validation; `WitnessReceipt` serde round-trip tests cover the public receipt shape; analyzer tests cover exact-card import and keep `configured` or wrong-tool receipts from clearing witness gaps; `raw_pointer_alignment_receipted` golden covers rendered card output | imports receipts only; `configured`, wrong-tool, and manual candidate receipts remain audit metadata and do not mark ReviewCard witness evidence present; does not execute witnesses, does not prove repository safety, and matches exact ReviewCard or manual candidate identity only |
| MIR/nightly facts | deferred | optional adapter | ADR needed | not v0.1 product default |

Recent proof-index additions that are tracked by the calibration ledger but not
expanded inline in the long table row above include
`str_from_utf8_unchecked_comment_not_guard` for comment-only UTF-8 validation
rejection,
`str_from_utf8_unchecked_if_let_ok_guard` for same-buffer if-let `Ok` UTF-8
validation before `str::from_utf8_unchecked`,
`str_from_utf8_unchecked_if_let_err_return_guard` for same-buffer if-let `Err`
early-return validation,
`str_from_utf8_unchecked_is_err_return_comment_not_guard` for rejecting
comment-only return text inside an `is_err` branch,
`str_from_utf8_unchecked_let_else_ok_guard` for same-buffer let-else `Ok`
validation,
`str_from_utf8_unchecked_match_ok_guard` for same-buffer match `Ok` arm
validation, `str_from_utf8_unchecked_if_let_err_reassigned_not_guard` for
stale-buffer rejection after if-let `Err` validation,
`str_from_utf8_unchecked_guard_then_shadowed_not_guard` for stale-buffer
rejection after binding shadowing,
`str_from_utf8_unchecked_guard_then_mutated_not_guard` for stale-buffer
rejection after direct buffer mutation,
`str_from_utf8_unchecked_if_let_ok_shadowed_not_guard` for stale if-let `Ok`
branch validation after binding shadowing,
`str_from_utf8_unchecked_let_else_ok_reassigned_not_guard` for stale-buffer
rejection after let-else validation, and
`str_from_utf8_unchecked_match_ok_reassigned_not_guard` for stale-buffer
rejection after reassignment inside that arm, and
`str_from_utf8_unchecked_prefix_validation_not_guard` for rejecting prefix-only
UTF-8 validation as full-buffer evidence.
