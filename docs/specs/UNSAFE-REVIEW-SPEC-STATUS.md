# Spec lifecycle status dashboard

This dashboard is an operator view over specification lifecycle state.

Until an automated generator lands, keep this file aligned with the current
active goal, implementation plan, and linked closeouts.

| Spec | Status | Implementation state | Proof commands | Last touched | Notes |
|---|---|---|---|---|---|
| `UNSAFE-REVIEW-SPEC-0019` first-run cockpit | accepted, release-prepped | Source and swarm lanes promoted; publication receipt documented | `cargo run --locked -p xtask -- check-first-pr-artifacts`; first-run smoke from release prep | 2026-05-21 | Reviewer-first cockpit contract for `doctor`, `first-pr`, `explain`, bundle honesty |
| `UNSAFE-REVIEW-SPEC-0020` source-of-truth stack | accepted, active-maintenance | Artifact taxonomy and linkage landed; current lane still tracks source-of-truth operations | `cargo run --locked -p xtask -- check-doc-artifacts`; `cargo run --locked -p xtask -- check-goals`; `cargo run --locked -p xtask -- check-ci-lanes`; `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- source-divergence` | 2026-05-23 | Requires active goal/plan freshness so the repo answers “what next?” without chat context |
| `UNSAFE-REVIEW-SPEC-0012` LSP/editor projection | accepted, partial-runtime | Saved projection contract promoted; live runtime remains limited to swarm lanes | `cargo run --locked -p xtask -- check-first-pr-artifacts`; projection contract/doc checks in `check-pr` | 2026-05-21 | Keep “no overclaim” boundary: saved projection is product truth; live server rollout is separate |
| `UNSAFE-REVIEW-SPEC-0013` agent packet projection | accepted | `unsafe-review context <card-id> --json` emits a bounded `ReviewCard`-sourced packet with card identity, operation context, source context, obligation evidence, missing evidence, agent-readiness, repair queue, witness routes, verify commands, `confirmation_cue` with minimal repro cue, do-not-do rules, stop conditions, and trust boundary | `cargo test -p unsafe-review-core agent_packet`; `cargo test -p unsafe-review --test e2e check_artifact_formats_context_and_explain_work_end_to_end`; `cargo run --locked -p xtask -- check-pr` | 2026-06-04 | Copy-only agent handoff; does not run agents, witnesses, `build_this_first` or minimal repro commands, source edits, comments, proof, site execution, UB-free, Miri-clean, or policy-ready claims |
| `UNSAFE-REVIEW-SPEC-0023` first-hour experience | accepted | `docs/FIRST_HOUR.md` ships the maintainer install/run/explain/support walkthrough, and `docs/FIND_AND_FIX_UB.md` carries the public `first-pr -> pr-summary -> explain -> context -> witness-plan -> receipt audit -> outcome` workflow; `cargo run --locked -p xtask -- check-first-hour` keeps the documented first-hour commands, artifact paths, fixture reference, trust-boundary text, and inbound links runnable, while `check-docs` verifies the public workflow order and UB-risk seam/review-gap wording | `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-first-hour`; `cargo run --locked -p xtask -- check-pr` | 2026-06-05 | Keeps post-first-run and public repair-loop surfaces ReviewCard-derived and advisory |
| `UNSAFE-REVIEW-SPEC-0024` CI design | accepted | CI lane taxonomy, permissions, example workflow shape, coverage posture, source/swarm CI routing, trusted-poster split, and first-pr review-kit ReviewCard queue artifact checks are documented and ledger-backed | `cargo run --locked -p xtask -- check-ci-lanes`; `cargo run --locked -p xtask -- check-docs-automation`; `cargo run --locked -p xtask -- check-pr`; `cargo run --locked -p xtask -- source-divergence` | 2026-06-03 | CI hard-fails malformed artifacts and repo policy failures, not advisory unsafe-review findings by default; swarm budget mode keeps coverage push/manual, GitHub-hosted fallback explicit, duplicate advisory lanes path-scoped, and review-kit queue previews bounded to checked cards/repair-queue state |
| `UNSAFE-REVIEW-SPEC-0021` VS Code/Open VSX extension | accepted, partial-runtime | Saved-`lsp.json` viewer MVP ships in `editors/vscode/` with packaging CI (`.github/workflows/editor-extension.yml`) and a `workflow_dispatch`-only manual publish workflow (`.github/workflows/editor-publish.yml`). Live `unsafe-review lsp` client wiring inside the extension stays deferred behind the SPEC-0018 hardening gate | `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-ci-lanes`; `cargo run --locked -p xtask -- check-pr` | 2026-05-23 | Thin read-only adapter over saved `lsp.json` today; no source edits, witness execution, telemetry by default, policy enforcement, or safety claim |
| `UNSAFE-REVIEW-SPEC-0022` PR commenting experience | accepted | `comment-plan.json` ships `summary` review-budget counts and reason codes, selected candidate fields (`card_id`, `path`, `line`, `class`, `priority`, `confidence`, `hypothesis_to_confirm`, `operation`, `operation_family`, `next_action`, `verify_commands`, `build_this_first`, `minimal_repro`, `confirmation_step`, `selection_reason`, `selection_reason_code`, `actionability`, `relevance`, `agent_readiness`, `repair_queue_buckets`, `repair_queue_bucket_reasons`, `context_command`, `body`, and `trust_boundary`), plus `not_selected[]` reasons and `reason_code` values with operation, next-action, and repair handoff context; `check-first-pr-artifacts` enforces the contract, caps bodies, verifies summary counts/reason codes, and rejects duplicates, unknown relevance or reason-code values, drifted repair handoff metadata, and drifted first-confirmation/minimal-repro cues | `cargo run --locked -p xtask -- check-first-pr-artifacts target/unsafe-review`; `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-pr` | 2026-06-04 | Plan-only artifact; `unsafe-review` does not post comments, run agents, run `build_this_first` or `minimal_repro` commands, or make findings authoritative by default |
| `UNSAFE-REVIEW-SPEC-0028` coverage instrument and product boundary | proposed | Umbrella for the ease-of-use lane; fixes the framing that `unsafe-review` "is the PR gate" to "unsafe coverage instrument with a standalone gate mode, consumed as a lens inside an orchestrator such as `ub-review`"; maps the five delivery surfaces to consumers by lifecycle moment and routes child specs 0029-0034; no analyzer or behavior change | `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- check-docs` | 2026-06-06 | Routing/boundary umbrella only; emits artifacts orchestrators consume, does not become the orchestrator; no proof, UB-free, Miri-clean, site-execution, calibrated precision/recall, or policy-readiness claim |
| `UNSAFE-REVIEW-SPEC-0029` unsafe evidence coverage model | proposed | Data-model slice landed: `CoverageBlock` struct with 9 closed-vocabulary slots, `CoverageBlock::derive(card)` derivation from existing evidence, `ReviewCard::coverage_block()` method, and JSON projection as `coverage` object on each card; baseline/movement/comment-plan slots default honestly; 559 golden fixtures regenerated; surface-refactor and cross-surface parity deferred to later slices | `cargo test -p unsafe-review-core`; `cargo run --locked -p xtask -- check-pr` | 2026-06-06 | Coverage describes review evidence, not safety; `present` contract coverage is not correctness and receipt coverage reflects an imported receipt claim, not execution; no proof, UB-free, Miri-clean, or site-execution claim |
| `UNSAFE-REVIEW-SPEC-0030` baseline and coverage movement | proposed | Full slice landed: `Summary` gains `new_gaps`/`worsened_gaps`/`resolved_gaps`/`inherited_gaps` counts; `CoverageBlock.baseline_state` and `outcome_movement` derived from card class; `--policy no-new-debt` exits nonzero iff `new` OR `worsened` is non-empty (diff-scoped, changed-line attributed); policy report projects all four movement buckets; `baseline init` scans repo for open actionable cards and writes the baseline ledger (`policy/unsafe-review-baseline.toml`) + coverage snapshot (`policy/unsafe-review-baseline-snapshot.toml`); `baseline add` adds/updates a single baseline entry + snapshot; `worsened_gaps` is now live — a `BaselineKnown` card whose current coverage block is strictly worse than the stored snapshot on any slot counts as worsened; `SnapshotCoverage::is_worsened_by` ordinal comparison (present=2, weak=1, missing=0); focused unit tests prove worsened detection and that an inherited-unchanged card is NOT counted as worsened | `cargo test -p unsafe-review-core policy`; `cargo test -p unsafe-review-cli`; `cargo test -p unsafe-review`; `cargo run --locked -p xtask -- check-pr` | 2026-06-06 | Baseline entries are debt records, not safety records; a no-new-debt pass means only that the change added no actionable coverage gaps above baseline; default advisory, no blocking, no proof/UB-free/Miri-clean/site-execution/policy-readiness claim |
| `UNSAFE-REVIEW-SPEC-0031` baseline-aware repo badge | proposed | Badge counts become baseline-aware coverage posture (open actionable; missing+weak) read from the coverage block against the baseline floor; numeric-only Shields-safe payloads with meaning behind a linked page; documents the consumer README-snippet + main-branch refresh adoption path; badge is a projection, not a gate | `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- check-docs` | 2026-06-06 | Numeric coverage-gap counts only; not safety, UB-free, Miri-clean, or site-execution; the badge does not run CI or block |
| `UNSAFE-REVIEW-SPEC-0032` comment-plan coverage hardening | proposed | Hardens the plan-only `comment-plan.json` (extends SPEC-0022): every selected comment names its coverage slot, carries card/manual identity + changed-line status + selection reason, not_selected carries a reason code, and a forbidden-claim rail-cages any generated body; surfaces confirmation_state | `cargo run --locked -p xtask -- check-first-pr-artifacts`; `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-pr` | 2026-06-06 | Plan-only artifact hardening; posting is opt-in and off by default (standalone gate posts via the trusted-poster path, the orchestrator posts when embedded); no proof/UB-free/Miri-clean/site-execution claim |
| `UNSAFE-REVIEW-SPEC-0033` LLM context packet | proposed | Stabilizes `context --json` / saved LSP as the agent-facing optic: coverage slots, missing evidence, allowed repairs, do-not-do rules, receipt route, baseline state, comment-plan state, and a staleness/generation marker, plus a file:line-range query so an agent gets a bounded packet list in one call with a changed-line filter | `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-pr` | 2026-06-06 | Copy-only bounded optic for agents, not authority; no execution, source edits, posting, or proof/UB-free/Miri-clean/site-execution claim |
| `UNSAFE-REVIEW-SPEC-0034` ub-review gate manifest | proposed | Defines `unsafe-review-gate.json`: a versioned manifest with status, a movement summary (new/worsened/resolved/inherited), pointers to the structured artifacts, and trust boundary, co-designed with ripr's `gate-decision.json` so the LLM layer routes by schema instead of scraping markdown | `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-pr` | 2026-06-06 | A routing manifest over advisory coverage artifacts; unsafe-review does not decide blocking, post, or run other sensors; not a merge verdict or proof |
| `UNSAFE-REVIEW-SPEC-0035` repo-scan diagnosability | proposed | Codifies the repo-mode scan contract a large-repo gate depends on: include/exclude/default ignores, list-files/dry-run, the `repo-scan-status/v1` progress/status sidecar (including optional `output_bytes` disk-footprint field), timeout/SIGTERM durable status + partial artifacts, direct file:line, and no silent empty output; adds an acceptance-smoke obligation over already-implemented behavior | `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-pr` | 2026-06-12 | Bounded, diagnosable scan posture; a completed-file partial is a snapshot, not whole-repo coverage proof; no analyzer change or blocking; `output_bytes` is diagnostic aperture only, not coverage/proof/UB-free/Miri-clean claim |
| `UNSAFE-REVIEW-SPEC-0037` PR-gate composite GitHub Action | proposed | Composite action contract: `cargo install` binary acquisition from crates.io, inputs (`base_ref`, `version`, `out_dir`, `fail_on_new_debt`), outputs (`bundle_dir`, `gate_status`), first-pr bundle artifacts including `unsafe-review-gate.json`, advisory posture (no write tokens, no comment posting, no default blocking, no witness execution), failure categories (tool-failure vs policy-result vs advisory-pass), and the fixed trust-boundary line; development copy in unsafe-review-swarm; published surface is `EffortlessMetrics/unsafe-review@v1` | `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- check-pr` | 2026-06-12 | Advisory coverage instrument only; does not post comments, block by default, run witnesses, claim memory-safety proof, UB-free status, Miri-clean status, or site-execution proof; `gate_status` is advisory metadata, not a merge verdict |
| `UNSAFE-REVIEW-SPEC-0025` docs automation | accepted | Policy ledger, `check-docs-automation`, `check-spec-status`, agent operating contract verification, active lane-plan wording checks, and external-agent-root boundaries landed; generator implementation remains follow-up | `cargo run --locked -p xtask -- check-docs-automation`; `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- check-doc-artifacts`; `cargo run --locked -p xtask -- check-goals`; `cargo run --locked -p xtask -- check-pr` | 2026-05-23 | Establishes machine-checkable docs automation control-plane boundaries before new generators land |
| `UNSAFE-REVIEW-SPEC-0026` accuracy validation and calibration | proposed | Label-ledger checker, checked calibration report, and claim-promotion guardrails landed with support-tier, claim-level wording, label-ledger trust-boundary limits, shared forbidden-claim, claim-fixture sample-map, and duplicate fixture/obligation/evidence sample checks plus fixture-pinned evidence, route-quality, identity-stability, and no-card honesty ledgers for raw pointer operation-family smoke, alignment/bounds/write initialization, private/local unsafe contract evidence, slice::from_raw_parts_mut initialized-memory, public unsafe API docs, Box::from_raw ownership, ptr::drop_in_place Box-origin evidence/routes, generic unsafe call callee-contract evidence, get_unchecked_mut bounds evidence, pointer arithmetic bounds evidence, copy_nonoverlapping valid-range, ptr::copy valid-range, NonNull::new_unchecked nullability, MaybeUninit::assume_init, Vec::set_len initialized-range and shrink controls, Vec::from_raw_parts capacity, transmute bool validity and multiline copy controls, UTF-8 unchecked conversion, `mem::zeroed` valid-zero evidence, `unreachable_unchecked` infallible-path evidence, `unwrap_unchecked` valid-value and Result if-let controls, diff unsafe site owner/site-kind identity, unsafe impl Send/Sync, atomic pointer state Loom/Shuttle routing, FFI route/obligation evidence, inline assembly human-review routing, static mut Loom/Shuttle routing, `Pin::new_unchecked` human-review routing, target-feature human-review routing, alignment receipt/non-guard controls, and zero-card controls | `cargo run --locked -p xtask -- check-calibration`; `cargo run --locked -p xtask -- check-dogfood`; `cargo run --locked -p xtask -- check-pr`; `cargo run --locked -p xtask -- source-divergence` | 2026-05-30 | Claim-scoped calibration only; checked report, label-ledger boundaries, and claim guardrails still make no global precision/recall, policy-ready, witness-execution, UB-free, or Miri-clean claim |
| `UNSAFE-REVIEW-SPEC-0027` manual candidate cards | proposed, partial-runtime | `manual-candidate/v1` import, canonical `source = manual` / `manual_candidate = true` markers, `candidate list` manual ledger reporting, explain/context fallback, derived implementer handoff with evidence commands, limitations, candidate-specific fix options, test targets, and do-not-touch non-goals, optional `oracle_map`, `proof_mode`, `fix_boundary`, and `pr_aperture` preservation, candidate witness-plan rendering, manual candidate receipt validation/audit matching with route/invariant/evidence/guidance/proof-mode cues, outcome comparison for single manual candidate JSON artifacts and aggregate `manual-candidates/v1` indexes with route/invariant/evidence/guidance/proof-mode handoff projection, first-pr `manual-candidates.json` index plus bounded review-kit candidate queue and verifier-checked review-kit proof-mode/stable-byte/ledger/oracle/guidance mix summaries, checked review-kit unified repair-queue front panel for ReviewCard and manual-candidate queue counts, dedicated `manual-repair-queue.json` sidecar cross-checks, and `tokmd-packets.json` formatting input sidecar cross-checks including packet-local `manual_repair_queue_item` projection from the manual repair queue, per-preset Bun packet inputs, optional root-local stable-byte seed-row metadata, and ReviewCard-only comment-plan review-budget metadata, compact `pr-summary.md`, `github-summary.md`, and bundled `witness-plan.md` proof-mode/fix-boundary/PR-aperture/stable-byte seed owner and next-PR front-door cues, and ReviewCard-only `policy-report.json` / `policy-report.md` manual-candidate applicability checks, committed-example smoke generation, structured ReviewCard-only applicability metadata for manual-candidate surfaces, first-pr verifier rejection of manual-candidate marker leakage in ReviewCard-only cards/SARIF/comment-plan/LSP/repair-queue/policy-report artifacts, stable-byte-source vocabulary for advisory Bun-style candidate packets, spec ledger, and support-tier row landed | `cargo test -p unsafe-review-core manual_candidate`; `cargo test -p unsafe-review-core receipt`; `cargo test -p unsafe-review-core outcome`; `cargo test -p unsafe-review-cli candidate`; `cargo test -p unsafe-review manual_candidate`; `cargo test -p unsafe-review first_pr_writes_standard_advisory_review_bundle`; `cargo test -p xtask manual_candidate`; `cargo test -p xtask first_pr_artifact_checker`; `cargo run --locked -p xtask -- check-manual-candidate-examples`; `cargo run --locked -p xtask -- check-pr`; `cargo run --locked -p xtask -- source-divergence` | 2026-06-04 | Manual candidates, oracle-map fields, proof-mode fields, stable-byte classifications, stable-byte seed rows, and tokmd packet inputs must stay advisory and labeled manual; comment-plan metadata in tokmd remains ReviewCard-only and plan-only; these inputs are not analyzer-discovered findings, imported ReviewCard witness evidence, ReviewCard repair-queue entries, policy-report inputs, rendered tokmd output, proof, site execution, UB-free status, Miri-clean status, or policy-ready claims |

## Reading notes

- **Status** is specification lifecycle intent (draft/accepted/etc.).
- **Implementation state** describes repository reality, including promotions and deferrals.
- **Proof commands** are the minimum commands that must stay green for the listed claim posture.

## Machine Check

`cargo run --locked -p xtask -- check-spec-status` validates that this table
keeps indexed specs visible, references existing spec files, includes known
`xtask` proof commands, and uses machine-readable status/date fields.
