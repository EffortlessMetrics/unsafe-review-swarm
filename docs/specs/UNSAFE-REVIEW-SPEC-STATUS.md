# Spec lifecycle status dashboard

This dashboard is an operator view over specification lifecycle state.

Until an automated generator lands, keep this file aligned with the current
active goal, implementation plan, and linked closeouts.

| Spec | Status | Implementation state | Proof commands | Last touched | Notes |
|---|---|---|---|---|---|
| `UNSAFE-REVIEW-SPEC-0019` first-run cockpit | accepted, release-prepped | Source and swarm lanes promoted; publication receipt documented | `cargo run --locked -p xtask -- check-first-pr-artifacts`; first-run smoke from release prep | 2026-05-21 | Reviewer-first cockpit contract for `doctor`, `first-pr`, `explain`, bundle honesty |
| `UNSAFE-REVIEW-SPEC-0020` source-of-truth stack | accepted, active-maintenance | Artifact taxonomy and linkage landed; current lane still tracks source-of-truth operations | `cargo run --locked -p xtask -- check-doc-artifacts`; `cargo run --locked -p xtask -- check-goals`; `cargo run --locked -p xtask -- check-ci-lanes`; `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- source-divergence` | 2026-05-23 | Requires active goal/plan freshness so the repo answers “what next?” without chat context |
| `UNSAFE-REVIEW-SPEC-0012` LSP/editor projection | accepted, partial-runtime | Saved projection contract promoted; live runtime remains limited to swarm lanes | `cargo run --locked -p xtask -- check-first-pr-artifacts`; projection contract/doc checks in `check-pr` | 2026-05-21 | Keep “no overclaim” boundary: saved projection is product truth; live server rollout is separate |
| `UNSAFE-REVIEW-SPEC-0023` first-hour experience | accepted | `docs/FIRST_HOUR.md` ships the maintainer install/run/explain/support walkthrough; `cargo run --locked -p xtask -- check-first-hour` keeps the documented commands, artifact paths, fixture reference, trust-boundary text, and inbound links runnable | `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-first-hour`; `cargo run --locked -p xtask -- check-pr` | 2026-05-23 | Keeps post-first-run surfaces ReviewCard-derived and advisory |
| `UNSAFE-REVIEW-SPEC-0024` CI design | accepted | CI lane taxonomy, permissions, example workflow shape, coverage posture, source/swarm CI routing, and trusted-poster split are documented and ledger-backed | `cargo run --locked -p xtask -- check-ci-lanes`; `cargo run --locked -p xtask -- check-docs-automation`; `cargo run --locked -p xtask -- check-pr`; `cargo run --locked -p xtask -- source-divergence` | 2026-05-26 | CI hard-fails malformed artifacts and repo policy failures, not advisory unsafe-review findings by default; swarm budget mode keeps coverage push/manual, GitHub-hosted fallback explicit, and duplicate advisory lanes path-scoped |
| `UNSAFE-REVIEW-SPEC-0021` VS Code/Open VSX extension | accepted, partial-runtime | Saved-`lsp.json` viewer MVP ships in `editors/vscode/` with packaging CI (`.github/workflows/editor-extension.yml`) and a `workflow_dispatch`-only manual publish workflow (`.github/workflows/editor-publish.yml`). Live `unsafe-review lsp` client wiring inside the extension stays deferred behind the SPEC-0018 hardening gate | `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-ci-lanes`; `cargo run --locked -p xtask -- check-pr` | 2026-05-23 | Thin read-only adapter over saved `lsp.json` today; no source edits, witness execution, telemetry by default, policy enforcement, or safety claim |
| `UNSAFE-REVIEW-SPEC-0022` PR commenting experience | accepted | `comment-plan.json` ships `summary` review-budget counts and reason codes, selected candidate fields (`card_id`, `path`, `line`, `class`, `priority`, `confidence`, `operation`, `operation_family`, `next_action`, `verify_commands`, `selection_reason`, `selection_reason_code`, `actionability`, `relevance`, `body`, and `trust_boundary`), plus `not_selected[]` reasons and `reason_code` values with operation and next-action context; `check-first-pr-artifacts` enforces the contract, caps bodies, verifies summary counts/reason codes, and rejects duplicates and unknown relevance or reason-code values | `cargo run --locked -p xtask -- check-first-pr-artifacts target/unsafe-review`; `cargo run --locked -p xtask -- check-docs`; `cargo run --locked -p xtask -- check-pr` | 2026-05-30 | Plan-only artifact; `unsafe-review` does not post comments by default and does not make findings authoritative |
| `UNSAFE-REVIEW-SPEC-0025` docs automation | accepted | Policy ledger, `check-docs-automation`, `check-spec-status`, agent operating contract verification, active lane-plan wording checks, and external-agent-root boundaries landed; generator implementation remains follow-up | `cargo run --locked -p xtask -- check-docs-automation`; `cargo run --locked -p xtask -- check-spec-status`; `cargo run --locked -p xtask -- check-doc-artifacts`; `cargo run --locked -p xtask -- check-goals`; `cargo run --locked -p xtask -- check-pr` | 2026-05-23 | Establishes machine-checkable docs automation control-plane boundaries before new generators land |
| `UNSAFE-REVIEW-SPEC-0026` accuracy validation and calibration | proposed | Label-ledger checker, checked calibration report, and claim-promotion guardrails landed with support-tier, claim-level wording, label-ledger trust-boundary limits, shared forbidden-claim, claim-fixture sample-map, and duplicate fixture/obligation/evidence sample checks plus fixture-pinned evidence, route-quality, identity-stability, and no-card honesty ledgers for raw pointer operation-family smoke, alignment/bounds/write initialization, private/local unsafe contract evidence, slice::from_raw_parts_mut initialized-memory, public unsafe API docs, Box::from_raw ownership, ptr::drop_in_place Box-origin evidence/routes, generic unsafe call callee-contract evidence, get_unchecked_mut bounds evidence, pointer arithmetic bounds evidence, copy_nonoverlapping valid-range, ptr::copy valid-range, NonNull::new_unchecked nullability, MaybeUninit::assume_init, Vec::set_len initialized-range and shrink controls, Vec::from_raw_parts capacity, transmute bool validity and multiline copy controls, UTF-8 unchecked conversion, `mem::zeroed` valid-zero evidence, `unreachable_unchecked` infallible-path evidence, `unwrap_unchecked` valid-value and Result if-let controls, diff unsafe site owner/site-kind identity, unsafe impl Send/Sync, atomic pointer state Loom/Shuttle routing, FFI route/obligation evidence, inline assembly human-review routing, static mut Loom/Shuttle routing, `Pin::new_unchecked` human-review routing, target-feature human-review routing, alignment receipt/non-guard controls, and zero-card controls | `cargo run --locked -p xtask -- check-calibration`; `cargo run --locked -p xtask -- check-dogfood`; `cargo run --locked -p xtask -- check-pr`; `cargo run --locked -p xtask -- source-divergence` | 2026-05-30 | Claim-scoped calibration only; checked report, label-ledger boundaries, and claim guardrails still make no global precision/recall, policy-ready, witness-execution, UB-free, or Miri-clean claim |
| `UNSAFE-REVIEW-SPEC-0027` manual candidate cards | proposed, partial-runtime | `manual-candidate/v1` import, canonical `source = manual` / `manual_candidate = true` markers, explain/context fallback, candidate witness-plan rendering, spec ledger, and support-tier row landed; receipt and outcome integration remain follow-up | `cargo test -p unsafe-review-core manual_candidate`; `cargo test -p unsafe-review-cli candidate`; `cargo test -p unsafe-review manual_candidate_import_explain_context_and_witness_plan_preserve_manual_marker`; `cargo run --locked -p xtask -- check-pr`; `cargo run --locked -p xtask -- source-divergence` | 2026-05-31 | Manual candidates must stay advisory and labeled manual; they are not analyzer-discovered findings, proof, site execution, UB-free status, Miri-clean status, or policy-ready claims |

## Reading notes

- **Status** is specification lifecycle intent (draft/accepted/etc.).
- **Implementation state** describes repository reality, including promotions and deferrals.
- **Proof commands** are the minimum commands that must stay green for the listed claim posture.

## Machine Check

`cargo run --locked -p xtask -- check-spec-status` validates that this table
keeps indexed specs visible, references existing spec files, includes known
`xtask` proof commands, and uses machine-readable status/date fields.
