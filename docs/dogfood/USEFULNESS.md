# Dogfood Usefulness Rollup

Status: generated counts file

Generated by `cargo run --locked -p xtask -- dogfood-usefulness`; do not edit by hand.
`cargo run --locked -p xtask -- check-dogfood` fails when this file drifts
from the committed judgment records.

This rollup aggregates raw per-label usefulness counts from the maintainer
judgment records under [`judgments/`](judgments/README.md). It counts labeled
rows only.

## Totals

| Measure | Count |
|---|---:|
| Judgment files | 7 |
| Judged cards | 17 |
| Missed-obligation rows | 0 |

## Judged Cards Per Label

| Label | Count |
|---|---:|
| `actionable` | 12 |
| `noise` | 2 |
| `missed` | 0 |
| `uncertain` | 1 |
| `human-only` | 2 |
| `good-agent-task` | 0 |
| `bad-agent-task` | 0 |

## Judged Cards Per Target

| Judgment file | Target | Judged cards | Missed rows | Label counts |
|---|---|---:|---:|---|
| `arrayvec-pr138.toml` | `arrayvec-pr138` | 2 | 0 | `actionable` 1, `noise` 1 |
| `arrayvec-pr288.toml` | `arrayvec-pr288` | 6 | 0 | `actionable` 5, `uncertain` 1 |
| `bun-manual-candidates-first-pr-smoke.toml` | `bun-manual-candidates-first-pr-smoke` | 3 | 0 | `actionable` 3 |
| `crossbeam-pr1226.toml` | `crossbeam-pr1226` | 2 | 0 | `actionable` 1, `human-only` 1 |
| `hashbrown-pr667.toml` | `hashbrown-pr667` | 1 | 0 | `actionable` 1 |
| `memchr-capped.toml` | `memchr-capped` | 2 | 0 | `actionable` 1, `noise` 1 |
| `mio-pr1388.toml` | `mio-pr1388` | 1 | 0 | `human-only` 1 |

## Trust Boundary

These are labeled usefulness counts from maintainer judgment records for
static unsafe contract review only. They are not calibrated precision or
recall, not a benchmark, not proof of analyzer quality, not a proof of
memory safety, not UB-free status, not a Miri result, not Miri-clean
status, not site execution evidence, not witness adequacy, and not policy
readiness or policy gating.
