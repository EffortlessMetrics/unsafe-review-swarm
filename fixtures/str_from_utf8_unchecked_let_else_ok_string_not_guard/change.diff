diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,0 +1,19 @@
+pub fn decode(bytes: &[u8]) -> &str {
+    let _note = "let Ok(_) = core::str::from_utf8(bytes) else { return \"\"; };";
+    record_invalid();
+
+    // SAFETY: this fixture intentionally keeps let-else validation text in a string literal only.
+    unsafe { core::str::from_utf8_unchecked(bytes) }
+}
+
+fn record_invalid() {}
+
+#[cfg(test)]
+mod tests {
+    use super::decode;
+
+    #[test]
+    fn decodes_ascii() {
+        assert_eq!(decode(b"ok"), "ok");
+    }
+}
