diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,0 +1,30 @@
+pub struct Control {
+    ctrl: *const u8,
+    bytes: usize,
+}
+
+impl Control {
+    pub fn new(ctrl: *const u8, bytes: usize) -> Self {
+        Self { ctrl, bytes }
+    }
+
+    fn num_ctrl_bytes(&self) -> usize {
+        self.bytes
+    }
+
+    pub fn ctrl(&self, mut index: usize) -> *const u8 {
+        debug_assert!(index < self.num_ctrl_bytes());
+        index += 1;
+        // SAFETY: this fixture intentionally mutates the checked offset.
+        unsafe { self.ctrl.add(index) }
+    }
+}
+
+#[cfg(test)]
+mod tests {
+    use super::Control;
+
+    #[test]
+    fn ctrl_mutates_checked_index() {
+        let bytes = [0_u8; 4];
+        let control = Control::new(bytes.as_ptr(), bytes.len());
+        let _ = control.ctrl(0);
+    }
+}
