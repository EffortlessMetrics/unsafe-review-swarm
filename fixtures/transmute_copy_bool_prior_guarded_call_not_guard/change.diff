diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -11,0 +12,1 @@ pub fn copy_byte_pair_to_bools(value: u8, other: u8) -> (bool, bool) {
+    let second = unsafe { core::mem::transmute_copy::<u8, bool>(&other) };
