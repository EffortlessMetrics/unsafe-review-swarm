diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,0 +1,35 @@
+pub struct Control {
+    ctrl: *const u8,
+    bytes: usize,
+}
+
+impl Control {
+    pub fn new(ctrl: *const u8, bytes: usize) -> Self {
+        Self { ctrl, bytes }
+    }
+
+    fn num_ctrl_bytes(&self) -> usize {
+        self.bytes
+    }
+
+    pub fn ctrl(&self, index: usize) -> *const u8 {
+        let in_bounds = index < self.num_ctrl_bytes();
+        observe(in_bounds);
+        // SAFETY: this fixture intentionally observes bounds without enforcing them.
+        unsafe { self.ctrl.add(index) }
+    }
+}
+
+fn observe(_in_bounds: bool) {}
+
+#[cfg(test)]
+mod tests {
+    use super::Control;
+
+    #[test]
+    fn ctrl_observes_index_guard() {
+        let bytes = [0_u8; 4];
+        let control = Control::new(bytes.as_ptr(), bytes.len());
+        let _ = control.ctrl(0);
+    }
+}

