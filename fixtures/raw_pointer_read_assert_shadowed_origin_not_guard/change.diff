diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -5,6 +5,7 @@ pub fn read_header(bytes: &[u8], other: &[u8]) -> Header {
     assert!(bytes.len() >= core::mem::size_of::<Header>());
     let bytes = other;
     let ptr = bytes.as_ptr();
     // SAFETY: fixture checks that an assertion over a shadowed origin is stale.
+    unsafe { ptr.cast::<Header>().read() }
 }
