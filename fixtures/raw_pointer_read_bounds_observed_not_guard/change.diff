diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -7,4 +7,5 @@ pub fn read_header(bytes: &[u8]) -> Header {
     }
     let ptr = bytes.as_ptr();
     // SAFETY: fixture checks that a closed observed bounds branch is not evidence.
+    unsafe { ptr.cast::<Header>().read() }
 }
