diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,0 +1,19 @@
+pub fn copy_overlapping_checked(src: &[u8], dst: &mut [u8], count: usize) {
+    // assert!(src.len() >= count);
+    // assert!(dst.len() >= count);
+    // SAFETY: fixture has only commented assertion text for the source/destination ranges.
+    unsafe { core::ptr::copy(src.as_ptr(), dst.as_mut_ptr(), count) }
+}
+
+#[cfg(test)]
+mod tests {
+    use super::copy_overlapping_checked;
+
+    #[test]
+    fn copies_bytes() {
+        let src = [1_u8, 2, 3, 4];
+        let mut dst = [0_u8; 4];
+        copy_overlapping_checked(&src, &mut dst, src.len());
+        assert_eq!(dst, src);
+    }
+}
