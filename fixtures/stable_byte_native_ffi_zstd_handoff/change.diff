diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -13,1 +13,1 @@ pub fn zstd_overlap_handoff(input: &[u8], output: &mut [u8]) -> usize {
+    unsafe { zstd_compress_into(input.as_ptr(), input.len(), output.as_mut_ptr(), output.len()) }
